# Journal — septembre 2026, une entrée

Forme, plancher et règle d'écriture : `docs/journal/README.md`. Ce fichier ne porte qu'une entrée.
Le README le prévoit — une branche qui craint le conflit pose son propre fichier — et
`scripts/gates/gov-etat.ts` dérive les entrées du **système de fichiers**, jamais d'une liste tenue
à la main. L'entrée a été écrite en tête de `2026-09.md` puis déplacée ici : elle y conflitait avec
chaque branche sœur, et c'était le dernier point de friction de la file.

## PR #102 — 2026-09-23 — docs(GOV-063): ADR 0018 tranche l'homonymie, le nom `gov:check` est retire des deux cotes

**Fait.** `gov:check` designait DEUX choses : l'entree de `docs/gates.json`, la garde des termes
interdits, bloquante en porte A ; et une CHAINE de dix-sept gardes declaree dans `package.json`, qui
ne la contient pas. La garde imprimait `gov:check` dans son rouge, un developpeur a tape cette
commande, obtenu dix-sept verts, et conclu a un aleas de forge pendant qu'un synonyme interdit du
glossaire montait jusqu'a une PR ouverte. `partners/ADR-0018` retire le nom des deux cotes au lieu
de le reattribuer : l'entree du registre prend `gov:termes-interdits` — au registre, l'identifiant
EST la commande qui lance la garde — et la chaine prend `gov:partiel`, nom qui dit ce qu'elle est,
un sous-ensemble strict de la porte A. Tout ce que la garde imprime, vert comme rouge, derive
desormais de `ID_REGISTRE` exporte : le nom du rouge est la commande qui reproduit le rouge. Six
temoins tiennent la famille « un nom, une garde » a l'interieur du depot. Effet de bord gratuit : la
reconnaissance de l'appel par `gov:conventions` ne tient plus au litteral d'un COMMENTAIRE de
`ci.yml`.

**Reste.** La chaine `gov:partiel` est condamnee : elle disparait quand `pnpm prevol` (GOV-047,
PR #99) atterrit, et le temoin de cette PR-la devient MUET a la fusion de celle-ci — il lit la cle
`gov:check` de `package.json`, qui vaudra la chaine vide. Mesure par la lentille `mutation` sur la
branche `t/gov-047` : DEUX de ses trois assertions cessent de mesurer, pas trois, celle du milieu
lisant `ci.yml`. La correction est due dans #99 AVANT sa fusion. Sept lignes de verbes d'outillage
hors depot prescrivent encore `pnpm gov:check` ; le perimetre d'une garde s'arrete au depot, aucun
des six temoins ne peut les voir, et elles sont portees par le dossier de reprise. Une citation
EXECUTABLE du nom mort survit dans le depot, `scripts/gates/gov-depot.ts:459`, dans une fixture de
workflow conforme : hors des `paths` de GOV-063, non corrigee ici, sans porteur. `docs/tasks.json:36`
— l'acceptance de GOV-000 — prescrit toujours un `package.json` declarant `gov:check` : laissee
telle quelle a dessein, c'est le releve de ce que GOV-000 avait promis, et trois de ses clauses non
tenues sont suivies ailleurs. `docs/requirements.json` (REQ-GOV-001, REQ-GOV-004) ecrit encore
`pnpm gov:check` pour la chaine : fichier reserve au `gardien-spec`. QUATRE mutants survivent, et ils ne
sont pas tous de la meme lentille — cette tete de phrase a dit TROIS et les a tous attribues a
`mutation`, et le paragraphe la contredisait deux lignes plus bas au lieu de la reecrire. Le
premier est de `mutation` : `continue-on-error: true` pose sur l'etape de la garde ne fait rougir
personne, alors que le commentaire de `ci.yml` promet que `lint_non_bloquant` le refuse — cette
famille ne parcourt que les etapes de lint. Les TROIS autres sont des nombres graves dans le temoin
alors que son en-tete affirme qu'aucun ne l'est : deux planchers de population et un facteur deux.
La trouvaille en revient a `simplicite` et a `securite`. GOV-093 est versee dans cette PR : le
champ `schema` d'une tache et ses `paths` ne sont confrontes par rien, et aucun verbe hors depot ne
sait ecrire ce champ. Les trois clauses non tenues de GOV-000 ne sont pas rouvertes ici.

DEUX DETTES NEUVES, mesurees par `mutation` sur la reparation elle-meme, et c'est la MEME RACINE
que le defaut repare : juger par INCLUSION DE TEXTE au lieu d'APPARTENANCE EXACTE.
(1) La plus mordante — le controle qui noue l'identifiant du registre a la commande de
`package.json` fait `commande.includes(script)`. Ecrire `tsx scripts/gates/gov-check.ts --prove`
sous l'identifiant de la garde laisse les six temoins VERTS et `gov:conventions` vert : l'etape
bloquante DEDIEE de la porte A sort alors en ZERO sur un vert d'auto-test, sans nommer le fichier
fautif. C'est le defaut que cette PR ferme, deplace de `ci.yml` vers `package.json`.

⚠ CE PARAGRAPHE A SUREVALUE SA PROPRE DETTE, et la lentille `securite` l'a mesure. Il ecrivait
que **le depot reel n'est plus jamais lu** : c'est faux de la porte A prise dans son ensemble.
`pnpm test` en est une autre etape, et le temoin des termes interdits y rend DEUX
verdicts sur le depot reel — le compte porte sur les VERDICTS, non sur les lectures, que ce
fichier fait en bien plus d'endroits — dont un par un lanceur qui appelle le script PAR SON CHEMIN
— hors de `package.json`, donc
hors d'atteinte de la mutation. Mesure de `securite` sur un arbre isole : la porte A rougit quand
meme, UN TEST en echec sur les quarante-deux du SEUL fichier de temoin des termes interdits, et cet
echec imprime les trois fautes. ⚠ Cette phrase a d'abord ecrit « un FICHIER en echec sur
quarante-deux » : l'unite avait ete changee en recopiant le chiffre, la ligne des fichiers et le
denominateur de la ligne des tests cousus ensemble. Le depot porte soixante-cinq fichiers de test,
et il n'existe aucune population de quarante-deux fichiers. Telle qu'ecrite, la phrase affirmait
que la suite ENTIERE avait ete jouee et que quarante et un AUTRES fichiers etaient passes — donc
que le rayon d'action de la mutation avait ete mesure a l'echelle de la suite. Il ne l'a PAS ete :
ni `securite` ni `mutation` n'ont pu lancer Docker. Une fausse BONNE nouvelle sur la couverture
residuelle d'une garde, dans l'encadre meme qui repare une fausse mauvaise. Ce qui reste vrai
et qui suffit : l'etape DEDIEE est desarmee en silence, et une garde bloquante qui ne garde plus
rien ne devrait pas dependre d'une autre etape pour etre rattrapee. Une fausse mauvaise nouvelle se
propage comme une fausse bonne — elle aurait decredibilise la moitie vraie de la meme phrase.
(2) La reparation nomme un IDENTIFIANT, pas une FAMILLE : retirer de la porte A n'importe quelle
autre etape — mesure sur `gov:publication:prove` — laisse tout vert. Trente-cinq des soixante-dix
etapes distinctes de `ci.yml` n'y sont citees qu'une fois — et CE COMPTE EST UNE INCLUSION DE
SOUS-CHAINE, COMMENTAIRES COMPRIS — c'est-a-dire la lecture par prefixe que cette PR abolit —, ce
que le paragraphe doit
dire puisqu'il enseigne trois lignes plus bas qu'un compte doit etre celui que la commande rend.
Les quatre comptes, pour qu'aucun ne se rejoue a l'aveugle : sous-chaine tout le fichier 35,
sous-chaine hors commentaires 40, jeton exact tout le fichier 56, jeton exact hors commentaires 67
sur 70 — et NON 70 sur 70, que ce paragraphe a ecrit d'abord : trois etapes portent aussi leur
nom dans le `- name:` de leur propre etape, une VALEUR de YAML qu'aucun retrait de commentaires
n'enleve, `contracts:hash`, `req:check` et `test` — la dette est donc plus LARGE que le chiffre
annonce. Le plancher voisin, lui, laisse
vingt de marge. Aucune des deux n'est une panne
du code TEL QUE LIVRE : ce sont des mutations, donc des
dettes, et la regle d'arret de Will les laisse passer.

**Appris.** Deux PR peuvent se donner le meme numero d'ADR sans jamais entrer en conflit : les
fichiers portent des NOMS differents, `git` fusionne les deux, et c'est `gov:adr` qui decouvre la
collision apres coup, sur la famille `numero_double`. La renumerotation coute douze renvois, repartis
dans sept fichiers.

Et une lecon qui a coute la relecture. J'ai vu le renvoi `partners/ADR-0017` dans `ci.yml` apres la
fusion, demande `git log -1 --format="%h %s" origin/main -- .github/workflows/ci.yml`, lu le nom de la
PR #100, et conclu qu'elle avait ecrit cette ligne. **C'etait faux.** `git log -1 -- <fichier>` repond
a « quel est le dernier commit qui a TOUCHE ce fichier », jamais a « qui a ecrit CETTE ligne ». Les
douze renvois venaient tous de la branche ; `git grep -n "ADR-0017" <sha de main>` rend CINQ lignes,
toutes legitimes — le fichier d'ADR de la #100, `docs/adr/INDEX.md`, `docs/PLAN-STATE.md` et deux
lignes de l'entree de journal de la #100 — et AUCUNE dans les fichiers que j'avais accuses.

⚠ La phrase precedente disait TROIS, et c'est la lentille `exactitude` qui l'a mesuree a CINQ.
Le paragraphe dont la lecon est « mesure avec `git grep` AVANT d'ecrire » donnait la commande et se
trompait sur sa sortie. Meme remarque pour la branche : `git grep -n "ADR-0017" 5d64695` rend
QUATORZE lignes, pas douze — douze est le compte des renvois reecrits A LA MAIN, les deux autres
etant des vues derivees. **Un compte qui decrit une commande doit etre celui que la commande rend**,
et si le chiffre utile est un autre, il faut dire lequel et pourquoi. La fausse trouvaille s'est propagee
dans un message de commit, dans la premiere redaction de cette entree, dans le corps de la PR et dans
le brief remis aux lentilles, avant que la lentille `exactitude` ne la mesure et REFUSE la PR pour ce
motif. La question « cette ligne existe-t-elle sur cette base ? » se pose avec `git grep` sur un sha,
en dix secondes, AVANT d'ecrire ; la paternite d'une ligne se demande a `git blame` ou `git log -S`.
Plus une trouvaille est spectaculaire — « un defaut dormait sur `main` sans que personne le voie » —
plus elle doit etre mesuree avant d'etre ecrite.
