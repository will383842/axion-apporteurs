# Journal — septembre 2026, une entrée

Forme, plancher et règle d'écriture : `docs/journal/README.md`. Ce fichier ne porte qu'une entrée.
Le README le prévoit — une branche qui craint le conflit pose son propre fichier — et
`scripts/gates/gov-etat.ts` dérive les entrées du **système de fichiers**, jamais d'une liste tenue
à la main.

## PR #91 — 2026-09-23 — feat(INT-T09): mandataire recherche-entreprises — cache, limiteur, disjoncteur, projection

**Fait.** `src/server/integrations/recherche-entreprises/**` pose le mandataire serveur de l'API
publique de recherche d'entreprises : anti-rebond de 300 ms, requete minimale, cache de 24 h,
limiteur, disjoncteur et repli. RIEN DE CE QUE LE TIERS DIT D'UNE PERSONNE NE TRAVERSE : le schema
du tiers ne lit ni l'annee ni le mois de naissance, les dirigeants deviennent une empreinte et une
qualite, et le rendu envoye au navigateur ne porte aucune donnee nominative. Une panne du tiers
n'est jamais une erreur pour l'apporteur : 429, delai depasse, 5xx, reponse hors schema menent tous
a la saisie manuelle avec controle de Luhn, jamais a un 500, et l'en-tete de reprise est respecte.
Trois compteurs de la famille du depot entrent au registre de SEC-10, sous le plafond annonce par le
tiers. `scripts/gates/aucun-annee-de-naissance.ts` est la garde : pour chacune des 22 fixtures
enregistrees, la reponse passe par le VRAI mandataire et la VRAIE projection, et l'on verifie
qu'aucune cle interdite ne survit. Le job nocturne rejoue le contrat de forme contre l'API REELLE,
avec les memes saisies : une derive du tiers se voit la nuit, pas le jour d'une demonstration.

**Reste.** Le contrat nocturne appelle le reseau : il ne dit rien tant qu'il n'a pas tourne une
premiere fois en production, et son premier rouge sera une derive du tiers ou une panne de reseau,
sans que rien ne les distingue au premier coup d'oeil. La promesse « une meme personne presente dans
deux fixtures d'un meme enregistrement garde le meme pseudonyme » ne vaut que par EXECUTION de
l'enregistreur : un cas rejoue seul ne partage plus sa cle avec les autres, et c'est desormais ecrit
au-dessus du filtre qui le permet.

**Appris.** La fixture du cas « organisme de formation » saisissait « axion ia », et `gov:entite`
l'a REFUSEE : le SIREN et le SIRET de l'entite contractante y etaient RECOPIES. Ce n'est pas un
secret — ces numeros sont publics — c'est une SECONDE SOURCE, et c'est exactement ce que RM-01
interdit : le SIREN du contrat, celui du mandat et celui du virement ne sont le meme octet que parce
qu'un seul endroit le porte. Les vingt et un autres cas saisissaient deja des tiers ; celui-ci le
fait desormais aussi, et la saisie est « cegos ».

⚠ Et un second piege, du meme tour : la sortie de l'enregistreur n'est PAS toujours celle de
Prettier. `JSON.stringify(..., null, 2)` eclate sur trois lignes un tableau court que Prettier replie
sur une seule ; la porte A a rougi sur `Format` pour QUATRE lignes de blancs, aucune donnee changee
(verifie : les deux documents JSON sont egaux apres analyse). L'usage de l'enregistreur le dit
desormais : recapturer, PUIS `npx prettier --write` sur le dossier des fixtures.

A RETENIR — Et une lecon d'outillage qui a failli couter une heure : `gov:entite` a continue de rougir en
nommant les deux numeros alors qu'un `grep` sur le disque n'en trouvait plus aucun. Ce n'est pas une
panne, c'est le contrat de la garde : elle lit les octets du BLOB que l'INDEX associe a chaque
chemin, jamais l'arbre de travail, parce que c'est le blob que le depot PUBLIE et que git reecrit
l'arbre a l'extraction selon des attributs qu'on n'a pas a enumerer. ⚠ Corollaire a retenir : une
correction non indexee est invisible pour cette garde. `git add` FAIT PARTIE de la mesure.

**Porte A — les trois rouges de la premiere CI, et ce qu'ils cachaient.** Cette branche a vecu
EN CONFLIT, donc MUETTE en integration continue : elle a rencontre trois gardes de `main` pour la
premiere fois. Aucun des trois n'etait un defaut de la fusion.

**Un (REQ-GOV-031, `gardes-transposees`).** Le job `contrat-api-gouv` de `nightly.yml` lancait trois
commandes `npx tsx`, que la garde refuse : `npx` resout un paquet HORS du verrou, et c'est ce que la
forme fermee `pnpm <script>` existe pour empecher. Remede : trois scripts declares dans
`package.json` — `securite:annee-naissance`, `securite:annee-naissance:prove`,
`integration:contrat-gouv` —, appeles par le workflow. La garde n'a pas ete elargie. Les deux
en-tetes d'USAGE et la `preuveRouge` du registre disaient `npx` eux aussi : corriges, sinon le depot
enseignait la forme qu'il refuse.

**Deux (REQ-GOV-032, `refus-de-rendre-et-de-publier`).** La garde NEUVE
`scripts/gates/aucun-annee-de-naissance.ts` naissait avec deux sorties non nulles que le registre
chiffre ne declarait pas : « scripts/gates/aucun-annee-de-naissance.ts ajoute 2 `process.exit(1)` et
n'est PAS declare ici: expected undefined to be defined ». Elle s'y declare avec `total: 2`,
`porte: 2`, et `temoins: 0` ASSUME — ses temoins d'effet sur le binaire vivent dans
`recherche-entreprises-repli.spec.ts`, pas dans le tableau `REFUS` que ce compteur confronte. Le
cliquet global a ensuite rougi, et il a ete LU, pas devine : « le total declare a change sans que le
test ci-dessus rougisse: expected 46 to be 44 ». 44 → 46.

**Trois (REQ-SEC-028, `env-boot`).** `src/server/integrations/recherche-entreprises/production.ts`
citait deux noms de secret entre delimiteurs de chaine, dans un `Pick<>` sur le type des secrets. Ce
sont de VRAIS secrets : la garde avait raison sur la famille, et il n'y avait rien a corriger de son
cote. Remede : `src/lib/env.ts` nomme desormais les deux types `CleDesPersonnes` et `ClesDEmpreinte`,
et le cablage de production les IMPORTE. Le moindre privilege est conserve — la fabrique des
dependances ne recoit toujours que la cle des personnes — sans qu'aucun nom de secret soit retape
hors de sa source unique.

**Reste du (porte A).** La garde de REQ-SEC-028 est LEXICALE : elle ne distingue pas une recopie
d'une lecture verifiee par le compilateur, et les deux lui rendent le meme rouge. Ici le remede est
meilleur que le code refuse, donc rien n'est du ; mais le prochain consommateur d'un sous-ensemble de
secrets rencontrera la meme chose et devra lui aussi passer par un alias declare dans
`src/lib/env.ts`. Aucune tache du backlog ne porte cette limite, et elle n'est ecrite nulle part
ailleurs qu'ici. ⚠ Le total `46` du cliquet depend de l'ORDRE DE FUSION : sur la meme base 44, les
branches soeurs #82, #93 et #92 declarent 45, 46 et 48. Celle qui atterrira apres celle-ci lira son
propre rouge, et l'arbitrera a son tour.

**Relecture.** `exactitude` a refuse la tete `ee8b05c` : le demi-ouvert du disjoncteur n'etait juge
par rien. Retirer `|| essaiEnCours` (plusieurs essais en demi-ouvert) ou `demiOuvert ||` (un essai
manque apres un 429 ne rouvre plus) laissait 45/45 vert. Quatre temoins exercent maintenant le
disjoncteur reel, directement et par le mandataire : N echecs l'ouvrent, un seul essai part en
demi-ouvert (le second, concurrent, est refuse sans reseau), reussi il referme, manque il rouvre
meme sous le seuil. Les deux mutants ont ete rejoues un par un : chacun rougit sur deux temoins.
`simplicite` a refuse la reecriture de l'empreinte d'adresse (`partners.adresse.v1`, 64 hex) a cote
de `empreinteAdresse` de la frontiere (`partners.ip.v1`, 16 hex) : une meme adresse avait deux
empreintes sans lien, et le rapprochement de REQ-SEC-017 aurait du choisir. Le mandataire IMPORTE
desormais `empreinteAdresse` depuis `api-entrante.ts`, comme le fait SEC-08 (#126) : pas de
deplacement, donc pas de conflit avec elle ; un temoin exige la meme valeur des deux cotes, et les
specs de la frontiere restent vertes. Dettes soldees dans le meme tour : `Retry-After` plafonne a une
heure (`retryAfterPlafondMs`, temoin rouge avant), `ficheEntreprise` renommee
`ficheEntreprisePourServeur` (elle porte les empreintes des dirigeants), un seul `normaliser` et un
seul HMAC a etiquette dans le module, le journal passe par `creerJournal` de QA-T08 au lieu de
stderr, commentaire de `limiteur.ts` remis a 600 par heure. RESTE DU : la cle de cache d'une
recherche est un SHA-256 NON sale de la saisie ; la passer sous cle exige d'injecter une cle dans
le mandataire, hors de ce tour. `pnpm integration:contrat-gouv` n'a pas ete rejoue : il appelle
l'API reelle, et ce tour n'en fait aucun appel.

`mutation` a refuse la meme tete sur 26 survivants reels. Chacun a maintenant un temoin qui passe
par le code reel, vu rouge sous son mutant rejoue sur le banc de la lentille : TTL et expiration
Redis (le cache de production est desormais `cacheSurClient`, un Redis simule au niveau de son api
juge le vrai `set ... 'EX'`), fiche du SIREN demande et non du premier resultat, cache altere relu
comme une absence, plafond par adresse atteint en changeant d'identite, identite insensible aux
en-tetes du client, disjoncteur referme et non bloque apres un refus du debit global, 4xx sans effet
sur lui, echec ferme de la diffusion sur une valeur inconnue, cle du SIREN d'un SIRET, SIRET refuse
pour SIREN, moitie « fiche » de la garde (temoin par `jugerLeDepot`), code de sortie du mode depot
(option `--fixtures`), et le cablage de `dependancesDeProduction` (cle, adresse, delai, `REDIS_URL`).
Aucun de ces mutants ne revelait un defaut du code livre : M09 en particulier (`!== 'P'`) est
l'inverse de ce que fait `=== 'O'`, deja ferme ; il manquait le temoin, pas la regle.
