# Registre des exigences — Axion Apporteurs

> ⚠️ **Ce fichier est une VUE. La source est `docs/requirements.json`.**
> Regénéré par `pnpm gov:requirements --render`, jamais édité à la main : une correction
> tapée ici disparaît à la régénération suivante.
> `pnpm gov:requirements --verifie-rendu` rougit si ce fichier a dérivé de sa source, et
> NOMME l’écart en nombre d’exigences (REQ-GOV-032). Jusqu’au 2026-09-05, aucune garde ne
> comparait les deux : la vue annonçait 353 exigences pour 354 au registre.
>
> **Aucun total n'est écrit à la main.** Trois comptages différents ont circulé dans les documents
> sources, tous faux. Ceux qui suivent sont comptés à la génération.
>
> **Dépôt public** — les renvois à la note d'analyse interne apparaissent sous la forme
> « note interne (hors dépôt) », et les seuils comme les montants du réseau vivent en
> configuration (`REQ-GOV-031`, garde `pnpm gov:publication`).

## Ce que porte le registre

| | Nombre |
| --- | ---: |
| Exigences | **355** |
| — dont actives | 321 |
| — dont absorbées par une autre (l'identifiant résout encore) | 34 |
| — dont retirées | 0 |
| Exigences couvertes par au moins une tâche | 354 |
| Exigences sans porteur | 1 |

## Couverture des 21 modules de l'audit de bout en bout

| # | Module | Exigences |
| ---: | --- | ---: |
| 1 | Recrutement & candidature | 13 |
| 2 | Scoring & priorisation | 3 |
| 3 | Entretien / webinaire | 1 |
| 4 | KYC & conformité | 19 |
| 5 | Contrat & signature électronique | 19 |
| 6 | Onboarding automatique | 1 |
| 7 | Enablement | 1 |
| 8 | Espace apporteur | 16 |
| 9 | Dépôt de contact | 42 |
| 10 | Vérification d'entreprise | 6 |
| 11 | Pipeline & statuts | 5 |
| 12 | Qualification structurée | 15 |
| 13 | Moteur de commissions | 33 |
| 14 | Parrainage | 15 |
| 15 | Relevés, approbation, paiement | 23 |
| 16 | Autofacturation & fiscal | 9 |
| 17 | Console de pilotage | 16 |
| 18 | Suivi d'activité par apporteur | 3 |
| 19 | Animation & ré-engagement | 1 |
| 20 | Support & messagerie | 3 |
| 21 | Suspension, résiliation, offboarding | 11 |

## Couverture des 12 étapes du parcours

| # | Étape | Exigences |
| ---: | --- | ---: |
| 1 | Sourcing | 3 |
| 2 | Candidature | 12 |
| 3 | Entretien / webinaire | 1 |
| 4 | KYC | 21 |
| 5 | Contrat | 20 |
| 6 | Onboarding | 2 |
| 7 | Activité | 59 |
| 8 | Qualification | 15 |
| 9 | Vente | 26 |
| 10 | Commission | 70 |
| 11 | Relevé & paiement | 31 |
| 12 | Fin de collaboration | 14 |

## Exigences

Chaque entrée porte son **module** (1-21), son **étape** (1-12), la **phase** où elle est
livrée — la plus précoce de ses tâches porteuses — et **les tâches qui la prouvent**.
Une exigence sans tâche n'est portée par personne : `gov:requirements` la nomme.

### domaine-donnees

- **REQ-DM-001** — Toute colonne monétaire de Partners est de type Int, exprimée en centimes, HT, signée, suffixée `Cents` ; aucune colonne Float/Decimal ni valeur en euros n'existe dans le schéma.
  <br>_phase 0 · tâches : `DM-01`, `DM-02`_ · _source : tableaux-de-bord §2.1, §2.4 ; nouvelle (F-DM-16)_
- **REQ-DM-002** — La clé d'attribution est le SIREN normalisé (CHECK siren ~ '^[0-9]{9}$') ; le SIRET est stocké en contexte (CHECK 14 chiffres) et, s'il est présent, ses 9 premiers chiffres égalent le SIREN (CHECK).
  <br>_phase 1 · tâches : `DM-07`_ · _source : audit-attribution §2.3, P5 ; fonctionnement R1_
- **REQ-DM-003** — Au plus une attribution occupante par SIREN : index unique partiel `ON attributions(siren) WHERE statut IN (ETATS_OCCUPANTS)` où ETATS_OCCUPANTS = {provisoire, active, rdv_pris, proposition, signee, convertie, figee_resiliation}, la liste étant une constante unique partagée par le code et la migration.
  <br>_module 11 · étape 9 · phase -1 · tâches : `DM-02`, `DM-07`, `GOV-006`_ · _source : anti-abus F6, M2 ; outil §8 ; nouvelle (F-DM-05)_
- **REQ-DM-004** — Au plus deux attributions `en_attente` par SIREN (index unique partiel sur (siren, rangAttente) WHERE statut = 'en_attente', CHECK rangAttente IN (1,2)) ; au-delà de deux, la déclaration **n'est pas conservée** et l'apporteur en est informé. 🔴 **La libération d'une occupante ne promeut plus rien automatiquement** (corrigé le 2026-09-03, M-26 — « lui revient automatiquement », sans terme et sans limite de nombre, rouvrait par la fenêtre la reconduction que l'art. 3.4 bis ferme par la porte, et permettait à un apporteur de se constituer une file d'attente permanente sur 300 SIREN) : la machine **notifie** l'apporteur dont la déclaration est au rang 1 et lui ouvre une fenêtre de **quinze jours** (`FILE_FENETRE_REDECLARATION_JOURS`, SSOT) pour **déclarer à nouveau** l'entreprise ; **aucune attribution ne naît d'une déclaration conservée sans nouvelle déclaration expresse**. À l'expiration de la fenêtre, la déclaration en attente est effacée et l'entreprise redevient librement déclarable par tous. Une déclaration en attente s'éteint en tout état de cause **douze mois** après son enregistrement (`FILE_EXPIRATION_MOIS`, SSOT). Tests frontière : J+14 la re-déclaration crée l'attribution, J+16 elle est traitée comme une déclaration ordinaire sur une entreprise libre ; deux passages du cron n'effacent qu'une fois.
  <br>_module 9 · étape 10 · phase 1 · tâches : `DM-07`, `DM-08`, `DM-13`_ · _source : anti-abus M2, C4 ; contrat art. 3.5 al. 2 et art. 3.4 bis ; synthèse du 2026-09-03, M-26_
- **REQ-DM-005** — `deposeeAt` est l'horodatage de la transaction serveur (now() Postgres, timestamptz(6)) ; il n'est jamais fourni par le client ; l'égalité est départagée par l'id séquentiel de l'événement de dépôt ; `clientCapturedAt` est informatif et non opposable.
  <br>_module 9 · étape 7 · phase 1 · tâches : `DM-07`_ · _source : fonctionnement R3 ; contrat art. 1.5 ; nouvelle (F-DM-15)_
- **REQ-DM-006** — Le statut d'attribution est un enum fermé {en_attente, provisoire, active, rdv_pris, proposition, signee, convertie, figee_resiliation, invalidee, perdue, perimee, expiree, annulee} ; toute transition passe par une matrice `from × événement → to` ; une transition absente de la matrice lève une erreur typée et n'écrit rien.
  <br>_étape 9 · phase 1 · tâches : `DM-08`, `DM-13`, `DM-24`_ · _source : outil §8 ; anti-abus M1/M4 ; fonctionnement §9 ; nouvelle (F-DM-06)_
- **REQ-DM-007** — `fenetreFinAt` (12 mois) et `peremptionAt` (`PEREMPTION_JOURS`, 90 j) sont des colonnes recalculées par la machine à chaque transition. 🔴 **Le chrono de péremption ne court qu'à compter du premier contact de la Société avec l'entreprise déclarée** (`premierContactAt`, posé par la Qualification) : `peremptionAt` est **null tant que ce contact n'a pas eu lieu** (chrono suspendu) et devient null définitivement dès qu'une suite existe (rdv_pris ou plus). L'attribution **n'expire pas** au titre de la péremption lorsque l'absence de rendez-vous, de devis et de commande est **imputable à la Société** : le marqueur `peremptionSuspendueMotifSociete` (posé par un rôle habilité, motivé, journalisé) neutralise le chrono, seule la fenêtre de 12 mois s'appliquant alors. Sans cette double règle, les trois événements qui sauvent l'attribution dépendent exclusivement de la Société et la péremption devient une condition potestative (art. 1304-2 et 1304-3 al. 1 C. civ.). Tests : dépôt sans contact à J+200 → `peremptionAt` null ; contact posé à J+200 → péremption à J+290 ; marqueur posé → aucun passage en `perimee` au cron.
  <br>_module 12 · étape 9 · phase 1 · tâches : `DM-08`, `DM-13`_ · _source : anti-abus M4, C7 ; fonctionnement R4 ; contrat art. 3.4 al. 2 et 3 ; synthèse du 2026-09-03, A-6_
- **REQ-DM-008** — Une Qualification est une entité liée à une attribution avec `resultatContact` enum {confirme, non_confirme, injoignable, ne_se_souvient_pas} obligatoire, `interet` enum, `prochaineEtape` enum, `motifPerte` enum nullable, auteur et horodatage serveur ; elle est append-only (une nouvelle qualification ne modifie jamais la précédente). 🔴 **Seul `non_confirme` — le représentant de l'entreprise indique EXPRESSÉMENT n'avoir eu aucun échange avec l'apporteur — éteint l'attribution et ouvre l'article 3.7** ; `injoignable`, `ne_se_souvient_pas`, le changement d'interlocuteur et le refus de répondre **maintiennent l'attribution `provisoire`** et ne sont imputables à personne (aucune suspension, aucun signal défavorable, aucune invalidation). La Qualification journalise la **date**, la **personne interrogée** et les **termes** de la réponse ; l'extrait en est communiqué à l'apporteur sur sa demande (route dédiée, cloisonnée). Tests : chacune des quatre valeurs → état d'attribution attendu ; `injoignable` ×3 → aucune suspension.
  <br>_module 12 · étape 8 · phase 1 · tâches : `DM-09`, `DM-24`_ · _source : outil §8 ; anti-abus M1, C2 ; contrat art. 3.2 al. 3 et 3.7 al. 2 ; synthèse du 2026-09-03, A-3 et M-7_
- **REQ-DM-009** — Le taux de confirmation et le palier d'un apporteur sont dérivés par fonction pure des N dernières Qualifications (fenêtre glissante de 10) ; aucun compteur mutable n'est stocké.
  <br>_module 12 · étape 8 · phase 1 · tâches : `DM-09`_ · _source : anti-abus M1, M3 ; outil §1.1_
- **REQ-DM-010** — `Apporteur.seuilVerificationPrioritaire` est une colonne Int (défaut dérivé du palier, surcharge manuelle tracée) ; aucun dépôt n'est refusé pour dépassement de seuil — au-delà, l'attribution porte `verificationPrioritaire = true`.
  <br>_module 9 · étape 7 · phase 0 · tâches : `DM-06`, `DM-09`, `JUR-T24`, `SEC-12`_ · _source : outil §1.1, D3_
- **REQ-DM-011** — Le statut d'apporteur est un enum {candidat, retenu, vivier, refuse, kyc_en_cours, pret_a_signer, signe, suspendu, resilie} avec matrice de transitions ; `resiliationMotif` enum {ordinaire_apporteur, ordinaire_axion, manquement_grave} — le mot « faute » est proscrit (décision du 2026-09-03), le vocabulaire disciplinaire est retiré du produit ; la résiliation applique ses effets sur les attributions et les lignes dans une seule transaction qui écrit un événement par effet.
  <br>_module 4 · étape 4 · phase 0 · tâches : `DM-06`, `DM-18`, `SEC-19`_ · _source : outil §5 étape 12 ; anti-abus F9, §4 ; nouvelle (F-DM-12, F-DM-24)_
- **REQ-DM-012** — Un apporteur porte deux identifiants distincts : `codeParrainage` public unique (lisible, non énumérable : préfixe + 6 caractères aléatoires) et un ou plusieurs `JetonDepot` {tokenHash unique, creeAt, revoqueAt nullable, dernierUsageAt} ; le quota, les déclarations non confirmées et l'horodatage se comptent sur l'apporteur, jamais sur le code public (le mot « strike » est proscrit, décision du 2026-09-03).
  <br>_module 14 · étape 10 · phase 0 · tâches : `DM-06`_ · _source : anti-abus M3, C6 ; outil §5 étape 6_
- **REQ-DM-013** — Un Contrat est versionné {apporteurId, gabaritVersion, grilleVersionId, docusealSubmissionId, signedAt, pdfHash SHA-256, auditTrailJson, statut enum {envoye, signe, remplace, resilie}} ; un apporteur a au plus un contrat `signe` en vigueur (index unique partiel).
  <br>_module 5 · étape 5 · phase 1 · tâches : `DM-11`, `DM-23`, `INT-T23`_ · _source : outil §6.2, D10_
- **REQ-DM-014** — La grille de commission est une entité versionnée GrilleCommission {version, contenuJson, hash, publieeAt} dont le contenu est DÉRIVÉ par export de `COMMERCIAL_COMMISSIONS` (axionia) et jamais retapé ; Contrat, Attribution et LigneCommission référencent une version ; le montant d'une ligne ne change jamais après publication d'une nouvelle grille.
  <br>_module 13 · étape 10 · phase 0 · tâches : `DM-03-A`, `DM-03-P`_ · _source : plan D3 ; outil §6.2 ; nouvelle (F-DM-09)_
- **REQ-DM-015** — Le calcul de commission est une fonction pure (grilleVersion, activite: ActiviteFacturation, jours: number|null, montantHtCents) → {montantCents} | {bloque: motif} couvrant les 5 valeurs d'ActiviteFacturation ; un cas sans barème produit le blocage « barème indéfini », jamais 0 ni une exception (statut `bloquee`, `motifBlocage = a_qualifier`, libellé apporteur « Prestation hors grille de commissions » — cf. REQ-ARG-017 et l'arbitrage l. 410). 🔴 **Le forfait est dû UNE FOIS par commande portant le palier : `flat = flatEur × 100`, JAMAIS multiplié par les journées** — la durée est déjà portée par le palier lui-même (un palier de deux journées porte deja le forfait des deux journées dans la grille publiée). Multiplier une seconde fois par `jours` double la commission sur tous les paliers pluri-journées ; `jours` n'entre dans aucun forfait et ne sert qu'à identifier le palier. `percent` = `round(percent × montantHtCents / 100)`. Test frontière **vu rougir avant correction** : palier forfaitaire appelé avec `jours = 2` → montant simple, jamais doublé.
  <br>_module 13 · étape 9 · phase 0 · tâches : `DM-04`_ · _source : audit-attribution §3.3 ; pricing.ts ; annexe 1 du contrat, chapeau ; nouvelle (F-DM-08) ; synthèse des cinq examens du 2026-09-03, A-2_
- **REQ-DM-016** — Une ligne `acquise` naît uniquement d'un événement `paiement.recu` de statut `succeeded`, idempotent sur (source, eventId) ; unicité (paymentId, attributionId, type) ; aucun autre déclencheur (ni devis signé, ni facture émise) ne crée une ligne acquise.
  <br>_étape 9 · phase 2 · tâches : `DM-15`_ · _source : tableaux-de-bord §2.3 ; fonctionnement R7 ; audit-attribution P6_
- **REQ-DM-017** — 🔴 **Prorata — formule unique du dossier, en entiers, numérateur et dénominateur de MÊME nature.** La part acquise au ième encaissement vaut `floor(commissionTotaleCents × cumulEncaisseTtcCents_i / factureTtcNetCents) − Σ parts déjà acquises` ; aucune part n'est négative ; aucune part ne dépasse la commission totale ; à facture soldée, Σ des parts = `commissionTotaleCents` exactement (le dernier encaissement absorbe le reliquat d'arrondi par construction, sans traitement particulier). ⚠️ **Trois formules coexistaient dans le dossier** (contrat art. 4.3 « HT sur TTC », `REQ-ARG-004` « floor cumul TTC/TTC », l'ancien corps de cette exigence « round HT/HT ») ; le contrat a été corrigé le 2026-09-03 (A-1) et **cette formule est désormais la seule**, identique mot pour mot à `REQ-ARG-004` et à l'article 4.3. Un ratio dont le numérateur est HT et le dénominateur TTC ampute chaque part d'un sixième à 20 % de TVA et empêche la somme d'atteindre le total : un test frontière (facture 14 400 TTC / 12 000 HT en 3 × 4 800) le voit rougir avant correction.
  <br>_module 13 · étape 10 · phase 0 · tâches : `DM-04`, `DM-15`_ · _source : tableaux-de-bord §2.4, B1 ; arbitrage de contradiction l. 411 ; contrat art. 4.3 ; synthèse des cinq examens du 2026-09-03, A-1_
- **REQ-DM-018** — Le HT encaissé est fourni par axionia dans le webhook (amountHtCents dérivé de facture.montantHtCents / montantTtcCents, repli montantHtCents + montantTvaCents si montantTtcCents null) ; Partners n'infère jamais un taux de TVA.
  <br>_étape 10 · phase -1 · tâches : `DM-15`, `INT-T01b`_ · _source : tableaux-de-bord §2.4, B2 ; nouvelle (F-DM-03)_
- **REQ-DM-019** — Une reprise est une ligne de type `reprise`, montant négatif, `sourceLigneId` FK vers la ligne d'origine, `sourceType` **repris à l'identique du `motif` de l'événement `paiement.rembourse`** (REQ-INT-032, enum **{remboursement, avoir, litige_tranche, rejet_prelevement, virement_rappele, geste_commercial, inexecution_societe, autre}**) + `sourceId` uniques ensemble ; la ligne d'origine n'est jamais modifiée ni supprimée. 🔴 **Le fait générateur d'une reprise est l'ANNULATION DE L'ENCAISSEMENT, quelle qu'en soit la cause** — pas seulement un remboursement décidé par Axion-IA ni un avoir : un litige tranché en faveur du client, un prélèvement rejeté après coup ou un virement rappelé produisent le même effet (l'argent repart) et doivent produire une reprise. 🔴 **Deux motifs ne produisent AUCUNE reprise** (corrigé le 2026-09-03, M-11) : `geste_commercial` — restitution décidée par la Société **sans réclamation du client** — et `inexecution_societe` — restitution résultant de l'inexécution par la Société de ses propres obligations. Éteindre une créance acquise par la seule volonté du débiteur est ce que l'article 4.5 du contrat exclut désormais expressément ; la ligne d'origine reste acquise, l'événement est journalisé et affiché en console, aucune ligne négative n'est créée. Le délai de reprise court depuis **la date de l'annulation**, jamais depuis l'encaissement d'origine. ⚠️ Aucun événement `avoir.emis` n'est transporté en V1 (arbitrage l. 413 et l. 418) : le motif `avoir` n'est atteignable que **par le champ `motif` de `paiement.rembourse`**, jamais par un sixième événement — en ajouter un exige un ADR amendant D1. Tests : chacun des huit motifs → reprise ou absence de reprise attendue, les deux exclusions **vues rougir** avant correction.
  <br>_module 17 · étape 10 · phase 2 · tâches : `DM-15`_ · _source : contrat art. 4.5 · REQ-INT-032 · constat de la session axion-ia-6b, 2026-09-03 ; le dépôt porte un runbook `R32-stripe-dispute-chargeback`, le cas n'est pas théorique ; synthèse du 2026-09-03, M-11_
- **REQ-DM-020** — LigneCommission sépare `type` enum {commission, reprise, parrainage, bonus_filleul} et `statut` enum {prevue, acquise, bloquee, a_payer, payee, annulee} — **6 valeurs ; `dechue` est retirée de l'enum le 2026-09-03 (décision D11), aucun chemin de code ne peut la produire (REQ-JUR-007)** ; `motifBlocage` enum nullable non null ssi statut = bloquee (CHECK) ; une ligne `prevue` ne peut jamais entrer dans un relevé (CHECK releveId IS NULL OR statut IN ('a_payer','payee')).
  <br>_module 14 · étape 10 · phase 2 · tâches : `T-ARG-010`_ · _source : tableaux-de-bord §2.6 ; outil §6.3 ; nouvelle (F-DM-10)_
- **REQ-DM-021** — La résolution d'un encaissement cherche l'attribution occupante du `beneficiarySiren` fourni par axionia, sinon un RattachementManuel actif ; un encaissement sans SIREN bénéficiaire ou sans facture crée une ligne `bloquee` motif `siren_manquant` et une Anomalie — jamais un silence.
  <br>_étape 10 · phase 0 · tâches : `DM-15`, `INT-T03`_ · _source : fonctionnement R13 ; tableaux-de-bord B11 ; anti-abus F12 ; nouvelle (F-DM-04)_
- **REQ-DM-022** — **Définition unique de la commande** (contrat art. 4.4, corrigé le 2026-09-03, M-10) : le **devis, le bon de commande ou la convention de formation signé** par l'entreprise attribuée ; la date retenue (`dateRef`) est celle de **cette signature** et, **à défaut de document signé, la date d'émission de la première facture** (`facture.emiseAt`) — le repli existait dans le code sans figurer au contrat, il y figure désormais. Une commande est dans la fenêtre si `dateRef` ∈ [dateDeDepart, fenetreFinAt] ; une ligne est créée pour tout état ∈ ETATS_OCCUPANTS (REQ-DM-003) ; les encaissements postérieurs à `fenetreFinAt` d'une commande signée dans la fenêtre restent commissionnés ; une commande signée hors fenêtre ne crée aucune ligne, même `prevue`.
  <br>_module 11 · étape 10 · phase 1 · tâches : `DM-08`, `DM-15`_ · _source : fonctionnement R2, §5.2 ; audit-attribution §3.1 ; contrat art. 4.4 ; synthèse du 2026-09-03, M-10_
- **REQ-DM-023** — Parrainage {parrainId, filleulId unique, codeUtilise, captureAt, contratFilleulSigneAt, finAt = contratFilleulSigneAt + 12 mois, tauxPct versionné} avec CHECK parrainId <> filleulId ; chaque ligne `commission` ou `reprise` du filleul acquise avant finAt engendre une ligne `parrainage` avec sourceLigneId ; aucune ligne n'est jamais créée pour le parrain du parrain (test).
  <br>_module 14 · étape 10 · phase 2 · tâches : `DM-16`_ · _source : plan §4.1 ; fonctionnement R8, §6 ; anti-abus F14 ; outil §12.1_
- **REQ-DM-024** — Le journal Evenement est append-only et chaîné (prevHash, selfHash = SHA-256(prevHash + payload canonique)) ; un trigger Postgres refuse UPDATE et DELETE ; toute transition d'agrégat (attribution, apporteur, ligne, relevé, KYC, contrat) écrit son événement dans la même transaction.
  <br>_module 4 · étape 4 · phase 0 · tâches : `DM-01`, `DM-20`_ · _source : audit-attribution P9 ; outil §9.1 ; nouvelle (F-DM-14)_
- **REQ-DM-025** — Un Relevé fige ses lignes (LigneCommission.releveId, une ligne dans au plus un relevé), est unique par (apporteurId, periode), porte un `numeroAutofacture` séquentiel sans trou par année (séquence Postgres, alloué à l'approbation) et devient immuable au statut `approuve`.
  <br>_module 13 · étape 11 · phase 2 · tâches : `T-ARG-015`_ · _source : tableaux-de-bord §2.7, B3 ; outil §6.3 ; nouvelle (F-DM-22)_
- **REQ-DM-026** — Un Paiement porte `endToEndId` unique conforme au jeu de caractères SEPA (≤ 35 caractères, [A-Za-z0-9/-?:().,'+ ]), `montantCents`, `executeAt`, `rapprocheAt`, `referenceBancaire` ; le passage des lignes à `payee` n'a lieu qu'au rapprochement.
  <br>_module 15 · étape 11 · phase 2 · tâches : `T-ARG-010`, `T-ARG-019`_ · _source : outil §6.3, D8_
- **REQ-DM-027** — PieceKyc {apporteurId, type enum {siret, tva, rib, identite, vigilance, rc_pro}, statut enum {manquante, a_verifier, valide, perimee, refusee}, verifieeAt, expireAt (**NOT NULL si type = vigilance OU rc_pro**), fichierRef, fichierPurgeAt} unique par (apporteurId, type) ; la fonction pure `piecesBloquantPaiement(pieces, cumulContratHtCents, seuilCents)` décide du blocage. 🔴 **`rc_pro` porte une échéance et un rappel, et ne bloque AUCUN versement** (corrigé le 2026-09-03, A-10) : l'attestation de responsabilité civile professionnelle est exigée à la signature puis à chaque échéance annuelle (contrat art. 6.4), avec rappel J-30 et alerte console à l'expiration, mais elle **n'entre pas dans `piecesBloquantPaiement()` ni dans `controlesVersement()`** — l'art. 5.4 réécrit ferme la liste des pièces pouvant différer un versement à SIREN valide, coordonnées bancaires au nom de l'apporteur, et attestation de vigilance **au seul seuil légal**. Tests : `rc_pro` périmée → alerte console, relevé **payé** ; ajouter `rc_pro` à la liste bloquante → gate rouge (test-cliquet nominatif sur le contenu de la liste).
  <br>_module 4 · étape 4 · phase 1 · tâches : `DM-11`, `DM-19`, `JUR-T16`, `T-ARG-032`_ · _source : outil §6.1, §3.3, D6, D7 ; contrat art. 5.4 et 6.4 ; synthèse du 2026-09-03, A-5 et A-10_
- **REQ-DM-028** — SirenListeNoire {siren PK, motif enum {opco, france_travail, region, of_partenaire, autre}, ajoutePar, ajouteAt} est consultée au dépôt, à la vérification et à la réception de client.* ; un SIREN listé ne peut être ni attribué ni EntrepriseConnue attribuable.
  <br>_module 9 · étape 7 · phase 1 · tâches : `DM-10-P`_ · _source : fonctionnement R14 ; tableaux-de-bord B12 ; anti-abus M5_
- **REQ-DM-029** — EntrepriseConnue {siren, origine enum **{client, devis, financeur}**, connueDepuisAt, dernierContactAt, clientStatut enum miroir} est alimentée exclusivement par les webhooks d'axionia (`client.*`, `devis.signe`) et, pour `financeur`, par la saisie manuelle de la liste de REQ-DM-028. 🔴 **`demande_entrante` est RETIRÉE de l'enum** (arbitrage l. 424, confirmé le 2026-09-03) : aucun des cinq événements arrêtés ne la transporte, le contrat ne peut donc pas promettre une antériorité tirée d'« une demande entrante de moins de quatre-vingt-dix jours » — la mention a été retirée de l'article 3.3, et elle ne peut y revenir qu'avec un sixième événement, donc un **ADR amendant la décision D1**. 🔴 **Deux fenêtres distinctes, alignées sur l'article 3.3 réécrit** (A-12), et non un paramètre unique : `ANTERIORITE_CLIENT_MOIS = 24` (entreprise **cliente au titre d'une prestation facturée** au cours des 24 derniers mois) et `ANTERIORITE_DEVIS_MOIS = 6` (entreprise **destinataire d'un devis** de moins de 6 mois), toutes deux dans la SSOT avec leur source (REQ-JUR-015). L'évaluation est **locale**, sans appel réseau au dépôt : « le contrôle est opéré sur les données dont la Société dispose à cet instant », ce que le miroir asynchrone permet — et le contrat ne promet plus un contrôle « automatique et immédiat » sur des données qu'il n'a pas. Tests frontière : facture à 23 mois → refus, à 25 mois → dépôt accepté ; devis à 5 mois → refus, à 7 mois → accepté ; `demande_entrante` présente dans l'enum → gate rouge.
  <br>_module 9 · étape 9 · phase 1 · tâches : `DM-10-P`_ · _source : contrat art. 3.3 ; anti-abus F13, C12 ; arbitrage l. 424 ; nouvelle (F-DM-17) ; synthèse du 2026-09-03, A-12_
- **REQ-DM-030** — Au dépôt, l'attribution stocke structurés, tels que rendus par l'API recherche-entreprises : raisonSociale, siret, codePostal, commune, departement, region, codeNaf, trancheEffectif, natureJuridique, etatAdministratif, dirigeantsApiJson ; aucune adresse en texte libre seul.
  <br>_module 10 · étape 7 · phase 1 · tâches : `DM-07`, `DM-21`_ · _source : anti-abus M5, C10 ; outil §1.2_
- **REQ-DM-031** — Les coordonnées du contact rencontré sont chiffrées (nom, téléphone, email) et doublées de hashes HMAC déterministes (email, téléphone) pour la détection et le droit d'accès ; `purgeContactAt` est calculé et exécuté par cron, qui écrit un événement.
  <br>_phase 1 · tâches : `DM-07`, `DM-13`, `DM-20`_ · _source : audit-attribution P8 ; outil §5 étape 12 ; nouvelle (F-DM-25)_
- **REQ-DM-032** — Verification {apporteurId, siren, resultat enum {libre, suivie, cliente, liste_noire, fermee}, at} est écrite à chaque consultation ; le plafond 30/jour est dérivé par comptage ; AlerteLiberation est unique par (apporteurId, siren) et consommée à l'envoi.
  <br>_phase 1 · tâches : `DM-12`_ · _source : outil §1.3, D5_
- **REQ-DM-033** — Anomalie {type enum, score Int, apporteurId, attributionId nullable, statut enum {ouverte, levee, confirmee}, traitePar, traiteAt} ; le gel des dépôts d'un apporteur (`depotsGelesDepuis`) n'est posé que par une Anomalie confirmée ou par une déclaration `non_confirme`, et chaque pose/levée écrit un événement.
  <br>_module 9 · étape 7 · phase 1 · tâches : `DM-12`, `SEC-14`_ · _source : anti-abus F8, M3 ; outil §9.2_
- **REQ-DM-034** — RattachementManuel {attributionId, sirenCommande, motif ≥ 20 caractères, `lienControleEtabliAt`, `lienControleSource`, decidePar, decideAt, revoqueAt} avec unique partiel (sirenCommande) WHERE revoqueAt IS NULL ; c'est l'unique exception à la règle « zéro arbitrage » et elle est tracée. 🔴 **Le rattachement n'est jamais automatique** (le contrat le disait, le produit disait l'inverse — corrigé le 2026-09-03, M-8 : aucune source ne rend de lien capitalistique, l'API recherche-entreprises pas davantage) : il est **décidé par la Société**, motivé, tracé et **notifié à l'apporteur avec son motif** ; l'apporteur peut le **demander** en justifiant du lien de contrôle, et la décision lui est notifiée motivée dans les quinze jours. 🔴 **Le lien de contrôle (L.233-3 C. com.) doit exister à la DATE DE LA DÉCLARATION** et être établi par les données publiques disponibles à cette date (`lienControleEtabliAt` ≤ `attribution.deposeeAt`, `lienControleSource` non vide) : un lien né postérieurement n'ouvre aucun droit — sans quoi un rachat postérieur étendrait seul le périmètre de l'apporteur. Tests : lien daté après le dépôt → rattachement refusé (vu rougir) ; rattachement sans `lienControleSource` → refus ; demande de l'apporteur sans réponse à J+16 → alerte console.
  <br>_module 9 · étape 7 · phase 1 · tâches : `DM-12`, `T-ARG-035`_ · _source : fonctionnement R11 ; contrat art. 3.6 al. 2 ; anti-abus F15 ; synthèse du 2026-09-03, M-8_
- **REQ-DM-035** — Apporteur porte le snapshot de candidature : candidatureId, reponsesJson, scoreInitial, scorePartsJson, scoreBaremeVersion (hash de SCORE_POIDS), sourceCanal enum, parrainCodeCapture ; le barème SSOT est `scoring.ts`.
  <br>_module 1 · étape 2 · phase 0 · tâches : `DM-06`, `INT-T22`_ · _source : plan §4.4 ; anti-abus C15 ; nouvelle (F-DM-01, F-DM-13)_
- **REQ-DM-036** — WebhookRecu {source enum {axionia, docuseal}, eventId unique par source, type, payloadHash, receivedAt, processedAt, error, retryCount} — dérivé du patron DocusealWebhookEvent ; rejouer un événement déjà traité ne produit aucune écriture métier.
  <br>_module 5 · étape 5 · phase -1 · tâches : `INT-T01b`_ · _source : outil §2.3 ; schema.prisma DocusealWebhookEvent_
- **REQ-DM-037** — Les migrations sont additives (aucune suppression de colonne ni changement d'enum destructif sans ADR) ; les index partiels sont écrits en SQL brut et vérifiés par un test qui lit pg_indexes après `migrate deploy`.
  <br>_phase 0 · tâches : `DM-02`_ · _source : nouvelle (F-DM-05) ; précédent prisma/migrations/20260709120000_session_formateurs_
- **REQ-DM-038** — Toute colonne dont le nom contient statut, type, motif, resultat, etat, origine, kind ou palier est un enum Prisma ; aucune chaîne libre ne porte un vocabulaire.
  <br>_module 13 · étape 10 · phase 0 · tâches : `DM-01`, `DM-02`_ · _source : règle maison ; anti-abus / outil §8 ; nouvelle (F-DM-19)_
- **REQ-DM-039** — Le webhook `facture.emise` transporte `destinataire: FactureFormationDestinataire` tel que défini côté axionia ; Partners stocke `payeurType` dans un enum généré depuis le même fichier de contrat partagé (jamais retapé) et l'échéance financeur attendue (`echeanceFinanceurAt`) pour la ventilation B13.
  <br>_phase -1 · tâches : `INT-T01b`, `INT-T05`_ · _source : tableaux-de-bord §2.9, B13 ; outil §2.3 ; nouvelle (F-DM-19)_
- **REQ-DM-040** — Le webhook `devis.signe` transporte par ligne {activite: ActiviteFacturation, jours: number|null, montantHtCents, offreCode} dérivés côté axionia ; Partners crée une ligne `prevue` par ligne de devis résoluble et ne recalcule jamais les jours. ⚠️ **`jours` sert à identifier le palier, jamais de multiplicateur** : le forfait est dû une fois par commande (REQ-DM-015, A-2).
  <br>_module 13 · étape 9 · phase -1 · tâches : `DM-04`, `INT-T01b`_ · _source : outil §2.3 ; audit-attribution §3.3 ; nouvelle (F-DM-08)_
- **REQ-DM-042** — 🔴 **Confirmation tacite à trente jours — le porteur manquant.** Le contrat promet depuis toujours qu'« à défaut de contact tenté dans les trente jours, l'attribution est réputée confirmée » (art. 3.2 al. 4) ; **aucune REQ, aucune tâche et aucun cron ne portaient cette promesse** — REQ-DM-006 ne promeut que sur qualification humaine, et une attribution `injoignable` n'avait alors ni terme ni horloge (constaté le 2026-09-03, M-7). Un cron quotidien, horloge injectée, promeut en `active` toute attribution `provisoire` dont l'enregistrement remonte à plus de `CONFIRMATION_TACITE_JOURS` (= 30, SSOT, contrat art. 3.2) **sans Qualification `confirme` ni `non_confirme`** ; il écrit l'événement `attribution.confirmee_tacitement` dans la même transaction et fait courir `fenetreFinAt` **à compter de cette date**. La promotion s'applique aussi aux attributions `injoignable` et `ne_se_souvient_pas` (REQ-DM-008). **C'est la seule conséquence attachée au défaut ou au retard de contact** : aucune autre sanction, aucun dédommagement, aucun signal. Tests frontière : J+29 aucune promotion, J+30 promotion unique ; deux passages du cron → une seule promotion et un seul événement ; une Qualification `non_confirme` posée à J+29 empêche la promotion.
  <br>_module 12 · étape 8 · phase 1 · tâches : `DM-13`, `DM-24`_ · _source : contrat art. 3.2 al. 3 et 4 ; synthèse du 2026-09-03, M-7_
- **REQ-DM-043** — 🔴 **Antériorité établie APRÈS l'enregistrement, et contestation d'un refus.** L'article 3.3 réécrit (A-12) organise ce que le miroir asynchrone rend inévitable : lorsque l'antériorité est établie postérieurement à l'enregistrement, l'attribution est **annulée** (transition vers `annulee`, événement écrit), l'apporteur en est **informé avec le motif**, **aucune commission nouvelle n'est due** et **les commissions déjà acquises restent acquises** — aucune ligne `acquise` n'est reprise, aucun statut `dechue` n'existe (REQ-JUR-007). Un job quotidien rapproche les nouvelles lignes d'`EntrepriseConnue` des attributions occupantes et applique cette règle. **Tout refus de dépôt et toute annulation sont contestables par écrit** : la contestation ouvre une entrée console, la Société y **répond de façon motivée dans les quinze jours** (`REPONSE_CONTESTATION_JOURS`, SSOT) et la réponse est journalisée ; une contestation sans réponse à J+16 lève une alerte. Tests : ligne d'antériorité arrivée après le dépôt → attribution `annulee`, lignes acquises inchangées, e-mail motivé envoyé une seule fois ; rejeu du job → aucune double annulation ; contestation sans réponse à J+16 → alerte.
  <br>_module 17 · étape 10 · phase 1 · tâches : `DM-25`_ · _source : contrat art. 3.3 et 3.3 bis ; synthèse du 2026-09-03, A-12_

### anti-abus-securite

- **REQ-SEC-001** — Le lien magique de connexion a un TTL de 15 minutes, est à usage unique (consommation atomique `updateMany` conditionnelle), n'est stocké qu'en hash SHA-256, et la réponse au formulaire est identique que l'email existe ou non.
  <br>_phase 0 · tâches : `SEC-03`_ · _source : tableaux-de-bord §1 B8 ; patron `src/server/formateur/magic-link.ts` et `auth.actions.ts` (précision nouvelle)_
- **REQ-SEC-002** — La demande de lien magique est limitée à 10 / 15 min par hash IP et 5 / 15 min par email, avec `surPanne: refuser` ; le message générique explique la suspension sans révéler l'existence du compte.
  <br>_module 21 · étape 12 · phase 0 · tâches : `SEC-03`_ · _source : nouvelle (patron `auth.actions.ts`)_
- **REQ-SEC-003** — La session est un cookie `__Host-` HttpOnly Secure SameSite=Lax de 30 jours, enregistrée en base (hash, `revokedAt`, `lastSeenAt`, `ipHash`) ; `apporteurs.sessionVersion` est incrémenté à toute suspension, résiliation, changement d'email ou d'IBAN et invalide toutes les sessions antérieures.
  <br>_module 21 · étape 12 · phase 0 · tâches : `SEC-04`, `SEC-19`, `UX-P1-04`_ · _source : audit-outil §5 étape 12 (« Accès coupé ») ; nouvelle_
- **REQ-SEC-004** — Toute modification d'IBAN, d'email ou de téléphone exige un step-up : un lien magique consommé depuis moins de 10 minutes.
  <br>_module 4 · étape 4 · phase 0 · tâches : `SEC-04`, `SEC-22`_ · _source : nouvelle_
- **REQ-SEC-005** — Le jeton de dépôt privé est un HMAC de scope `depot` lié à l'apporteurId, stocké en hash, multi-usage, expirant à 12 mois, révocable ; un seul jeton actif par apporteur (index unique partiel `WHERE revoked_at IS NULL`) ; régénérable par l'apporteur ; révoqué automatiquement à suspension/résiliation ; jamais dérivé du code de parrainage ni exposé dans une URL publique.
  <br>_module 9 · étape 7 · phase 1 · tâches : `SEC-11`, `SEC-19`, `UX-P1-11`_ · _source : audit-anti-abus §2 M3, §7 C6 ; audit-outil §5 étape 6_
- **REQ-SEC-006** — Chaque dépôt réalisé via le jeton privé envoie à l'apporteur un accusé contenant un lien « ce n'est pas moi » qui révoque le jeton ; un dépôt depuis un hash IP / hash UA jamais vus sur cette identité pose un signal d'anomalie.
  <br>_module 9 · étape 7 · phase 1 · tâches : `SEC-11`_ · _source : nouvelle (corrige F4 résiduel)_
- **REQ-SEC-007** → **absorbée par REQ-DM-012** — Quota, déclarations non confirmées, gel et horodatage sont comptés sur l'identité (apporteurId) et jamais sur le code public. _(source : audit-anti-abus M3)_ **→ ABSORBÉE par REQ-DM-012** (annexe de dédoublonnage, fusion appliquée par GOV-001, 2026-09-03) : **le texte en vigueur est celui de REQ-DM-012** ; l'identifiant est conservé — et non supprimé — pour que les tâches qui le citent continuent de résoudre. **→ voir REQ-DM-012.**
  <br>_module 9 · étape 7 · phase 1 · tâches : `SEC-11`_ · _source : critique de complétude (2026-09-03)_
- **REQ-SEC-008** — Toute lecture ou écriture de l'espace apporteur passe par une couche d'accès qui injecte l'`apporteurId` de la session dans le `where` ; aucun import direct de `prisma` sous `src/app/(espace)/**` et `src/server/espace/**`.
  <br>_module 8 · étape 7 · phase 0 · tâches : `SEC-05`_ · _source : tableaux-de-bord §6.1 ; audit-outil §7_
- **REQ-SEC-009** — Un test de cloisonnement en boîte noire, bloquant en CI, appelle chaque route et chaque Server Action de l'espace avec l'ID d'un autre apporteur (PDF inclus) et exige un 404 strictement identique à celui d'un ID inexistant.
  <br>_phase 0 · tâches : `SEC-05`, `UX-P2-06`_ · _source : audit-outil §7 ; plan §6 étape 7_
- **REQ-SEC-010** — Les webhooks entrants d'axionia sont signés HMAC-SHA256 sur `t.rawBody` avec un secret dédié, tolérance 300 s, comparaison en temps constant, corps ≤ 128 Ko lu en `text()` ; échec → 401 et alerte de sécurité plafonnée.
  <br>_phase 0 · tâches : `SEC-06`_ · _source : audit-outil §2.3 (précisé par `webhook-signature.ts`)_
- **REQ-SEC-011** → **absorbée par REQ-DM-036** — Chaque événement webhook est inséré dans une outbox à `eventId` unique avant traitement ; un rejeu répond 200 sans effet ; il existe au plus une ligne de commission par `paymentId` et une reprise par `refundId` (index uniques). _(source : audit-outil §2.3 ; tableaux-de-bord §2.3 ; patron `api/docuseal/webhook`)_ **→ ABSORBÉE par REQ-DM-036** (annexe de dédoublonnage, fusion appliquée par GOV-001, 2026-09-03) : **le texte en vigueur est celui de REQ-DM-036** ; l'identifiant est conservé — et non supprimé — pour que les tâches qui le citent continuent de résoudre. **→ voir REQ-DM-036.**
  <br>_module 5 · étape 10 · phase 0 · tâches : `DM-15`, `SEC-06`_ · _source : critique de complétude (2026-09-03)_
- **REQ-SEC-012** — Les API appelées par axionia répondent 404 sans jeton bearer dédié (≥ 32 octets, temps constant), sont restreintes à une allowlist d'IP, limitées à 60/min, journalisent chaque appel ; `/attributions` ne renvoie ni nom ni email d'apporteur, seulement un booléen, un mois de fin et une référence opaque.
  <br>_phase 0 · tâches : `SEC-07`_ · _source : audit-outil §2.3 ; fonctionnement R3_
- **REQ-SEC-013** — L'autocomplétion d'entreprise passe exclusivement par un proxy serveur rate-limité par identité (120/j) et par hash IP, journalisé, mis en cache 24 h ; la réponse ne contient jamais `dirigeants[]` ; le serveur les stocke côté base pour le signal F8 sans jamais les renvoyer.
  <br>_module 13 · étape 10 · phase 0 · tâches : `INT-T09`_ · _source : audit-anti-abus F1, M5, F8 ; audit-attribution P4_
- **REQ-SEC-014** — La transaction de dépôt prend un verrou consultatif par apporteurId (quota) et par SIREN (file) ; l'index unique partiel porte sur `siren WHERE statut IN (ETATS_OCCUPANTS) — 7 états, cf. REQ-DM-003 ; aucune liste littérale hors REQ-DM-003` ; le « max 2 en_attente » tient sous 20 requêtes concurrentes ; l'horodatage est en microsecondes avec un identifiant séquentiel de départage.
  <br>_module 11 · étape 7 · phase 1 · tâches : `DM-07`, `SEC-12`_ · _source : audit-anti-abus F6, M2 ; fonctionnement R3 ; règle maison index partiel_
- **REQ-SEC-015** → **absorbée par REQ-DM-010** — Le seuil de dépôt (5/15/25 sur 7 jours glissants, valeur manuelle possible) marque « vérification prioritaire » sans jamais refuser ; au-delà de 30 dépôts par 24 h sur une identité, les dépôts passent en attente (sans refus). _(source : audit-outil §1.1 D3)_ **→ ABSORBÉE par REQ-DM-010** (annexe de dédoublonnage, fusion appliquée par GOV-001, 2026-09-03) : **le texte en vigueur est celui de REQ-DM-010** ; l'identifiant est conservé — et non supprimé — pour que les tâches qui le citent continuent de résoudre. **→ voir REQ-DM-010.**
  <br>_module 9 · étape 7 · phase 1 · tâches : `SEC-12`_ · _source : critique de complétude (2026-09-03)_
- **REQ-SEC-016** — Le dépôt est en outre limité à 20 / 10 min par hash IP (`surPanne: laisser-passer`) ; le compteur par identité déclare `surPanne: refuser` ; tout compteur sous préfixe `magic:`, `depot:`, `verif:`, `webhook:`, `auth:` déclare sa conduite sur panne.
  <br>_module 9 · étape 7 · phase 0 · tâches : `SEC-03`, `SEC-10`_ · _source : nouvelle (patron `rate-limit.spec.ts` garde de famille)_
- **REQ-SEC-017** — 🔴 **CINQ signaux de CONTENU, et le franchissement du seuil n'a plus AUCUN effet défavorable automatique** (réécrite le 2026-09-03, A-3 (a) et (b) — c'est le point le plus lourd du dossier). Le score se compose exclusivement des **cinq signaux qui décrivent ce qui est DÉCLARÉ** : nom du contact = dirigeant de l'API (comparaison de hashes, REQ-INT-021), contact générique, texte court ou identique, multi-identités par hash IP + UA, ordre alphabétique ou croissant des SIREN déclarés. 🔴 **`rafale` (rythme), `nocturne` (horaire) et `hors zone` (lieu) sont RETIRÉS du score** : une fonction qui mesure **quand, à quel rythme et où** quelqu'un travaille et en tire seule une mesure défavorable réunit en un appel les trois pouvoirs que la charte relationnelle interdit de réunir — elle contredit frontalement REQ-JUR-031 (« aucun compteur de volume, de rythme, de délai de réponse ou de méthode ne peut l'atteindre ») et la phrase du contrat « elle n'obéit à aucun barème, à aucun compteur et à aucun seuil ». Ces trois-là peuvent rester des **critères de priorisation d'une revue humaine en console**, jamais des déclencheurs, et n'entrent dans aucun score, aucune alerte automatique et aucun DTO exposé (garde AST). Le critère de tri est explicite : un signal qui décrit **où et quand** l'apporteur travaille ou **à quel rythme** sort ; un signal qui décrit **ce qui est déclaré** reste (`ordre alphabétique` est conservé à ce titre : c'est la signature d'une liste recopiée depuis un annuaire, pas une méthode de travail — arbitrage C-3). 🔴 **Le franchissement du seuil OUVRE une Anomalie ; il ne pose plus le gel** : `gele_fraude` n'est posé que par une Anomalie **confirmée par un humain** (REQ-DM-033 le disait déjà, cette exigence le démentait). Le seuil reste un paramètre de Paramètres (défaut dans la SSOT). Tests : un test par signal (fixture positive, fixture négative) sur fixtures du producteur réel ; test frontière (score = seuil − 1 : rien ; score = seuil : **Anomalie `ouverte`, aucun gel, aucun jeton révoqué**) ; **vu rougir** : un signal `rafale`/`nocturne`/`hors zone` présent dans la composition du score → gate rouge ; un chemin posant `gele_fraude` sans Anomalie `confirmee` → gate rouge.
  <br>_module 2 · étape 2 · phase 1 · tâches : `SEC-14`_ · _source : audit-anti-abus F8, §3 ; audit-outil §9.2 ; REQ-JUR-031 ; contrat art. 3.7 al. 3 ; synthèse du 2026-09-03, A-3 (a)(b) et arbitrage C-3_
- **REQ-SEC-018** — Une déclaration `non_confirme` **ouvre à la Société la FACULTÉ** de suspendre l'enregistrement des nouvelles déclarations de l'apporteur le temps d'une vérification ; elle ne la suspend **pas automatiquement** (corrigé le 2026-09-03, A-3 (f) : le contrat n'ouvre qu'une faculté — « la Société **peut** suspendre » — là où le produit exécutait la mesure de plein droit ; un écart texte/pratique de ce type se lit contre son rédacteur, art. 1190 C. civ.). La suspension est **posée par un rôle habilité**, notifiée avec les faits qui la motivent, et levée par un rôle habilité ou de plein droit (REQ-SEC-019). Aucun seuil, aucun compteur, aucun rang : une non-confirmation ouvre la faculté, une seconde ne change rien à l'état. La résiliation reste une décision humaine motivée, jamais une conséquence de comptage. Tests : une `non_confirme` → **aucun changement d'état automatique**, une entrée console à traiter ; pose par `admin` → suspension + journal + e-mail ; pose par `lecteur` → 403 ; un chemin posant la suspension sans acteur humain → **vu rougir**.
  <br>_module 21 · étape 12 · phase 1 · tâches : `SEC-15`_ · _source : audit-anti-abus M1, C3 ; contrat art. 3.7 al. 3 ; réécrite le 2026-09-03 — barème gradué retiré du produit, cf. REQ-JUR-031 ; synthèse du 2026-09-03, A-3 (f)_
- **REQ-SEC-019** — 🔴 **Le gel est un enum à TROIS valeurs — `libre`, `gele_non_confirmation`, `gele_fraude`** — avec motif, date et auteur (réécrite le 2026-09-03, A-3 (c) : `gele_anomalie` est renommée `gele_fraude` pour porter le mot du contrat, et `gele_manuel` **est supprimée** — un gel « manuel » sans motif écrit est exactement le pouvoir discrétionnaire que l'article 3.7 exclut ; la valeur `gele_strikes` avait déjà été renommée, « strike » étant un synonyme interdit au glossaire). **Tous les chemins de gel passent par une fonction unique** dont l'entrée est typée sur ces deux motifs et que garde `jur:suspension-motifs-fermes` (REQ-SEC-038). 🔴 **Portée strictement limitée** (contrat art. 3.7 al. 3 réécrit) : le gel porte **exclusivement sur l'enregistrement de nouvelles déclarations**. Il est **sans effet** sur les attributions en cours, provisoires ou définitives, sur les commandes signées, sur les commissions acquises ou à venir, et **sur l'accès de l'apporteur à son espace** — aucun jeton n'est révoqué, `sessionVersion` n'est pas incrémenté, aucune attribution n'est annulée (c'est REQ-SEC-032 qui réserve ces effets à la **résiliation**). Les attributions `provisoire` non qualifiées **restent `provisoire`**, portent `verificationPrioritaire = true` (la valeur `suspendue` n'existe pas dans l'enum fermé de REQ-DM-006) et libèrent la file M2. 🔴 **Durée maximale de quinze jours** (`SUSPENSION_MAX_JOURS`, SSOT, contrat art. 3.7) : un cron quotidien **lève de plein droit** tout gel de plus de quinze jours à compter de sa notification, écrit l'événement et notifie l'apporteur ; la Société demeure libre de résilier selon l'article 11. Une levée avant terme reste réservée à un rôle habilité et journalisée. Tests : gel posé à J0 → levé par le cron à J+15, jamais avant ; **un gel qui survit à J+16 → rouge** ; gel posé → jeton toujours valide, session vivante, attributions inchangées, lignes de commission inchangées (quatre assertions, **vues rougir** sur l'ancien comportement) ; `gele_manuel` ou `gele_anomalie` présent dans l'enum → gate rouge.
  <br>_module 21 · étape 12 · phase 1 · tâches : `SEC-15`, `SEC-28`_ · _source : audit-outil §1.1 ; contrat art. 3.7 al. 3 ; synthèse du 2026-09-03, A-3 (c) et (e)_
- **REQ-SEC-020** — Zone déclarée et secteur d'expertise sont **indicatifs et ne restreignent ni les déclarations, ni les droits, ni leur validité** (contrat art. 2.4 réécrit, M-5). Aucun contrôle de zone ou de secteur ne refuse un dépôt, **et — corrigé le 2026-09-03, A-3 (a) — ils n'entrent dans aucun score, aucune mesure défavorable automatique et aucun DTO exposé à l'apporteur** : le signal `hors zone` est retiré de la composition du score de REQ-SEC-017 et ne subsiste que comme critère de priorisation d'une revue humaine en console. Tests : dépôt hors zone → accepté sans signal défavorable ; `zone` ou `secteur` atteignable depuis le calcul de score → gate rouge.
  <br>_module 9 · étape 7 · phase 1 · tâches : `SEC-12`, `SEC-14`_ · _source : audit-outil §1.2 D4 ; contrat art. 2.4 ; synthèse du 2026-09-03, A-3 (a) et M-5_
- **REQ-SEC-021** — « Vérifier une entreprise » n'est accessible qu'en session authentifiée, limité par identité et par hash IP selon la configuration, journalisé (`verification_events`) ; un rythme de dépôt au-delà des seuils de configuration pose un signal d'anomalie ; la date de libération est arrondie au mois ; l'identité de l'autre apporteur n'est jamais révélée.
  <br>_module 13 · étape 7 · phase 1 · tâches : `SEC-16`_ · _source : audit-outil §1.3 D5 (précisé)_
- **REQ-SEC-022** — Les libellés de refus dérivent d'un enum `MotifRefusDepot` 🔴 **aligné un pour un sur les catégories écrites aux articles 3.3 et 3.3 bis du contrat** (réécrite le 2026-09-03, A-12 — le produit appliquait au moins six causes de refus dont **quatre sans aucune base écrite** : `SirenListeNoire`, `file_complete`, `fermee`, `financeur`, plus le registre d'opposition « dont le motif n'est pas révélé » ; refuser à un indépendant l'acquisition d'un droit pour un motif ni écrit ni communiqué, dans 300 contrats d'adhésion identiques, est le cas d'école du déséquilibre significatif — L.442-1, I, 2° C. com. et art. 1171 C. civ.). Valeurs : `anteriorite_client` (facturée ≤ 24 mois), `anteriorite_devis` (< 6 mois), `etablissement_cesse` (art. 3.3 bis a), `entreprise_hors_perimetre` (art. 3.3 bis b — administration, financeur public ou paritaire, organisme de formation en relation avec la Société, figurant sur la liste de REQ-DM-028 **dont l'existence et le principe sont portés à la connaissance de l'apporteur**), `file_complete` (art. 3.3 bis c — deux déclarations déjà en attente), `opposition_demarchage` (art. 3.3 bis d), `insincerite` (art. 3.7). **Le refus est notifié avec sa catégorie ; il n'emporte aucune autre conséquence et n'est pas un manquement** — aucun refus ne pose de signal défavorable ni n'entre dans un compteur. « Déjà suivie » et « déjà cliente » ne révèlent ni qui ni quand ; la collision au dépôt et la réponse de « Vérifier une entreprise » ne contiennent ni identifiant, ni nom, ni date de dépôt, ni stade de l'autre apporteur (seule la date de fin est autorisée) — un test snapshot du corps de réponse le prouve ; toute ressource étrangère répond 404 (jamais 403). Tests : `Object.values(MotifRefusDepot)` = les sept catégories exactement, **vu rougir** sur un motif hors liste ; chaque refus produit un libellé nommant sa catégorie.
  <br>_module 9 · étape 7 · phase 0 · tâches : `DM-10-P`, `DM-25`, `SEC-05`, `SEC-12`, `SEC-16`_ · _source : fonctionnement §4.3 ; contrat art. 3.3 et 3.3 bis ; fusion ← REQ-QA-012 ; synthèse du 2026-09-03, A-12_
- **REQ-SEC-023** — La console porte un enum `ConsoleRole { admin, qualifieur, comptable, lecteur }` et une matrice droits × actions en un seul fichier ; chaque Server Action et route de la console appelle `requireRole` ; le défaut est le refus ; IBAN en clair, approbation de lot, pain.001, levée de gel, suspension/résiliation et export DAS2 sont réservés selon la matrice.
  <br>_module 21 · étape 12 · phase 0 · tâches : `SEC-17`_ · _source : règle maison ; audit-outil §9 ; mémoire #871_
- **REQ-SEC-024** — Nom, email, téléphone du tiers rencontré et de l'apporteur, et l'IBAN, sont chiffrés AES-256-GCM avec AAD = identifiant de ligne ; des hash HMAC salés (`emailHash`, `phoneHash`, `ibanHash`, `siretHash`) servent aux recherches et comparaisons ; aucune IP brute n'est stockée, seul un hash salé tronqué.
  <br>_module 4 · étape 4 · phase 0 · tâches : `SEC-08`_ · _source : audit-attribution P8 ; nouvelle (corrige `createdIp`/`ipAddress` en clair)_
- **REQ-SEC-025** — L'IBAN est affiché masqué ; tout changement exige un step-up, entre en carence 72 h, notifie l'ancienne adresse et l'ancien numéro, exclut l'apporteur du lot en cours (« RIB en carence ») et déclenche une alerte console.
  <br>_module 4 · étape 4 · phase 2 · tâches : `SEC-22`_ · _source : nouvelle_
- **REQ-SEC-026** — Les pièces KYC sont sur un stockage privé, servies par URL signée ≤ 5 min, jamais publiques ; la pièce d'identité est purgée après validation, seule la mention « validée le » subsiste.
  <br>_module 4 · étape 4 · phase 1 · tâches : `DM-11`_ · _source : audit-outil §6.1_
- **REQ-SEC-027** → **absorbée par REQ-DM-024** — Le journal d'audit est append-only (rôle applicatif sans UPDATE/DELETE, trigger de refus), hash chaîné, conservé 10 ans, exclu de toute purge, vérifié par un cron hebdomadaire ; il enregistre dépôts (horodatage serveur, hash IP), qualifications, changements d'état, déclarations non confirmées, gel/levée, changements IBAN/email, approbations de lot, résiliations. _(source : audit-outil §9.1 ; audit-attribution P9 (corrige l'appui sur `ActivityLog` purgé))_ **→ ABSORBÉE par REQ-DM-024** (annexe de dédoublonnage, fusion appliquée par GOV-001, 2026-09-03) : **le texte en vigueur est celui de REQ-DM-024** ; l'identifiant est conservé — et non supprimé — pour que les tâches qui le citent continuent de résoudre. **→ voir REQ-DM-024.**
  <br>_module 9 · étape 8 · phase 0 · tâches : `DM-01`, `DM-20`_ · _source : critique de complétude (2026-09-03)_
- **REQ-SEC-028** — Sept secrets distincts (`SESSION_SECRET`, `MAGIC_LINK_SECRET`, `DEPOSIT_TOKEN_SECRET`, `AXIONIA_WEBHOOK_SECRET`, `AXIONIA_API_TOKEN`, `DOCUSEAL_WEBHOOK_SECRET`, `PII_ENCRYPTION_KEY`) plus `IP_HASH_SALT`, ≥ 32 octets (64 hex pour la clé), validés par Zod au boot, préfixes `dev_`/`stub` refusés en production, `kid` dans les jetons pour la rotation ; aucun secret dans le dépôt.
  <br>_module 5 · étape 5 · phase 0 · tâches : `SEC-01`_ · _source : nouvelle (patron `src/env.ts`)_
- **REQ-SEC-029** — Toutes les routes servent CSP par nonce (`frame-ancestors 'none'`, `strict-dynamic`, `connect-src` restreint), HSTS preload, `X-Content-Type-Options`, `Referrer-Policy: strict-origin-when-cross-origin`, `Permissions-Policy` restrictif.
  <br>_phase 0 · tâches : `SEC-02`_ · _source : nouvelle (patron `src/proxy.ts`, `next.config.ts`)_
- **REQ-SEC-030** — Les coordonnées du tiers rencontré sont purgées par le cron de REQ-DM-031 selon `retention.ts` : `CONTACT_PURGE_APRES_LIBERATION_JOURS` (défaut 90, états `invalidee`/`perdue`/`expiree`/`perimee`) et `CONTACT_PURGE_CONVERTIE_APRES_DERNIER_CONTACT_JOURS` (défaut 1 095), toutes deux sous marqueur `HYP-RGPD-RETENTION` **jusqu'à la décision de Will consignée dans `DECISIONS.md`** (il n'y a pas de DPO sur le projet, décision du 2026-09-03), avec un test HYP qui échoue si la valeur change ; l'information art. 14 est délivrée selon REQ-JUR-009 ; apporteur et tiers disposent d'une voie d'accès et d'effacement par jeton (1/jour, `surPanne: refuser`, testée) ; le registre art. 30 décrit trois traitements et un test vérifie qu'il en liste exactement trois.
  <br>_phase 0 · tâches : `DM-20`, `JUR-T04`_ · _source : audit-outil §3.3, §5 étape 12 ; audit-attribution P8 ; reformulation d'annexe appliquée le 2026-09-03_
- **REQ-SEC-031** — Le parrainage est marqué `a_verifier` si `siretHash`, `emailHash`, `phoneHash` ou `ibanHash` du filleul égale celui du parrain, contrôlé à la candidature parrainée et à toute saisie/modification de RIB.
  <br>_module 14 · étape 4 · phase 1 · tâches : `SEC-18`_ · _source : audit-anti-abus F14, §4 « Parrainage — identité distincte »_
- **REQ-SEC-032** — 🔴 **La coupure est réservée à la RÉSILIATION ; la suspension n'en produit AUCUN effet** (réécrite le 2026-09-03, A-3 (e) : cette exigence faisait exécuter suspension **et** résiliation dans la même transaction, ce qui révoquait les jetons, coupait l'accès à l'espace et annulait des attributions ayant une valeur pécuniaire pour une simple mesure de vérification — la spécification se contredisait d'ailleurs elle-même, l'arbitrage l. 416 et REQ-SEC-019 disant que ces attributions **restent `provisoire`**). **Résiliation** : une transaction unique révoque les jetons de dépôt, incrémente `sessionVersion`, annule `provisoire`/`en_attente`, libère `active` non converties et laisse `convertie` intactes. **Suspension** : aucun de ces effets — cf. REQ-SEC-019. 🔴 **Après résiliation, une session en LECTURE SEULE survit** (contrat art. 12.3, A-4) : l'apporteur conserve l'accès en lecture à son espace — relevés, factures, motif de tout blocage, journal de ses propres déclarations — **jusqu'à l'extinction de ses droits**, ou les reçoit par courrier électronique à sa dernière adresse déclarée (REQ-UX-016). Sans cette réserve, la Société pourrait notifier valablement dans un espace fermé et devrait payer des commissions que l'apporteur ne peut plus lire. La session résiliée n'ouvre **aucune** écriture : aucun dépôt, aucune modification de profil, aucun jeton. La Server Action de dépôt relit le statut de l'apporteur dans sa transaction. Tests : après suspension → jeton valide, session vivante, attributions inchangées (**vu rougir**) ; après résiliation → dépôt refusé, écriture refusée, **lecture des relevés et factures accessible**, `/deposer` en 404 ; extinction des droits → accès clos, notifications par e-mail.
  <br>_module 21 · étape 12 · phase 1 · tâches : `SEC-12`, `SEC-19`, `T-ARG-038`_ · _source : audit-outil §5 étape 12 ; audit-anti-abus §4 « Effets de la résiliation » ; contrat art. 3.7 al. 3 et 12.3 ; synthèse du 2026-09-03, A-3 (e) et A-4_
- **REQ-SEC-033** — Aucune notification Telegram, SMS ou push ne contient les coordonnées du tiers ni un montant ; le brouillon hors-ligne ne contient pas les coordonnées du tiers et est purgé après envoi ou à 24 h.
  <br>_phase 1 · tâches : `SEC-26`, `UX-P1-03`_ · _source : audit-outil §9.3, §7 Notifications, §12.3 ; nouvelle_
- **REQ-SEC-034** — Le webhook DocuSeal est vérifié en HMAC strict avec timestamp (sans fallback secret en clair), dédupliqué par outbox, et le `metadata.apporteurId` est confronté à la soumission émise ; le PDF est récupéré par l'API et son SHA-256 stocké puis recomparé à chaque téléchargement.
  <br>_module 5 · étape 5 · phase 1 · tâches : `INT-T12`_ · _source : audit-outil §6.2 ; patron `src/lib/docuseal.ts`_
- **REQ-SEC-035** — Le honeypot répond en succès silencieux mais journalise l'apporteurId (sans la valeur) dans Anomalies ; l'accusé de dépôt n'est envoyé que si la ligne existe.
  <br>_module 9 · étape 7 · phase 0 · tâches : `SEC-10`_ · _source : nouvelle (patron `honeypot-observable.ts`)_
- **REQ-SEC-036** — Aucun fingerprinting navigateur ; le signal multi-identités repose sur (hash IP, hash User-Agent, tranche horaire) et n'est jamais bloquant seul.
  <br>_phase 1 · tâches : `SEC-14`_ · _source : nouvelle (corrige F8 « empreinte navigateur »)_
- **REQ-SEC-037** — Le code public de parrainage porte ≥ 25 bits aléatoires ; un `?p=` inconnu est ignoré silencieusement, journalisé et limité à 30/h par IP ; l'existence d'un code n'est jamais confirmée publiquement.
  <br>_module 14 · étape 10 · phase 1 · tâches : `SEC-21`_ · _source : audit-anti-abus F4_
- **REQ-SEC-038** — 🔴 **Un seul point d'entrée de gel, prouvé par un test-cliquet NOMINATIF des appelants.** La gate `jur:suspension-motifs-fermes` ne vérifiait qu'une **signature** (`GATES.md` l. 96) : elle laissait passer un second chemin de gel écrit ailleurs dans `src/` — c'est-à-dire exactement ce qu'elle prétendait interdire (constaté le 2026-09-03, A-3 (d) ; « une garde qui ne teste qu'une signature ne garde qu'une signature »). Sur le modèle de REQ-INT-007, un test-cliquet **énumère nominativement** tous les écrivains de `Apporteur.depotsGelesDepuis` et de l'enum de gel dans `src/` et `scripts/`, et **rougit** si l'un d'eux n'appelle pas la fonction unique de REQ-SEC-019 — l'ajout d'un écrivain non listé fait échouer la CI. Le même cliquet couvre la **levée** de gel. Preuve rouge exigée : une seconde fonction posant `gele_fraude` en direct, ajoutée à un témoin, doit faire rougir la gate avant son activation.
  <br>_module 21 · étape 12 · phase 1 · tâches : `SEC-14`, `SEC-15`, `SEC-28`_ · _source : GATES.md l. 96 ; REQ-INT-007 (patron du cliquet nominatif) ; REQ-JUR-031 ; synthèse du 2026-09-03, A-3 (d)_

### argent

- **REQ-ARG-001** → **absorbée par REQ-DM-016** — Une ligne de commission `acquise` n'est créée que par un événement `paiement.recu` (status succeeded, amountCents > 0). `devis.signe` et `facture.emise` ne créent jamais de ligne `acquise` (ils créent/actualisent l'attendu `en_attente_encaissement`). _(source : tableaux-de-bord §2.3 ; fonctionnement R7 ; audit-attribution P6)_ **→ ABSORBÉE par REQ-DM-016** (annexe de dédoublonnage, fusion appliquée par GOV-001, 2026-09-03) : **le texte en vigueur est celui de REQ-DM-016** ; l'identifiant est conservé — et non supprimé — pour que les tâches qui le citent continuent de résoudre. **→ voir REQ-DM-016.**
  <br>_étape 10 · phase 0 · tâches : `DM-15`, `INT-T05`_ · _source : critique de complétude (2026-09-03)_
- **REQ-ARG-002** — Idempotence : deux livraisons d'un même `eventId`, ou deux événements portant le même `paymentId`, produisent exactement une ligne par bénéficiaire ; la seconde répond 200 `{duplicate:true}` et ne déclenche aucune notification.
  <br>_phase -1 · tâches : `INT-T01b`, `SEC-06`, `T-ARG-022`_ · _source : audit-outil §2.3 ; nouvelle (ARG-07)_
- **REQ-ARG-003** — Rejeu : rejouer dans l'ordre l'intégralité des événements reçus sur une base vide reproduit un registre identique (mêmes montants, mêmes statuts, même hash de registre).
  <br>_phase 0 · tâches : `SEC-06`, `T-ARG-022`_ · _source : nouvelle (audit-outil §2.3 « rejouables »)_
- **REQ-ARG-004** — Prorata : part acquise sur l'encaissement i = floor(commissionTotaleCents × cumulEncaisséTTC_i / factureTTCNetCents) − Σ parts précédentes ; à facture soldée, Σ parts = commissionTotaleCents exactement ; aucune part négative ; aucune part ne dépasse la commission totale.
  <br>_module 13 · étape 10 · phase 0 · tâches : `DM-04`, `T-ARG-036`, `T-ARG-037`_ · _source : tableaux-de-bord §2.1, §2.4 ; nouvelle (ARG-02, ARG-03)_
- **REQ-ARG-005** — Multi-payeurs : les encaissements de plusieurs payeurs (OPCO subrogé, entreprise, France Travail) sur les factures d'une même prestation sont proratisés sans cas particulier ; la résolution passe par le `clientId` bénéficiaire résolu par axionia (facture ?? session ?? enrollment ?? dossier), jamais par `destinataireSiret` ; la commission porte sur le HT total de la prestation.
  <br>_module 13 · étape 10 · phase -1 · tâches : `DM-15`, `INT-T01b`, `INT-T05`_ · _source : tableaux-de-bord §2.9, R13, R15, B10, B11 ; ARG-08_
- **REQ-ARG-006** → **absorbée par REQ-DM-015** — Commission totale d'une ligne de facture : `flat` = flatEur × 100 — **une fois par commande portant le palier, jamais multiplié par les journées** (corrigé le 2026-09-03, A-2 ; la formule antérieure `flatEur × quantiteJournees × 100` doublait la commission sur tous les paliers pluri-journées) ; `percent` = round(percent × montantHtCents / 100) ; `scale` ou règle introuvable → ligne `bloquee` motif `a_qualifier` (montant null) avec alerte, jamais un silence. _(source : pricing.ts COMMERCIAL_COMMISSIONS ; audit-attribution §3.3 ; ARG-04, ARG-05 ; synthèse du 2026-09-03, A-2)_ **→ ABSORBÉE par REQ-DM-015** (annexe de dédoublonnage, fusion appliquée par GOV-001, 2026-09-03) : **le texte en vigueur est celui de REQ-DM-015** ; l'identifiant est conservé — et non supprimé — pour que les tâches qui le citent continuent de résoudre. **→ voir REQ-DM-015.**
  <br>_module 13 · étape 9 · phase -1 · tâches : `DM-04`, `INT-T01b`, `T-ARG-035`_ · _source : critique de complétude (2026-09-03)_
- **REQ-ARG-007** — Plafond : si commissionTotaleCents > plafondPct × montantHtCents de la ligne (paramètre, défaut 100 %), la ligne est `bloquee` motif `commission_sup_ht` et ne peut être relevée sans décision journalisée.
  <br>_module 15 · étape 10 · phase 0 · tâches : `DM-04`_ · _source : nouvelle (ARG-04)_
- **REQ-ARG-008** — Snapshot de grille : chaque ligne porte `grilleVersionId` = version de grille signée par l'apporteur **en vigueur à la DATE DE CONFIRMATION DE L'ATTRIBUTION** à laquelle la ligne se rattache, figée à la création ; un changement ultérieur de grille ne modifie aucune ligne existante. 🔴 **Le pivot est la confirmation de l'attribution, pas `dateRef`** (corrigé le 2026-09-03, M-9) : le contrat (art. 4.1 al. 3) rattache la commission à la grille en vigueur à la confirmation, le produit la rattachait à `dateRef` (= signature du devis, REQ-DM-022) — **jusqu'à quinze mois séparent les deux, soit deux montants différents pour une même vente**. Le contrat est signé, il est plus protecteur, **c'est lui qui gagne** ; c'est le produit qui bouge. Test à deux versions, sur ce pivot (prolongement de REQ-EXT-028 (b)) : attribution confirmée sous v1, devis signé après publication de v2 → la ligne est calculée avec **v1** (vu rougir sur le pivot `dateRef`).
  <br>_étape 9 · phase 2 · tâches : `DM-15`, `T-ARG-037`_ · _source : audit-outil §6.2, D10 ; ARG-11 ; contrat art. 4.1 al. 3 ; synthèse du 2026-09-03, M-9_
- **REQ-ARG-009** → **absorbée par REQ-DM-022** — Fenêtre : `dateRef = devis.signeAt ?? facture.emiseAt` ; une ligne n'est créée que si l'attribution est `active` ou `convertie` et dateRef ∈ [attribution.enregistreeLe, enregistreeLe + 12 mois] ; l'encaissement peut être postérieur à la fenêtre. _(source : fonctionnement §5.2, R2 ; audit-attribution §3.1 ; ARG-10)_ **→ ABSORBÉE par REQ-DM-022** (annexe de dédoublonnage, fusion appliquée par GOV-001, 2026-09-03) : **le texte en vigueur est celui de REQ-DM-022** ; l'identifiant est conservé — et non supprimé — pour que les tâches qui le citent continuent de résoudre. **→ voir REQ-DM-022.**
  <br>_étape 9 · phase 2 · tâches : `DM-15`_ · _source : critique de complétude (2026-09-03)_
- **REQ-ARG-010** — Reprise : `paiement.rembourse` crée une ligne `reprise` négative = −min(Σ acquis sur la facture, floor(commissionTotale × rembourséTTC / factureTTC)) ; `avoir.emis` et `facture.annulee` recalculent l'attendu sans créer de reprise ; la ligne d'origine n'est jamais modifiée ni supprimée.
  <br>_module 13 · étape 10 · phase 2 · tâches : `DM-15`_ · _source : tableaux-de-bord §2.5, B5 ; ARG-01, ARG-06_
- **REQ-ARG-011** → **absorbée par REQ-DM-023** — Parrainage : pour chaque ligne (acquise ou reprise) d'un filleul dont `dateRef` < contratFilleul.signedAt + 12 mois, une ligne parrain de 10 % (même méthode d'arrondi) est créée dans la même transaction ; profondeur 1 (aucune ligne pour le parrain du parrain) ; jamais de ligne sur l'inscription. _(source : fonctionnement §6, R8 ; plan §4.1 ; ARG-12)_ **→ ABSORBÉE par REQ-DM-023** (annexe de dédoublonnage, fusion appliquée par GOV-001, 2026-09-03) : **le texte en vigueur est celui de REQ-DM-023** ; l'identifiant est conservé — et non supprimé — pour que les tâches qui le citent continuent de résoudre. **→ voir REQ-DM-023.**
  <br>_module 14 · étape 10 · phase 2 · tâches : `DM-16`_ · _source : critique de complétude (2026-09-03)_
- **REQ-ARG-012** — Anti auto-parrainage : aucune ligne parrain si parrainId = filleulId ou si SIRET, IBAN, email ou téléphone du parrain et du filleul sont identiques ; le lien de parrainage est refusé à la signature dans ces cas.
  <br>_module 14 · étape 4 · phase 2 · tâches : `DM-16`_ · _source : anti-abus F14, §4 (Parrainage — identité distincte)_
- **REQ-ARG-013** — Le bonus filleul est piloté par `BONUS_FILLEUL_CENTS` dans la SSOT (**null = désactivé, valeur en vigueur**, marqueur `HYP-D14`). 🔴 **Tant qu'il est null, aucune ligne `bonus_filleul` ne peut naître, et un test-cliquet ROUGIT si une seule est produite** (corrigé le 2026-09-03, M-12) : l'article 4.1 du contrat exclut toute rémunération autre que celles qu'il énumère, et un bonus **activable en silence** contredirait un contrat signé 300 fois. L'ouvrir suppose de le **chiffrer dans un article 4.6 bis** du contrat et un avenant — jamais un simple changement de constante. Quand il est non null, une ligne `bonus_filleul` unique par couple (parrainId, filleulId) naît à la première ligne `acquise` du filleul, est reprise si la facture d'origine est intégralement remboursée, et entre dans l'autofacture, le cumul vigilance et le cumul DAS2. 🔴 **Le DTO de parrainage exposé à l'apporteur ne porte QUE le montant qui lui revient** (contrat art. 4.6 al. nouveau) : ni identité, ni activité, ni nombre de déclarations, ni état d'avancement du filleul — le parrainage n'emporte aucune fonction d'encadrement, d'animation, de formation ni de suivi (cf. REQ-UX-006, gate `jur:aucun-agregat-reseau`). Tests : paramètre null → une ligne `bonus_filleul` produite = **rouge** ; paramètre non null → comportement nominal ; assertion sur les clés du DTO parrainage.
  <br>_module 14 · étape 11 · phase 2 · tâches : `DM-16`_ · _source : audit-outil §12.1, D14 ; ARG-13 ; contrat art. 4.1 et 4.6 ; reformulation d'annexe appliquée le 2026-09-03 ; synthèse du 2026-09-03, M-12_
- **REQ-ARG-014** — Gel mensuel : le job `statement-YYYY-MM` (jobId BullMQ unique) relève toutes les lignes `acquise`/`reprise`/`bonus` avec `statementId IS NULL` à l'instant T, quelle que soit leur date ; un second run du même mois ne crée aucun relevé ; un relevé émis n'est jamais réouvert.
  <br>_module 13 · étape 10 · phase 2 · tâches : `T-ARG-015`_ · _source : tableaux-de-bord §2.7, B3 ; audit-outil §6.3 ; ARG-14_
- **REQ-ARG-015** — Seuil : si le solde d'un apporteur (Σ lignes relevables) < `SEUIL_VERSEMENT` (5 000 cents, SSOT), y compris négatif, **aucun relevé n'est établi et aucune facture n'est émise** — les lignes restent relevables le mois suivant (report). ✅ **Le seuil est au stade de la FACTURATION, jamais du paiement** : c'est le code qui avait raison et le contrat qui a été corrigé (art. 5.1, A-5). Écrit au stade du paiement, il reportait jusqu'à onze mois une créance **déjà facturée** et exposait au plafond de 60 jours de l'article L.441-10, I C. com. ; écrit ainsi, aucune facture n'est émise, donc aucun délai ne court, donc il n'y a **aucun manquement** (arbitrage C-5). 🔴 **Deux exceptions, à écrire dans le code** (contrat art. 5.3 et 5.4) : au relevé du mois de **janvier**, le seuil n'est pas appliqué et **tout solde positif reporté est versé** ; il ne s'applique pas davantage au **dernier relevé établi après la fin du contrat** (REQ-ARG-026, HYP-E1-17). Tests frontière : 4 999 cents en novembre → report ; 4 999 cents en janvier → **versé** (vu rougir) ; 4 999 cents au dernier relevé d'un résilié → versé ; solde négatif → jamais de facture.
  <br>_module 15 · étape 12 · phase 2 · tâches : `T-ARG-015`, `T-ARG-033`, `T-ARG-038`_ · _source : tableaux-de-bord §2.7, B4 ; ARG-15 ; contrat art. 5.1 et 5.3 ; synthèse du 2026-09-03, A-5 et arbitrage C-5_
- **REQ-ARG-016** — 🔴 **Trois contrôles bloquants, et TROIS SEULEMENT** (réécrite le 2026-09-03, A-4 et A-5, sur l'article 5.4 réécrit) : SIREN vérifié valide et actif, coordonnées bancaires valides **au nom de l'apporteur** (IBAN checksum + titulaire), et attestation de vigilance **lorsque le cumul atteint le seuil légal** des articles L.8222-1 et D.8222-5 C. trav. (`SEUIL_VIGILANCE`, 500 000 cents HT, SSOT). **Aucune autre pièce et aucun autre motif ne peuvent différer un versement** : ni l'absence d'attestation RC pro (REQ-DM-027), ni un défaut de rattachement d'un encaissement, ni l'absence de palier à la grille (annexe 1, A1.7 / REQ-EXT-025) ne le diffèrent au-delà de soixante jours (REQ-ARG-034). 🔴 **L'absence de mandat d'autofacturation ne bloque plus rien** : elle produit un relevé **payable sur facture de l'apporteur**, jamais un relevé `bloque` motif `mandat_non_signe` (REQ-ARG-018, REQ-ARG-035) — ce blocage se déclenchait à la fin de **chaque** contrat, avec une créance impayée d'un côté et une TVA non déductible de l'autre. Le régime TVA déclaré reste requis pour **émettre** une autofacture (REQ-ARG-018) et non pour payer. Tout échec des trois contrôles produit un relevé `bloque` avec motif enum visible par l'apporteur et aucun virement. Tests : mandat absent → relevé **`a_payer` sur facture de l'apporteur** (vu rougir sur `bloque`) ; RC pro périmée → payé ; vigilance absente sous le seuil → payé ; vigilance absente au seuil → bloqué.
  <br>_module 4 · étape 4 · phase 2 · tâches : `JUR-T16`, `T-ARG-015`, `T-ARG-032`, `T-ARG-038`_ · _source : audit-outil §6.1, §6.3 ; tableaux-de-bord §2.8 ; R10 ; ARG-19 ; contrat art. 5.4 et annexe 2.5 ; synthèse du 2026-09-03, A-4 et A-5_
- **REQ-ARG-017** — Motif de blocage = enum Prisma `MotifBlocage` (`rib_manquant`, `rib_a_verifier`, `siret_invalide`, `tva_non_declaree`, `vigilance_perimee`, `sous_seuil`, `commission_sup_ht`, `a_qualifier`, `non_resolue`) ; aucune colonne de statut ou de motif en chaîne libre. 🔴 **`mandat_non_signe` est RETIRÉ de l'enum** (2026-09-03, A-4) : l'absence de mandat change le **mode d'émission** de la facture, pas le droit au paiement (REQ-ARG-016, REQ-ARG-035). 🔴 **Trois motifs cessent de pouvoir bloquer un versement et deviennent des ALERTES console** — `commission_sup_ht`, `a_qualifier`, `non_resolue` (A-5) : ils signalent un chiffre à vérifier, jamais un droit à suspendre, et **ne peuvent différer un versement au-delà de soixante jours** (REQ-ARG-034) ; la ligne concernée est payée au montant établi, l'écart éventuel se corrige par une `LigneAjustement` (REQ-CPL-010). ⚠️ **`bareme_indefini` n'est PAS une valeur de cet enum** : c'est un **libellé de cas** (REQ-DM-015, REQ-EXT-025), porté en base par `motifBlocage = a_qualifier` et rendu à l'apporteur « **Prestation hors grille de commissions** » (REQ-UX-011) — l'arbitrage l. 410 l'a tranché, et les textes qui écrivent encore « motif `bareme_indefini` » désignent ce cas, jamais une valeur d'enum. Tests : `Object.values(MotifBlocage)` = les neuf valeurs exactement, **vu rougir** sur `mandat_non_signe` ; ligne `commission_sup_ht` non payée à J+61 → rouge.
  <br>_module 17 · étape 10 · phase 0 · tâches : `CPL-T12`, `DM-04`, `T-ARG-010`, `T-ARG-015`, `T-ARG-039`, `UX-P2-01`_ · _source : nouvelle (règle maison « une colonne de vocabulaire = un enum ») ; arbitrage l. 410 ; contrat art. 5.4 ; synthèse du 2026-09-03, A-4, A-5 et A-11_
- **REQ-ARG-018** — Autofacture : produite **si et seulement si** `mandat.signeAt` < date du relevé ; mention « Autofacturation » ; émetteur = apporteur (raison sociale, SIREN, adresse, n° TVA si assujetti), destinataire = Axion-IA ; TVA selon `regimeTva` figé (mention art. 293 B si franchise) ; numéro de série par apporteur sans trou ; PDF et XML CII EN 16931 stockés avec hash SHA-256 ; immuable (correction = avoir). 🔴 **Sans mandat, le relevé n'est pas bloqué : il bascule en mode « facture de l'apporteur »** (2026-09-03, A-4 — annexe 2.5 réécrite) : la Société met le relevé à disposition, l'apporteur établit lui-même sa facture, le paiement intervient dans les trente jours de sa réception conforme, et le lot de paiement l'accepte à ce titre (REQ-ARG-035). 🔴 **Deux mentions obligatoires supplémentaires, sur le PDF ET sur le XML CII EN 16931** (art. L.441-9, I C. com., contrat art. 5.3 et annexe 2.2, A-5) : le **taux des pénalités de retard** (taux BCE de refinancement le plus récent majoré de dix points) et l'**indemnité forfaitaire de recouvrement de 40 euros** (art. D.441-5 C. com.). Sur ~3 600 autofactures par an que la Société rédige elle-même, leur absence est un manquement répété et documenté. Tests : mandat absent → aucune autofacture **et** relevé `a_payer` en mode facture de l'apporteur (vu rougir) ; les deux mentions présentes dans le PDF **et** dans le XML (assertion sur le nœud CII, pas seulement sur le rendu).
  <br>_module 15 · étape 11 · phase 1 · tâches : `JUR-T01c`, `T-ARG-016`, `T-ARG-038`, `T-ARG-039`_ · _source : audit-outil §3.3, §6.3 ; ARG-16, ARG-17, ARG-18 ; contrat art. 5.2, 5.3, annexe 2.2 et 2.5 ; synthèse du 2026-09-03, A-4 et A-5_
- **REQ-ARG-019** — Lot de paiement : `payout_batches.statut` enum `brouillon → approuve → exporte → rapproche | rejete` ; l'approbation enregistre adminId + horodatage ; approuver un lot déjà approuvé est un no-op journalisé ; exporter un lot non approuvé est refusé.
  <br>_module 15 · étape 11 · phase 2 · tâches : `T-ARG-017`_ · _source : audit-outil §6.3 ; ARG-22_
- **REQ-ARG-020** — SEPA : fichier pain.001 (version documentée par la banque, XSD validé en CI), un `PmtInf`, N `CdtTrfTxInf`, `EndToEndId = AP{statementId}` (≤ 35 caractères), `Ustrd` = numéro d'autofacture, montants = solde TTC du relevé, IBAN normalisés, jeu de caractères SEPA ; le fichier est généré une fois, son hash stocké, et tout ré-export renvoie les mêmes octets.
  <br>_module 15 · étape 11 · phase 2 · tâches : `T-ARG-018`_ · _source : audit-outil §6.3 ; ARG-21, ARG-22_
- **REQ-ARG-021** — Non-double-paiement : une ligne relevée appartient à exactement un relevé (`statementId` non null, unique par ligne) et un relevé à exactement un lot ; une ligne `payee` ne peut jamais être relevée à nouveau ; contrainte en base, pas seulement applicative.
  <br>_module 15 · étape 11 · phase 2 · tâches : `T-ARG-010`, `T-ARG-018`, `T-ARG-019`_ · _source : nouvelle (ARG-22)_
- **REQ-ARG-022** — Rapprochement : l'import du relevé bancaire rapproche d'abord par `EndToEndId`, sinon par (montant exact, IBAN, date ±3 j) avec confirmation ; un débit rapproché passe le relevé `rapproche` et ses lignes `payee` avec `paidAt` = date banque ; un retour de virement passe le relevé `rejete`, les lignes redeviennent relevables sans nouvelle autofacture, et le RIB passe `rib_a_verifier`.
  <br>_module 15 · étape 11 · phase 2 · tâches : `T-ARG-019`_ · _source : audit-outil §6.3 ; ARG-23_
- **REQ-ARG-023** — Export comptable : par autofacture une pièce 6222 (HT) + 44566 (TVA, seulement si > 0) / 401 auxiliaire apporteur (TTC) datée de l'autofacture ; par règlement rapproché une pièce 401 / 512 datée banque ; chaque pièce est équilibrée ; format conforme à l'import de la plateforme comptable (TIIME).
  <br>_module 16 · étape 11 · phase 3 · tâches : `T-ARG-030`_ · _source : audit-outil §6.3 ; ARG-24_
- **REQ-ARG-024** — DAS2 : cumul par bénéficiaire (SIRET apporteur) et par année civile des montants TTC effectivement VERSÉS (`paidAt` bancaire des lots rapprochés) ; export annuel (identité, SIRET, adresse, nature « commissions », montant) pour tout bénéficiaire ≥ seuil paramétré ; le seuil n'a pas de valeur par défaut valide.
  <br>_module 16 · étape 11 · phase 2 · tâches : `DM-19`_ · _source : audit-outil §3.3, §6.3, D9 ; ARG-20_
- **REQ-ARG-025** — 🔴 **Vigilance au SEUIL LÉGAL — contradiction interne levée le 2026-09-03 (A-5).** Cette exigence disait à la fois « quel que soit le cumul (HYP-D7) » dans son corps et « si cumul ≥ 500 000 cents » dans son texte fusionné : **deux règles opposées pour le même contrôle**. La règle en vigueur est celle de l'article 5.4 réécrit, alignée sur la loi : le cumul = Σ HT des autofactures émises du contrat + solde HT du relevé en cours ; le relevé passe `bloque` motif `vigilance_perimee` avant numérotation **lorsque ce cumul atteint le seuil des articles L.8222-1 et D.8222-5 C. trav.** (`SEUIL_VIGILANCE` = 500 000 cents HT, SSOT avec source et date) **et qu'il n'existe aucune attestation valide** ; sous le seuil, l'absence d'attestation **ne bloque aucun versement** et lève une alerte console. `valideJusquAu` = date d'attestation + 6 mois ; code de vérification URSSAF stocké ; rappel J-15 avant l'échéance et J-15 avant l'atteinte prévisible du seuil. ⚠️ `HYP-D7` est **tranchée** en faveur du seuil légal : le marqueur devient un test de non-régression (une comparaison qui bloque sous le seuil → rouge), et REQ-JUR-014 (absorbée) y renvoie sans le contredire. Tests frontière : cumul 499 999 sans attestation → **versé** (vu rougir sur l'ancienne règle) ; 500 000 sans attestation → bloqué ; attestation à J+181 → périmée.
  <br>_module 16 · étape 11 · phase 2 · tâches : `JUR-T16`, `T-ARG-032`_ · _source : audit-outil §3.3, §6.1, D7 ; ARG-19 ; contrat art. 5.4 et 6.2 ; L.8222-1, D.8222-5 C. trav. ; synthèse du 2026-09-03, A-5_
- **REQ-ARG-026** — Sortie : à `resilie`, **quel que soit le motif** : les lignes `acquise` sont portées au dernier relevé, produit au lot suivant **sans le seuil de versement minimal** (HYP-E1-17) ; les lignes `prevue` restent `prevue` et deviennent `acquise` si l'encaissement survient dans les conditions du contrat art. 12.3 ; un solde ≤ 0 devient une `creance_apporteur` exportée en compta, sans virement. Les attributions `convertie` restent résolubles jusqu'à leur expiration. Aucune ligne `dechue` n'existe (REQ-DM-020, REQ-JUR-007).
  <br>_module 15 · étape 11 · phase 2 · tâches : `DM-18`, `T-ARG-033`, `T-ARG-038`_ · _source : audit-outil §5 étape 12, D11 ; anti-abus §4 ; ARG-15, ARG-25 ; réécrite le 2026-09-03 — déchéance retirée du produit_
- **REQ-ARG-027** — Traçabilité : chaque ligne porte un snapshot (eventId, paymentId, factureNumero, factureHtCents, factureTtcCents, clientId, siren, raisonSociale, attributionId, apporteurId, gridVersionId, dateRef, resolutionPath) suffisant pour l'afficher et l'expliquer sans interroger axionia.
  <br>_phase 2 · tâches : `DM-15`, `T-ARG-010`, `T-ARG-035`_ · _source : tableaux-de-bord §2.2, §4.4 ; ARG-26_
- **REQ-ARG-028** — Immuabilité : sur `commission_lines`, `statements`, `autofactures` et `payout_batches`, tout UPDATE d'un montant ou d'un identifiant de rattachement et tout DELETE sont refusés par un trigger Postgres ; seules les transitions d'état énumérées sont permises ; les corrections sont des lignes d'ajustement signées (adminId, motif).
  <br>_module 16 · étape 11 · phase 2 · tâches : `T-ARG-016`, `T-ARG-034`_ · _source : audit-outil §9.1 ; tableaux-de-bord §2.5 ; ARG-27_
- **REQ-ARG-029** — Cloisonnement : l'espace apporteur, `GET /apporteurs/:id/statement/:month` et les téléchargements (relevé, autofacture) ne renvoient jamais une ligne, un relevé ou un document d'un autre apporteur, y compris par manipulation d'URL : **404 strictement identique à un identifiant inexistant** (même statut, même corps), PDF inclus — jamais 403, qui révélerait l'existence.
  <br>_module 8 · étape 11 · phase 0 · tâches : `INT-T17`, `SEC-05`_ · _source : tableaux-de-bord §6.1 ; audit-outil §7_
- **REQ-ARG-030** → **absorbée par REQ-DM-021** — Résolution de client : `paiement.recu` porte `clientId` et `siren` résolus par axionia ; si null, l'événement est accepté et produit une ligne `non_resolue` alertée en console ; un événement n'est jamais ignoré faute de client. _(source : anti-abus F12 ; ARG-08, ARG-09)_ **→ ABSORBÉE par REQ-DM-021** (annexe de dédoublonnage, fusion appliquée par GOV-001, 2026-09-03) : **le texte en vigueur est celui de REQ-DM-021** ; l'identifiant est conservé — et non supprimé — pour que les tâches qui le citent continuent de résoudre. **→ voir REQ-DM-021.**
  <br>_module 17 · phase -1 · tâches : `DM-15`, `INT-T01b`, `INT-T05`, `T-ARG-035`_ · _source : critique de complétude (2026-09-03)_
- **REQ-ARG-031** → **absorbée par REQ-DM-014** — Dérivation de la grille : `grid_versions` est alimentée uniquement par le script de publication qui lit `COMMERCIAL_COMMISSIONS` de pricing.ts ; le hash de la dernière version publiée est égal au hash calculé depuis pricing.ts ; aucune valeur de commission n'est écrite à la main dans Partners. _(source : plan D3 ; audit-attribution §3.3 ; règle maison « dériver, jamais recopier »)_ **→ ABSORBÉE par REQ-DM-014** (annexe de dédoublonnage, fusion appliquée par GOV-001, 2026-09-03) : **le texte en vigueur est celui de REQ-DM-014** ; l'identifiant est conservé — et non supprimé — pour que les tâches qui le citent continuent de résoudre. **→ voir REQ-DM-014.**
  <br>_étape 10 · phase 0 · tâches : `DM-03-A`, `DM-03-P`_ · _source : critique de complétude (2026-09-03)_
- **REQ-ARG-032** — Prévisionnel : « en cours » = Σ (commissionTotale − Σ acquis) par facture émise non soldée, ventilé par payeur (type, montant attendu, échéance financeur) depuis `facture.emise` ; affiché à l'apporteur avec l'échéance attendue.
  <br>_module 17 · étape 10 · phase 2 · tâches : `UX-P2-01`_ · _source : tableaux-de-bord §2.9, B13 ; audit-outil §7_
- **REQ-ARG-033** — Régime TVA de l'apporteur : `regimeTva` enum (`assujetti` | `franchise_293b`) avec historique daté ; l'autofacture fige le régime et le taux à sa date ; un changement de régime n'affecte que les autofactures postérieures. 🔴 **Lorsque la mention de la taxe résulte d'une erreur imputable à la Société ou à son système de facturation** (la facture est matériellement établie par le logiciel de la Société à partir d'un régime qu'elle a figé), **la Société en fait son affaire** : elle émet sans délai une facture rectificative et garantit l'apporteur de toute conséquence — écran de correction en console, motivé et journalisé, sans reprise sur le relevé de l'apporteur (annexe 2.3 complétée, M-30). Tests : régime `franchise_293b` et TVA portée → écran de correction disponible, facture rectificative émise, aucune ligne négative pour l'apporteur.
  <br>_module 16 · étape 11 · phase 2 · tâches : `T-ARG-010`, `T-ARG-016`_ · _source : audit-outil §3.3, §6.1 ; tva.ts RegimeTva ; ARG-16 ; contrat annexe 2.3 ; synthèse du 2026-09-03, M-30_
- **REQ-ARG-034** — 🔴 **Délai de versement et plafond de 60 jours — la chaîne réelle est mesurée, pas promise.** Le contrat s'engage désormais (art. 5.3 réécrit, A-5) sur **dix jours ouvrés** suivant l'établissement du relevé — et non cinq, qui ignoraient la chaîne réelle : contrôles bloquants → autofacture → lot `brouillon` → **approbation humaine** (REQ-ARG-019) → export pain.001 → exécution bancaire, avec `SEPA_EXPORT_ENABLED` fermé par défaut et un mois « à blanc » préalable (REQ-CPL-011) — **et en tout état de cause dans les soixante jours de l'émission de la facture** (art. L.441-10, I C. com.). Le produit porte les deux : un indicateur de console **par relevé** affiche l'âge depuis l'établissement et depuis l'émission de la facture ; une **alerte bloquante de console** se lève à J+45 depuis l'émission, et tout relevé encore non payé à **J+60** est une anomalie de premier rang, nommée, quel que soit son motif de blocage. Aucun motif de la liste « alertes » de REQ-ARG-017 ne peut porter un relevé au-delà de ce plafond. **Pénalités de retard et indemnité de 40 €** sont dues de plein droit et portées aux mentions de REQ-ARG-018. Tests : relevé établi le 1er, payé le 15e jour ouvré → alerte ; relevé à J+61 depuis l'émission de la facture → **rouge**, avec le motif nommé ; `SEPA_EXPORT_ENABLED` fermé → l'indicateur mesure quand même et l'alerte se lève.
  <br>_module 15 · étape 11 · phase 2 · tâches : `JUR-T16`, `T-ARG-015`, `T-ARG-039`_ · _source : contrat art. 5.3 et 5.4 ; L.441-10, I et D.441-5 C. com. ; REQ-CPL-011 ; synthèse du 2026-09-03, A-5 et arbitrage C-5_
- **REQ-ARG-035** — 🔴 **Après la fin du contrat, quelqu'un peut encore payer — la chaîne survit.** L'annexe 2.5 faisait cesser le mandat d'autofacturation « en même temps que le contrat », l'art. 12.3 oblige la Société à établir des relevés « jusqu'à extinction complète » des droits nés avant le terme, et l'art. 5.2 faisait de l'autofacturation le **seul** mode d'émission : après résiliation, la Société devait payer des commissions qu'elle n'avait plus le droit de facturer, et le produit ne connaissait **aucun chemin de paiement au-delà** (REQ-ARG-018 ne produisait d'autofacture que sous mandat `signe`, REQ-ARG-016 passait le relevé `bloque` sinon — aucune ligne `a_payer`, aucun virement, **à la sortie de chaque apporteur**). Trois règles, alignées sur l'annexe 2.5 et l'article 21 réécrits (A-4) : (1) le mandat **survit de plein droit** pour les seules commissions restant à acquérir au titre de l'art. 12.3, jusqu'à leur extinction complète — `Mandat.surviePostContrat = true` tant qu'il reste une ligne `prevue` ou `acquise` non payée ; (2) **la fin du mandat, quelle qu'en soit la cause, ne fait obstacle ni à l'acquisition ni au paiement** : le relevé bascule en mode « facture de l'apporteur » (REQ-ARG-018), payable dans les trente jours de la réception conforme, et le lot de paiement l'accepte à ce titre ; (3) le dernier relevé est produit **sans le seuil** (REQ-ARG-015, REQ-ARG-026) et l'apporteur conserve un **accès en lecture** ou reçoit ses relevés, factures et motifs de blocage par e-mail (REQ-SEC-032, REQ-UX-016). Tests : apporteur résilié, encaissement à M+8 → ligne `acquise`, relevé produit, **virement émis** (vu rougir : l'ancienne règle produisait `bloque` motif `mandat_non_signe`) ; mandat dénoncé avec préavis de 30 jours → mode facture de l'apporteur, paiement à J+30 ; extinction des droits → mandat clos, accès clos.
  <br>_module 15 · étape 11 · phase 2 · tâches : `T-ARG-016`, `T-ARG-033`, `T-ARG-038`, `UX-P2-04`_ · _source : contrat art. 5.2, 12.3, 21 et annexe 2.5 ; synthèse du 2026-09-03, A-4 — l'une des trois corrections les plus importantes du dossier_

### integration

- **REQ-INT-001** — Tout événement axionia → Partners est écrit dans une outbox persistante DANS la transaction de l'écriture métier qui le produit ; un rollback métier ne laisse aucun événement, un commit métier laisse exactement un événement.
  <br>_phase 0 · tâches : `INT-T02`_ · _source : nouvelle (dérivée de crm-sync/enqueue.ts, durcie car ici la perte coûte une commission)_
- **REQ-INT-002** → **absorbée par REQ-SEC-010** — Chaque requête sortante est signée HMAC-SHA256 de `<timestamp_unix>.<corps exact>` dans les en-têtes X-Axionia-Timestamp / X-Axionia-Signature ; le récepteur refuse en 401 toute signature absente, invalide, ou dont l'horodatage s'écarte de plus de 300 s ; comparaison à temps constant. _(source : audit-outil §2.3 ; src/server/crm-sync/emit.ts ; src/app/api/internal/crm-webhook/route.ts)_ **→ ABSORBÉE par REQ-SEC-010** (annexe de dédoublonnage, fusion appliquée par GOV-001, 2026-09-03) : **le texte en vigueur est celui de REQ-SEC-010** ; l'identifiant est conservé — et non supprimé — pour que les tâches qui le citent continuent de résoudre. **→ voir REQ-SEC-010.**
  <br>_phase 0 · tâches : `INT-T02`_ · _source : critique de complétude (2026-09-03)_
- **REQ-INT-003** — Chaque événement porte une enveloppe `{event_id (uuid v4), event_type, schema_version, occurred_at, emitted_at, producer, subject_ref, sequence, payload}` validée par un schéma Zod porté à l'identique des deux côtés ; un événement hors schéma est refusé en 422 et l'outbox le passe en `gave_up` avec alerte.
  <br>_module 5 · étape 5 · phase -1 · tâches : `INT-T01a`_ · _source : nouvelle (INT-F08)_
- **REQ-INT-004** — Les types d'événements sont : `client.cree`, `client.mis_a_jour`, `devis.signe`, `facture.emise`, `avoir.emis`, `paiement.recu`, `paiement.rembourse` — nommés sur les modèles réels (Client, Devis, FactureFormation, Payment) ; aucun événement ne référence `Invoice` ni `Refund`.
  <br>_étape 9 · phase -1 · tâches : `INT-T01a`, `INT-T05`_ · _source : audit-outil §2.3 corrigé par INT-F02/INT-F24_
- **REQ-INT-005** — `paiement.recu` porte `paymentId, factureId, clientId (bénéficiaire, jamais destinataire), siren, montantEncaisseTtcCents, factureMontantHtCents, factureMontantTtcCents, regimeTva, totalEncaisseTtcCents, paidAt, provider` ; Partners dérive le HT encaissé en centimes, arrondi vers le bas, et le dernier encaissement soldant la facture absorbe le reliquat de sorte que Σ HT dérivés = factureMontantHtCents.
  <br>_module 13 · étape 10 · phase -1 · tâches : `INT-T01b`, `INT-T05`_ · _source : tableaux-de-bord §2.4, R13 ; INT-F03_
- **REQ-INT-006** — `devis.signe` porte par ligne `{commissionId ∈ ids de COMMERCIAL_COMMISSIONS | null, montantHtCents, activite, designation}` calculé dans axionia par une fonction pure dérivée de pricing.ts ; Partners ne contient aucune copie de la grille.
  <br>_étape 9 · phase -1 · tâches : `INT-T01b`, `INT-T04`_ · _source : plan D3 ; audit-attribution §3.3 ; INT-F06/F07_
- **REQ-INT-007** — Tous les écrivains de Payment, du statut `accepte` de Devis, de l'émission de FactureFormation/avoir et de Client passent par une fonction d'émission unique ; un test-cliquet nominatif énumère les écrivains dans src/ et scripts/ et rougit si l'un n'émet pas.
  <br>_étape 9 · phase 0 · tâches : `INT-T03`, `INT-T04`, `INT-T05`, `SEC-28`_ · _source : nouvelle (INT-F04/F05, règle « chercher les appelants »)_
- **REQ-INT-008** — L'émission est inerte sans `PARTNERS_SYNC_ENABLED=true` : aucune ligne d'outbox, aucun job, aucun appel réseau ; au build (`DATABASE_URL` contenant stub.invalid) aucun code d'intégration ne s'exécute.
  <br>_phase 0 · tâches : `INT-T02`_ · _source : ADR 0026 ; crm-sync/config.ts_
- **REQ-INT-009** — Le rejeu suit un backoff exponentiel, 8 tentatives maximum, timeout 10 s ; 422 = abandon immédiat avec alerte ; 503 = tentative non comptée ; après abandon, la ligne reste visible en console et rejouable à la main par event_id.
  <br>_module 17 · phase 0 · tâches : `INT-T02`_ · _source : crm-sync/config.ts et emit.ts (dérivés)_
- **REQ-INT-010** → **absorbée par REQ-DM-036** — Le récepteur Partners enregistre chaque event_id dans une table à contrainte unique avant tout traitement, répond 200 `{duplicate:true}` sur doublon, traite hors requête (BullMQ) et ne répond jamais 5xx pour un échec de traitement. _(source : DocusealWebhookEvent + crm-webhook (patrons) ; INT-F09)_ **→ ABSORBÉE par REQ-DM-036** (annexe de dédoublonnage, fusion appliquée par GOV-001, 2026-09-03) : **le texte en vigueur est celui de REQ-DM-036** ; l'identifiant est conservé — et non supprimé — pour que les tâches qui le citent continuent de résoudre. **→ voir REQ-DM-036.**
  <br>_module 5 · étape 5 · phase 0 · tâches : `SEC-06`_ · _source : critique de complétude (2026-09-03)_
- **REQ-INT-011** — Un événement dont la dépendance manque (facture inconnue pour un paiement, client inconnu pour un devis) est stocké en `en_attente_dependance` et rejoué automatiquement à l'arrivée du parent ; il n'est jamais rejeté.
  <br>_étape 9 · phase 0 · tâches : `INT-T21-A`, `INT-T21-P`, `SEC-06`_ · _source : nouvelle (INT-F08)_
- **REQ-INT-012** — axionia expose `GET /api/partners/evenements?after_sequence=&limit=` (auth HMAC) qui relit l'outbox avec le payload exact conservé ; Partners peut rattraper toute lacune sans reconstruction.
  <br>_phase 0 · tâches : `INT-T02`, `INT-T08-A`, `INT-T08-P`, `INT-T21-A`, `INT-T21-P`_ · _source : nouvelle (INT-F10)_
- **REQ-INT-013** — Un job quotidien de réconciliation compare, sur 7 jours glissants, Σ encaissements HT par SIREN attribué côté axionia et côté Partners ; tout écart produit une alerte nommant les event_id manquants et déclenche leur rejeu ; le job journalise ses compteurs même quand tout va bien.
  <br>_étape 10 · phase 1 · tâches : `INT-T08-A`, `INT-T08-P`_ · _source : crm-sync/reconcile.ts (dérivé) ; INT-F10/F22_
- **REQ-INT-014** — API 1 `GET /attributions?siren=` : auth = jeton bearer dédié + allowlist IP + 60/min + **404 sans jeton** (REQ-SEC-012), route `/api/integrations/axionia/attributions?siren=` ; réponse **minimale** `{statut: libre|attribuee|cliente, until: 'AAAA-MM'|null, apporteurRef: opaque|null}` — **jamais nom, prénom, e-mail** (R3 : l'identité d'un apporteur ne traverse jamais la frontière) ; chaque appel journalisé (siren, ipHash, résultat) ; côté axionia, timeout 2 s, cache 5 min, échec ouvert : le devis se crée avec `attributionVerifiee=false` et une alerte, la vérification est rejouée par job.
  <br>_étape 9 · phase 0 · tâches : `INT-T07-A`, `INT-T07-P`, `SEC-07`_ · _source : audit-outil §2.3, §5 étape 9 ; audit anti-abus C13 ; INT-F11_
- **REQ-INT-015** — Quand API 1 répond `attribuee`, axionia exige un SIREN sur le Client avant l'envoi du devis ; le SIREN est normalisé (9 chiffres, Luhn) et dérivé du SIRET s'il est vide.
  <br>_étape 9 · phase 0 · tâches : `INT-T03`, `INT-T07-A`, `INT-T07-P`_ · _source : audit anti-abus F12/C13 ; INT-F20_
- **REQ-INT-016** — API 2 `GET /api/integrations/axionia/apporteurs/:ref/releve/:mois` rend un relevé JSON signé (lignes, HT, TVA selon régime déclaré, total, hash) et son PDF ; le format d'export comptable vers TIIME est décrit dans `docs/tiers/tiime.md` (URL officielle, date de lecture, extrait, exemple officiel) et chaque fixture d'export porte `Source:` vers cette fiche (gate REQ-GOV-022) ; un test valide l'export contre l'exemple officiel de la fiche.
  <br>_module 15 · étape 11 · phase 2 · tâches : `INT-T17`, `T-ARG-030`_ · _source : audit-outil §2.3, §6.3 ; INT-F12 ; reformulation d'annexe appliquée le 2026-09-03_
- **REQ-INT-017** — La grille de commissions est publiée par axionia via `GET /api/partners/grille` avec `grilleVersion` = SHA-256 du JSON canonique de COMMERCIAL_COMMISSIONS ; Partners en conserve chaque version et les contrats référencent un hash ; un test croisé compare hash publié et dernier snapshot.
  <br>_étape 10 · phase 0 · tâches : `DM-03-A`, `DM-03-P`_ · _source : plan D3 ; audit-outil §6.2 ; INT-F07_
- **REQ-INT-018** → **absorbée par REQ-SEC-034** — Le contrat DocuSeal de Partners vérifie strictement la signature `<ts>.<hex64>` (tolérance 300 s), sans repli plaintext ; l'idempotence est dérivée de (event_type, submission_id, timestamp) ; au `submission.completed`, le PDF signé et l'audit trail sont téléchargés immédiatement, stockés, et leur SHA-256 persisté avec `signed_at` et `contractVersion`. _(source : audit-outil §6.2 ; src/lib/docuseal.ts ; doc DocuSeal (rejeu 48 h, payload possiblement incomplet))_ **→ ABSORBÉE par REQ-SEC-034** (annexe de dédoublonnage, fusion appliquée par GOV-001, 2026-09-03) : **le texte en vigueur est celui de REQ-SEC-034** ; l'identifiant est conservé — et non supprimé — pour que les tâches qui le citent continuent de résoudre. **→ voir REQ-SEC-034.**
  <br>_module 5 · étape 5 · phase 1 · tâches : `INT-T12`, `INT-T24`_ · _source : critique de complétude (2026-09-03)_
- **REQ-INT-019** — Toute soumission DocuSeal `pending` depuis plus de 24 h est réinterrogée par `GET /submissions/:id` ; un contrat complété sans webhook reçu est rattrapé par ce chemin.
  <br>_module 5 · étape 5 · phase 1 · tâches : `INT-T12`, `INT-T24`_ · _source : nouvelle (INT-F13)_
- **REQ-INT-020** — L'autocomplétion entreprise passe par un proxy serveur : debounce 300 ms, `minimal=true&include=siege,dirigeants`, cache 24 h, limiteur global 5 req/s, respect de `Retry-After` ; sur 429/timeout/5xx, le dépôt bascule en saisie manuelle du SIREN/SIRET contrôlée par Luhn et la fiche est marquée `entreprise_a_verifier` — le dépôt n'est jamais bloqué.
  <br>_module 9 · étape 7 · phase 0 · tâches : `INT-T09`_ · _source : OpenAPI recherche-entreprises (7 req/s/IP, 30/s/ASN, per_page ≤ 25) ; audit-attribution P4 ; INT-F15_
- **REQ-INT-021** — Depuis la réponse API, Partners persiste `siren, siret (siège), nom_complet, nature_juridique, activite_principale, tranche_effectif_salarie, etat_administratif, categorie_entreprise, siege.{code_postal, libelle_commune, departement, region}` ; des dirigeants, seulement un hash normalisé de `nom+prenoms` et `qualite` ; jamais `annee_de_naissance` ; `statut_diffusion` est respecté à l'affichage.
  <br>_module 13 · étape 10 · phase 0 · tâches : `INT-T09`_ · _source : audit anti-abus M5 ; INT-F16_
- **REQ-INT-022** — Partners émet ses emails depuis une adresse humaine (jamais noreply@) d'un domaine dont SPF inclut eu.zeptomail.net et dont le DKIM ZeptoMail est publié ; l'envoi automatique n'est possible que si `PARTNERS_EMAIL_DMARC_VERIFIE=true` — un test prouve qu'aucun envoi ne part sans ce drapeau — et ce drapeau n'est posé qu'avec, dans `docs/tiers/zeptomail.md`, la référence datée du rapport rua montrant dkim=pass et spf=pass pour ce domaine.
  <br>_phase 0 · tâches : `INT-T10`_ · _source : src/lib/email/client.ts (DMARC p=reject mesuré 2026-09-01) ; INT-F17 ; reformulation d'annexe appliquée le 2026-09-03_
- **REQ-INT-023** — Les rebonds ZeptoMail de Partners sont reçus par un webhook vérifiant `Producer-Signature` (base64 percent-décodé, tolérance 300 s) et alimentent une liste de suppression ; un envoi vers une adresse supprimée est retenu et visible.
  <br>_phase 0 · tâches : `INT-T10`_ · _source : src/server/email/zeptomail-webhook-signature.ts ; INT-F17_
- **REQ-INT-024** — Les alertes console de Partners passent par un bot Telegram dédié avec dédup et plafond horaire par catégorie ; aucun message Telegram ne contient de coordonnées de tiers ni de lien de console.
  <br>_module 17 · phase 0 · tâches : `INT-T14`_ · _source : src/server/notifications (ADR 0027) ; ADR 0046 décision 5 ; audit-outil §9.3_
- **REQ-INT-025** — Toute notification Telegram/SMS à un apporteur exige une liaison par jeton signé à usage unique et un opt-in journalisé ; le contenu se limite à l'événement, sans nom d'entreprise tierce ni montant.
  <br>_module 13 · étape 10 · phase 3 · tâches : `UX-P3-01`_ · _source : audit-outil §7, D12 ; INT-F18_
- **REQ-INT-026** — L'adaptateur MCP `partners` est fédéré : porte `POST /api/mcp`, secret propre `PARTNERS_MCP_SHARED_SECRET` (503 sans secret, 401 secret faux, limiteur avant la serrure), contrat du socle porté à l'identique avec le sceau des profils vérifié, manifeste versionné et gardé par `pnpm mcp:manifeste`.
  <br>_phase 0 · tâches : `INT-T11`_ · _source : ADR 0046 ; audit-outil §2.3_
- **REQ-INT-027** — Les trois outils `partners.verifier_entreprise`, `partners.contacts_a_qualifier`, `partners.commissions_du_mois` sont `effect: read`, ne rendent ni coordonnées de tiers ni identité d'un autre apporteur (test de contenu sur fixtures), portent un `maxBytes` ≤ 6 500 ; un test génère la réponse maximale de chaque outil depuis son jeu versionné et vérifie qu'elle tient sous `maxBytes` (contrôle 4 de l'ADR 0046), et `pnpm mcp:manifeste` rougit si le manifeste diverge.
  <br>_module 13 · étape 10 · phase 1 · tâches : `INT-T13`, `INT-T17`_ · _source : ADR 0046 décisions 6-8 ; mémoire budget §14 ; INT-F19 ; reformulation d'annexe appliquée le 2026-09-03_
- **REQ-INT-028** → **absorbée par REQ-DM-029** — ~~L'antériorité au dépôt interroge `GET /api/partners/anteriorite` côté axionia (clients, devis, demandes entrantes selon la fenêtre C12) avec échec ouvert : dépôt accepté, marqué `anteriorite_non_verifiee`, re-vérifié par job.~~ 🔴 **RETIRÉE — le texte en vigueur est celui de REQ-DM-029.** L'arbitrage de contradiction (l. 424) l'avait déjà retirée : un appel synchrone est une **3e API hors du périmètre D1** (5 webhooks + 2 API) et rend le dépôt dépendant de la disponibilité d'axionia, contraire au principe « Partners est indépendant » ; l'évaluation se fait **localement** sur le miroir `EntrepriseConnue`. Le corps de cette exigence n'avait pas été mis à jour et la tâche `DM-10-A` la citait encore — **corrigé le 2026-09-03** (synthèse A-12), la tâche est **différée, ADR requis**. La mention « demande entrante de moins de quatre-vingt-dix jours » a été **retirée de l'article 3.3 du contrat** et ne peut y revenir qu'avec un **sixième événement**, donc un ADR amendant D1. L'identifiant est conservé — et non supprimé — pour que les tâches qui le citent continuent de résoudre. **→ voir REQ-DM-029.**
  <br>_module 9 · étape 9 · phase — · tâches : **aucune**_ · _source : fonctionnement §4.3 ; audit anti-abus F13/C12 ; INT-F21 ; arbitrage l. 424 ; synthèse du 2026-09-03, A-12_
- **REQ-INT-029** — Ne franchissent jamais la frontière axionia → Partners : les montants négociés avant `devis.signe`, l'identité des autres apporteurs, les coordonnées chiffrées du contact rencontré ; un test de contenu de payload le vérifie sur les fixtures.
  <br>_étape 9 · phase -1 · tâches : `INT-T01a`_ · _source : audit-outil §2.3 (« Ce qui ne traverse jamais la frontière »), B6, R3_
- **REQ-INT-030** — Un tableau de santé de l'intégration (des deux côtés) montre : lignes pending/failed/gave_up, âge de la plus ancienne, dernier événement émis/reçu par type, dernier passage de réconciliation et ses écarts ; alerte si pending > 15 min ou silence > 7 j avec activité.
  <br>_phase 1 · tâches : `INT-T08-A`, `INT-T08-P`, `QA-T19`_ · _source : crm-sync/health.ts (dérivé) ; INT-F22_
- **REQ-INT-031** — Les secrets d'intégration (webhook, MCP, DocuSeal, ZeptoMail) ne transitent jamais en clair dans une conversation ou un dépôt ; ils sont posés via les secrets GitHub → workflow `coolify-poser-variable.yml` → Coolify.
  <br>_module 5 · étape 5 · phase 0 · tâches : `INT-T02`_ · _source : ADR 0046 (« MCP_SHARED_SECRET »), mémoire jeton Calendly_

### juridique

- **REQ-JUR-001** — Aucun contenu public de recrutement (copy, mots-clés, JSON-LD JobPosting `occupationalCategory`, titres) ne contient aucun terme du lexique interdit (liste tenue hors dépôt), hors d'une unique phrase de désambiguïsation identifiée ; une garde CI rougit sinon.
  <br>_module 1 · étape 7 · phase 0 · tâches : `JUR-T03`, `JUR-T29`_ · _source : plan §2 D2, §3.2 · audit bout-en-bout §12.2 · nouvelle (vérification code)_
- **REQ-JUR-002** — Toute formulation publique ou contractuelle du déclencheur de commission dit « encaissement » ; les motifs /vente signée|dès la signature/ sont interdits dans src/content/recrutement et dans les gabarits d'e-mails.
  <br>_module 1 · étape 10 · phase 0 · tâches : `JUR-T03`, `JUR-T29`_ · _source : fonctionnement R7 · audit SIREN A8 · nouvelle_
- **REQ-JUR-003** — Le gabarit de contrat versionné contient chacun des identifiants de clause obligatoires suivants — **22 identifiants**, alignés un pour un sur la table de correspondance de `CONTRAT-APPORTEUR-V1.md` : CL-PORTEE, CL-ANTI-REQUALIF, CL-LIBERTE-EXERCICE, CL-ATTRIBUTION, CL-CONFIRMATION, CL-DUREE, CL-SINCERITE, CL-SUSPENSION-VERIFICATION, CL-ENCAISSEMENT, CL-REPRISE, CL-PARRAINAGE, CL-IDENTITE-PARRAINAGE, CL-AUTOFACTURATION, CL-VIGILANCE, CL-RGPD, CL-CPF, CL-DISCOURS, CL-CONFIDENTIALITE, CL-RESILIATION-ORDINAIRE, CL-RESILIATION-EFFETS, CL-DROIT, CL-GRILLE ; un gabarit incomplet ne peut pas être publié. **`CL-SANCTION` est retiré le 2026-09-03** : la clause de sanction n'existe plus, ni au produit ni au contrat (décision D11) ; un gabarit qui la porterait est refusé. ⚠️ **Les corrections du 2026-09-03 n'ajoutent AUCUN identifiant de clause** (P-5) : les matières nouvelles — charges sociales et devoir de vigilance (art. 6.5, 6.6), discussion des conditions et conflit d'intérêts (art. 8.3, 8.4), garantie plafonnée et contradictoire (art. 8.5), supports de présentation (art. 22) — **élargissent les libellés existants** `CL-VIGILANCE`, `CL-RGPD` et `CL-DISCOURS` plutôt que de créer un 23ᵉ identifiant, pour ne pas rouvrir cette exigence, la table de correspondance et le test à chaque ajout. Le compte reste **22**.
  <br>_module 5 · étape 10 · phase 0 · tâches : `JUR-T01`, `JUR-T01b`_ · _source : contrat art. 2 · audit anti-abus §4 · audit bout-en-bout §6.2 · décision du 2026-09-03_
- **REQ-JUR-004** — Aucun dépôt de contact n'est accepté pour un apporteur dont le contrat n'est pas à l'état `signed` avec `contractVersion` renseignée et hash du PDF stocké.
  <br>_module 5 · étape 7 · phase 1 · tâches : `INT-T12`, `INT-T23`_ · _source : audit bout-en-bout §5 étape 5_
- **REQ-JUR-005** — Le gabarit DocuSeal exige une acceptation distincte (case/initiales) sur **CL-AUTOFACTURATION, CL-REPRISE, CL-SUSPENSION-VERIFICATION, CL-RESILIATION-EFFETS et CL-RGPD**, ainsi que sur la clause attributive de compétence de l'article 14 (case propre, mention très apparente, art. 48 CPC) ; une soumission sans ces champs n'est pas reconnue `signe`. **`CL-SANCTION` est retirée de la liste le 2026-09-03** : elle rendait toute soumission insignable, donc tout dépôt impossible (REQ-JUR-004). 🔴 **Six acceptations, comptées** (M-20) : `contract-template-complete.spec.ts` vérifie qu'il y en a **exactement six** et que la note encadrée qui les signale figure **dans le corps** sous chacun des articles 3.7, 4.5, 5.2, 7, 12 **et** 14 — le corps ne la portait que sous l'art. 14, et la mention « très apparente » de l'art. 48 CPC était renvoyée à une note hors clause. Le gabarit porte en outre la variable `{{APPORTEUR_QUALITE}}`, **alimentée depuis le statut d'exercice recueilli au KYC** (REQ-DM-013, DM-11) et jamais saisie à la main : la validité de la clause attributive dépend d'un fait — la qualité de commerçant des deux parties — qu'une part significative des 300 apporteurs (professions libérales, retraités en cumul, associations) ne remplira pas. Tests : cinq acceptations → **rouge** ; note absente sous l'un des six articles → rouge ; `{{APPORTEUR_QUALITE}}` non résolue depuis le KYC → génération refusée.
  <br>_module 4 · étape 5 · phase 1 · tâches : `INT-T12`_ · _source : nouvelle (art. 1171 C. civ., art. 48 CPC, ADR 0014 SES) · décision du 2026-09-03 · synthèse du 2026-09-03, M-20_
- **REQ-JUR-006** — La suspension des déclarations est une mesure de vérification : elle prend effet immédiatement, est notifiée avec son motif (enum fermé `non_confirme | fraude`, REQ-JUR-031) et se lève par un rôle habilité, journalisée. La résiliation exige un acteur humain identifié, un motif écrit et le préavis `PREAVIS_JOURS` (contrat art. 11.1) ou, en cas d'inexécution, une décision motivée après mise en demeure (art. 11.2). Aucun compteur de gradation, aucun délai de « contradictoire », aucune conséquence automatique attachée à un nombre de déclarations. La machine d'états refuse structurellement une transition `suspendu`/`resilie` sans acteur.
  <br>_module 21 · étape 12 · phase 1 · tâches : `DM-09`_ · _source : audit anti-abus M1, §4 · audit bout-en-bout §1.1 · **texte unique arrêté le 2026-09-03** ; les deux autres définitions du fichier — arbitrage de contradiction et reformulation d'annexe — sont supprimées_
- **REQ-JUR-007** — Aucune ligne de commission ne peut prendre le statut `dechue` : la valeur est retirée de l'enum. À la fin du contrat, quel qu'en soit le motif, les lignes `acquise` sont payées au dernier relevé sans le seuil de versement minimal (HYP-E1-17) et les lignes `prevue` suivent la règle ordinaire — payées si l'encaissement correspondant survient dans les conditions du contrat art. 12.3. Un test prouve qu'aucun chemin de code ne produit `dechue`.
  <br>_module 15 · étape 10 · phase 0 · tâches : `DM-25`, `JUR-T01`, `JUR-T01b`, `T-ARG-033`_ · _source : note interne (hors dépôt) §6.1 D11, décision du 2026-09-03_
- **REQ-JUR-008** — Le formulaire de dépôt exige la case « j'ai informé cette personne » (échec fermé : absente ou décochée = refus serveur, pas seulement client) et enregistre la version du texte de la case.
  <br>_module 9 · étape 7 · phase 1 · tâches : `SEC-12`_ · _source : plan §3.3 · audit SIREN P8_
- **REQ-JUR-009** — Axion-IA informe elle-même le prospect (art. 14 RGPD) au premier contact : la mention est affichée dans le script de qualification et un e-mail d'information est envoyé et journalisé au plus tard lors de la qualification.
  <br>_module 12 · étape 8 · phase 0 · tâches : `JUR-T04`, `JUR-T09`_ · _source : nouvelle (art. 14 RGPD)_
- **REQ-JUR-010** → **absorbée par REQ-DM-031** — Les coordonnées du contact rencontré sont chiffrées au repos et purgées automatiquement à l'expiration d'une durée définie dans une SSOT `retention.ts` ; le SIREN et l'horodatage sont conservés ; un test prouve la purge (au-delà : absente ; en deçà : présente). _(source : audit SIREN P8 · audit bout-en-bout étape 12 · nouvelle)_ **→ ABSORBÉE par REQ-DM-031** (annexe de dédoublonnage, fusion appliquée par GOV-001, 2026-09-03) : **le texte en vigueur est celui de REQ-DM-031** ; l'identifiant est conservé — et non supprimé — pour que les tâches qui le citent continuent de résoudre. **→ voir REQ-DM-031.**
  <br>_phase 3 · tâches : `DM-20`_ · _source : critique de complétude (2026-09-03)_
- **REQ-JUR-011** — La réponse de « Vérifier une entreprise » est binaire (libre / non disponible), ne distingue jamais « cliente » de « suivie », n'expose ni identifiant d'apporteur ni date au jour (mois seul) ; chaque consultation est journalisée et plafonnée.
  <br>_module 10 · étape 7 · phase 1 · tâches : `SEC-16`_ · _source : audit bout-en-bout §1.3 D5 · fonctionnement R3 · nouvelle (secret des affaires)_
- **REQ-JUR-012** — Aucun écran de l'espace ni aucun gabarit d'e-mail ne contient d'objectif chiffré, de classement, de quota de vente, de formation exigée ni de conséquence à l'inactivité ; une garde lexicale (/objectif|quota|classement|top \d|challenge|obligatoire|vous devez/) rougit sur les textes destinés aux apporteurs.
  <br>_phase 1 · tâches : `JUR-T13`, `UX-P1-11`_ · _source : plan §3.2 · audit bout-en-bout §3.2, §7_
- **REQ-JUR-013** — Le webinaire et toute formation sont présentés comme facultatifs ; aucune transition d'état de l'apporteur ne dépend de sa présence.
  <br>_module 3 · étape 3 · phase 1 · tâches : `JUR-T13`, `UX-P1-11`_ · _source : plan §3.2 (aucune présence obligatoire) · audit bout-en-bout §3.2_
- **REQ-JUR-014** → **absorbée par REQ-ARG-025** — Attestation de vigilance et justificatif d'immatriculation sont exigés à la signature puis tous les 6 mois ; la vérification enregistre le code de sécurité URSSAF et le résultat ; un versement est bloqué (échec fermé) si l'attestation est absente ou périmée ; rappel à J-15. _(source : audit bout-en-bout §3.3, §6.1, D7 · L.8222-1, R.8222-1, D.8222-5 C. trav.)_ **→ ABSORBÉE par REQ-ARG-025** (annexe de dédoublonnage, fusion appliquée par GOV-001, 2026-09-03) : **le texte en vigueur est celui de REQ-ARG-025** ; l'identifiant est conservé — et non supprimé — pour que les tâches qui le citent continuent de résoudre. **→ voir REQ-ARG-025.**
  <br>_module 4 · étape 4 · phase 2 · tâches : `T-ARG-032`_ · _source : critique de complétude (2026-09-03)_
- **REQ-JUR-015** — Les seuils légaux (DAS2 2 400 €, vigilance 5 000 € HT, périodicité 6 mois, seuil de versement minimal, **préavis de résiliation ordinaire 30 j (`PREAVIS_JOURS`, contrat art. 11.1)**, fenêtres 12 mois attribution et 12 mois parrainage, péremption 90 j) vivent dans une seule SSOT avec source et date ; aucun littéral ailleurs ; un test échoue si une constante n'a pas de source. **La SSOT ne contient AUCUNE constante de gradation ni de délai de contradictoire : un test échoue si `CONTRADICTOIRE_JOURS` ou un seuil de strikes y apparaît** (décision du 2026-09-03). 🔴 **La SSOT ne portait que six littéraux quand le contrat en crée une vingtaine** (P-3) : chacun serait exécuté en dur par un cron et divergerait du contrat à la première modification. Elle est **étendue à TOUS les délais du contrat**, chaque constante nommée d'après son article — `PRISE_DE_CONTACT_JOURS_OUVRES = 2 // art. 3.2`, `CONFIRMATION_TACITE_JOURS = 30 // art. 3.2`, `FILE_FENETRE_REDECLARATION_JOURS = 15 // art. 3.5`, `FILE_EXPIRATION_MOIS = 12 // art. 3.5`, `SUSPENSION_MAX_JOURS = 15 // art. 3.7`, `ANTERIORITE_CLIENT_MOIS = 24` et `ANTERIORITE_DEVIS_MOIS = 6 // art. 3.3`, `PROLONGATION_DEVIS_MOIS = 3 // art. 3.4 (W9)`, `MISE_EN_DEMEURE_JOURS = 15 // art. 11.2`, `REPONSE_CONTESTATION_JOURS = 15 // art. 5.6 et 3.3`, `FORCLUSION_CONTESTATION_MOIS = 12 // art. 5.5`, `CONTESTATION_FACTURE_JOURS = 30 // art. 5.2`, `REPRISE_MOIS = 12 // art. 4.5`, `IMPUTATION_MOIS = 12`, `VERSEMENT_JOURS_OUVRES = 10` et `VERSEMENT_PLAFOND_JOURS = 60 // art. 5.3`, `PALIER_HORS_GRILLE_JOURS = 60 // annexe A1.7`, `MANDAT_DENONCIATION_JOURS = 30 // annexe 2.5`, `CONFIDENTIALITE_ANS = 2 // art. 9`, `FORCE_MAJEURE_MOIS = 3 // art. 15`. Le grep-test est **élargi aux valeurs 2, 3, 6, 15, 24 et 60** hors SSOT, et un **test de cohérence gabarit ↔ SSOT** (sur le modèle de REQ-EXT-028 a) rougit si une valeur rendue dans le contrat diffère de la constante. 🔴 **`PREAVIS_JOURS` est une constante UNIQUE, jamais indexée sur l'ancienneté** (M-16) : le contrat prévoyait « 30 jours la première année, augmenté d'un mois par année complète, plafond six mois » — la **structure exacte de L.134-11**, poussée au-delà de son maximum légal de trois mois, qui écrivait noir sur blanc que la relation est durable et prend de la valeur avec le temps ; et la SSOT ne portant qu'une valeur, la console aurait notifié 30 jours à un apporteur qui en avait six, mettant Axion-IA en rupture brutale **avec sa propre clause pour preuve**. Le préavis est désormais fixe quelle que soit l'ancienneté, stipulé au titre de L.442-1, II C. com. à l'exclusion de L.134-11 et suivants. Test : **toute fonction calculant un préavis à partir d'une ancienneté, d'une date d'entrée ou d'un nombre d'années → gate rouge**, vue rougir sur un témoin.
  <br>_module 14 · étape 12 · phase 0 · tâches : `JUR-T02`_ · _source : nouvelle (règle maison SSOT) · BOFiP ACTU-2024-00154 · contrat V1 · synthèse du 2026-09-03, P-3_
- **REQ-JUR-016** → **absorbée par REQ-ARG-024** — Le cumul DAS2 par bénéficiaire est calculé sur les sommes à l'état `payee` (date de virement) de l'année civile ; l'export annuel porte nom, adresse, SIRET, nature « commissions » et montant ; un test frontière couvre 2 399,99 / 2 400,00. _(source : audit bout-en-bout §3.3, §6.3 · art. 240 CGI)_ **→ ABSORBÉE par REQ-ARG-024** (annexe de dédoublonnage, fusion appliquée par GOV-001, 2026-09-03) : **le texte en vigueur est celui de REQ-ARG-024** ; l'identifiant est conservé — et non supprimé — pour que les tâches qui le citent continuent de résoudre. **→ voir REQ-ARG-024.**
  <br>_module 16 · étape 11 · phase 2 · tâches : `DM-19`_ · _source : critique de complétude (2026-09-03)_
- **REQ-JUR-017** → **absorbée par REQ-ARG-018** — Toute autofacture porte la mention « Autofacturation », le régime TVA snapshoté au KYC (avec « TVA non applicable, art. 293 B du CGI » en franchise), une numérotation chronologique continue propre à l'apporteur, et n'est réputée acceptée qu'au terme de la procédure d'acceptation contractuelle (délai paramétré, contestation possible) ; elle est archivée des deux côtés. _(source : audit bout-en-bout §3.3, §6.3, D9 · art. 289 I-2 CGI, 242 nonies ann. II)_ **→ ABSORBÉE par REQ-ARG-018** (annexe de dédoublonnage, fusion appliquée par GOV-001, 2026-09-03) : **le texte en vigueur est celui de REQ-ARG-018** ; l'identifiant est conservé — et non supprimé — pour que les tâches qui le citent continuent de résoudre. **→ voir REQ-ARG-018.**
  <br>_module 4 · étape 4 · phase 1 · tâches : `JUR-T01c`, `T-ARG-016`_ · _source : critique de complétude (2026-09-03)_
- **REQ-JUR-018** → **absorbée par REQ-ARG-016** — Aucune ligne de commission ne passe `a_payer` sans SIRET validé (API : `etat_administratif = A`), RIB validé, contrat `signed` et vigilance valide ; le motif du blocage est visible par l'apporteur. _(source : fonctionnement R10 · tableaux §2.8 · audit bout-en-bout §6.3)_ **→ ABSORBÉE par REQ-ARG-016** (annexe de dédoublonnage, fusion appliquée par GOV-001, 2026-09-03) : **le texte en vigueur est celui de REQ-ARG-016** ; l'identifiant est conservé — et non supprimé — pour que les tâches qui le citent continuent de résoudre. **→ voir REQ-ARG-016.**
  <br>_module 4 · étape 4 · phase 1 · tâches : `DM-11`, `JUR-T16`, `T-ARG-032`_ · _source : critique de complétude (2026-09-03)_
- **REQ-JUR-019** → **absorbée par REQ-DM-014** — Chaque attribution et chaque contrat portent `grilleVersion` (hash) et le contenu de la grille snapshotée ; la grille servie par axionia est dérivée de `pricing.ts` (un test compare le hash publié au hash calculé) ; Partners ne recopie jamais une valeur de commission. _(source : plan D3 · audit bout-en-bout §6.2 · règle maison « dériver »)_ **→ ABSORBÉE par REQ-DM-014** (annexe de dédoublonnage, fusion appliquée par GOV-001, 2026-09-03) : **le texte en vigueur est celui de REQ-DM-014** ; l'identifiant est conservé — et non supprimé — pour que les tâches qui le citent continuent de résoudre. **→ voir REQ-DM-014.**
  <br>_étape 10 · phase 0 · tâches : `DM-03-A`, `DM-23`, `JUR-T29`_ · _source : critique de complétude (2026-09-03)_
- **REQ-JUR-020** — L'enum des faits générateurs de commission ne contient que `paiement.recu` et `paiement.rembourse` ; aucun événement lié à l'inscription, au parrainage sans vente, ou à un volume de filleuls ne peut créer une ligne ; le calcul de parrainage ignore le parrain du parrain (un niveau).
  <br>_module 14 · étape 10 · phase 2 · tâches : `DM-16`_ · _source : plan §4.1 · fonctionnement R8 · L.121-15 C. conso_
- **REQ-JUR-021** → **absorbée par REQ-ARG-012** — Au KYC, le filleul ne partage avec son parrain ni SIRET, ni e-mail, ni téléphone, ni IBAN ; la violation refuse le lien de parrainage (échec fermé) et produit un signal. _(source : audit anti-abus F14, §4)_ **→ ABSORBÉE par REQ-ARG-012** (annexe de dédoublonnage, fusion appliquée par GOV-001, 2026-09-03) : **le texte en vigueur est celui de REQ-ARG-012** ; l'identifiant est conservé — et non supprimé — pour que les tâches qui le citent continuent de résoudre. **→ voir REQ-ARG-012.**
  <br>_module 14 · étape 4 · phase 1 · tâches : `DM-16`, `SEC-18`_ · _source : critique de complétude (2026-09-03)_
- **REQ-JUR-022** — Le KYC lève un signal bloquant « attestation spécifique requise » sur les codes NAF 69.20Z, 66.19B et 66.22Z ; l'enum `ProfessionReglementee` est typé ; la liste vit sous marqueur `HYP-JUR-PROF-REGLEMENTEES` avec un test qui échoue si elle change, **tant que la décision de Will n'est pas consignée dans `DECISIONS.md`** (il n'y a pas d'avocat sur le projet, décision du 2026-09-03). La couverture contractuelle est l'article 23 du contrat (déclarations de l'Apporteur : il n'est empêché par aucune règle professionnelle).
  <br>_module 4 · étape 4 · phase 1 · tâches : `DM-11`_ · _source : plan §7.1 · nouvelle · reformulation d'annexe appliquée le 2026-09-03_
- **REQ-JUR-023** — Aucune commission n'est due au titre d'une prestation **effectivement financée, en tout ou partie, par le compte personnel de formation**, quelle que soit l'origine du contact. 🔴 **La garde n'avait aucune donnée à lire** (constaté le 2026-09-03, M-15) : elle se fonde sur `cpfEligible`, un champ qui **n'existait dans aucune ligne de grille** — REQ-EXT-026 énumérait limitativement les colonnes d'une ligne de `GrilleContrat` et ne le portait pas. Le champ `cpfEligible: Boolean` est ajouté aux lignes de `GrillePubliee` **et** de `GrilleContrat` (REQ-EXT-026), **dérivé de `pricing.ts` par le script d'export** (REQ-DM-014, jamais retapé), et rendu en colonne « CPF » dans l'annexe 1 générée (REQ-EXT-028 a). ⚠️ **L'exclusion porte sur le financement effectif, pas sur l'éligibilité** : L.6323-8-1 C. trav. interdit la **prospection des titulaires** de comptes, pas la vente à une entreprise sur son budget — le contrat a été resserré en conséquence (art. 8.1 al. 2). Tests : ligne de grille sans `cpfEligible` → **gate rouge** (vue rougir avant l'ajout du champ) ; prestation financée par le CPF → aucune ligne de commission ; palier `cpfEligible = true` non financé par le CPF → commission due.
  <br>_module 13 · étape 10 · phase 0 · tâches : `DM-23`, `JUR-T01`, `SEC-12`_ · _source : plan §3.5 · contrat art. 8.1 · L.6323-8-1 C. trav. · synthèse du 2026-09-03, M-15_
- **REQ-JUR-024** — Les phrases sur le financement (OPCO, Qualiopi, CPF) dans toute page ou JSON-LD de recrutement sont dérivées de `isQualiopiCertificationObtenue()` ; un test vérifie que « OPCO » est absent du JobPosting quand le flag est faux.
  <br>_module 1 · étape 2 · phase 0 · tâches : `JUR-T03`_ · _source : plan §3.4 · code memo-isere/page.tsx:690 · nouvelle_
- **REQ-JUR-025** — L'espace apporteur affiche et fait accepter une politique de confidentialité propre à Partners (base légale, durées SSOT, sous-traitants, droits) à la première connexion ; l'apporteur peut exporter ses données (art. 15/20) depuis « Mon profil ».
  <br>_module 8 · étape 7 · phase 0 · tâches : `EXT-T01`, `JUR-T04`, `T-ARG-033`_ · _source : audit bout-en-bout §4 (RGPD à formaliser) · nouvelle_
- **REQ-JUR-026** → **absorbée par REQ-DM-024** — Le journal des attributions, qualifications, contrats, sanctions et paiements est append-only (rôle applicatif sans UPDATE/DELETE) et chaîné par hash ; un test tente une modification et attend un refus. _(source : audit SIREN P9 · audit bout-en-bout §9.1)_ **→ ABSORBÉE par REQ-DM-024** (annexe de dédoublonnage, fusion appliquée par GOV-001, 2026-09-03) : **le texte en vigueur est celui de REQ-DM-024** ; l'identifiant est conservé — et non supprimé — pour que les tâches qui le citent continuent de résoudre. **→ voir REQ-DM-024.**
  <br>_module 12 · étape 8 · phase 0 · tâches : `DM-01`_ · _source : critique de complétude (2026-09-03)_
- **REQ-JUR-027** → **absorbée par REQ-DM-038** — Tous les vocabulaires (statut apporteur, statut attribution, résultat de qualification, motif de perte, état de pièce KYC, type de résiliation, fait générateur) sont des enums Prisma ; une garde refuse toute colonne `String` nommée `statut|status|type|motif|resultat`. _(source : règle maison « colonne de vocabulaire = enum » · audit anti-abus M1)_ **→ ABSORBÉE par REQ-DM-038** (annexe de dédoublonnage, fusion appliquée par GOV-001, 2026-09-03) : **le texte en vigueur est celui de REQ-DM-038** ; l'identifiant est conservé — et non supprimé — pour que les tâches qui le citent continuent de résoudre. **→ voir REQ-DM-038.**
  <br>_module 4 · étape 4 · phase -1 · tâches : `DM-02`, `GOV-006`_ · _source : critique de complétude (2026-09-03)_
- **REQ-JUR-028** — Le code de parrainage n'est jamais posé dans un cookie persistant sans consentement ; il est propagé par URL/champ caché ou sessionStorage.
  <br>_module 14 · étape 10 · phase 1 · tâches : `SEC-21`_ · _source : plan §6 C2 · nouvelle (CNIL cookies)_
- **REQ-JUR-029** — Les contrats, autofactures, relevés et preuves de paiement sont conservés 10 ans ; la pièce d'identité est purgée dès validation (seule la date de validation reste) ; ces durées sont dans `retention.ts`.
  <br>_module 16 · étape 11 · phase 0 · tâches : `DM-11`, `JUR-T02`, `T-ARG-033`_ · _source : audit bout-en-bout §6.1, étape 12 · L.123-22 C. com._
- **REQ-JUR-030** — La page « Retraité » est en `noindex` avec bandeau tant que `PAGE_RETRAITES_VALIDEE_PAR_EC` est faux ; le contrat porte une déclaration de l'apporteur sur la vérification de sa situation auprès de sa caisse.
  <br>_module 9 · étape 7 · phase 3 · tâches : `JUR-T22`_ · _source : plan §7.3 · nouvelle_

### produit-ux

- **REQ-UX-001** — Le dépôt d'un contact depuis un téléphone compte ≤ 8 interactions (taps + champs) de l'accueil à l'écran de confirmation, avec une seule saisie de l'entreprise (nom + ville) et aucun champ SIREN/SIRET obligatoire ; vérifié par le test E2E mobile de REQ-QA-017 ; la mesure chrono sur appareil réel (iPhone, 4G) est ≤ 90 s et consignée (vidéo ou trace Playwright) dans la PR qui livre l'écran de dépôt.
  <br>_module 9 · étape 7 · phase 1 · tâches : `UX-P1-01`, `UX-P1-02`_ · _source : fonctionnement §4.1-4.2 ; audit-attribution P4 ; audit-outil §12.3 point 1 ; reformulation d'annexe appliquée le 2026-09-03_
- **REQ-UX-002** — Chaque issue possible d'un dépôt (enum `IssueDepot`) affiche un titre, un « pourquoi » et un « quoi faire », et dit explicitement si le dépôt est horodaté à son nom. 🔴 **L'enum est aligné un pour un sur les catégories des articles 3.3 et 3.3 bis** (réécrite le 2026-09-03, A-12, en miroir de `MotifRefusDepot` / REQ-SEC-022) : `enregistree`, `prioritaire`, `en_attente` (rang 1 ou 2), `file_complete` (art. 3.3 bis c), `anteriorite_client` et `anteriorite_devis` (art. 3.3, rendus à l'apporteur sous le libellé unique « déjà connue de la Société »), `etablissement_cesse` (a), `entreprise_hors_perimetre` (b), `opposition_demarchage` (d), `gele`, `captcha`, `brouillon_hors_ligne`. Les valeurs `fermee`, `financeur` et `deja_connue` sont **renommées** sur ces catégories : elles refusaient un droit pour un motif que le contrat n'écrivait pas. Le refus est notifié **avec sa catégorie**, sans révéler ni qui ni quand, et le texte dit qu'il **n'emporte aucune autre conséquence et n'est pas un manquement** ; chaque issue de refus porte en outre le lien vers la contestation écrite de REQ-DM-043. Tests : `Object.values(IssueDepot)` exhaustivement rendu ; une valeur sans base contractuelle → **gate rouge** ; snapshot des libellés sans date, sans nom, sans UUID.
  <br>_module 9 · étape 9 · phase 0 · tâches : `DM-25`, `SEC-12`, `UX-P0-01`, `UX-P1-02`, `UX-P3-05`_ · _source : audit-outil §1.1, §1.3 ; anti-abus M2, M5 ; contrat art. 3.3 et 3.3 bis ; synthèse du 2026-09-03, A-12_
- **REQ-UX-003** — Aucun texte visible par un apporteur ne contient les mots « attribution », « SIREN », « prorata », « quota », « strike », « commercial » (liste noire testée sur tous les fichiers de micro-copy et gabarits e-mail apporteur).
  <br>_module 13 · étape 10 · phase 0 · tâches : `UX-P0-01`, `UX-P1-10`, `UX-P3-02`_ · _source : audit-outil §12.3 point 1 et §12.2 ; plan §3.2_
- **REQ-UX-004** — Le statut affiché à l'apporteur est dérivé par une fonction pure exhaustive `statutApporteur(EtatAttribution)` ; chaque état interne (dont provisoire, en_attente, invalidée, perdue, expirée, périmée) a un libellé, une couleur et une phrase « prochaine étape et par qui » ; l'ajout d'une valeur d'enum sans libellé fait échouer le typecheck et le test.
  <br>_phase 1 · tâches : `UX-P1-05`_ · _source : tableaux-de-bord §3.2 ; audit-outil §7 et §8_
- **REQ-UX-005** — Avant signature d'un devis, aucun montant (prix, devis, marge) n'est transmis au client ni affiché dans l'espace apporteur ; après signature, seule la commission de l'apporteur est affichée. Le contrôle porte sur le DTO serveur, pas seulement sur le rendu.
  <br>_module 8 · étape 9 · phase 2 · tâches : `UX-P2-01`, `UX-P2-06`_ · _source : tableaux-de-bord §3.2 et B6 ; audit-outil §2.3_
- **REQ-UX-006** — Aucune réponse serveur de l'espace apporteur ne contient l'identité (nom, e-mail, code, id) ni les montants d'un autre apporteur ; toute route paramétrée (/entreprises/:id, /commissions/:id, /documents/:id) répond 404 pour un id appartenant à un autre apporteur. 🔴 **« Mes filleuls » n'affiche QUE le montant de parrainage qui revient au parrain, agrégé par mois** (contrat art. 4.6 al. nouveau, M-12) : le parrainage n'emporte **aucune fonction d'encadrement, d'animation, de formation ni de suivi**, et le parrain **n'a accès à aucune donnée relative à l'activité du filleul** — ni nom, ni nombre de déclarations, ni état d'avancement, ni date de dernière vente, ni existence même d'une activité. Un écran qui ferait voir au parrain ce que fait son filleul est un écran de management, et c'est la lecture que l'écran produira devant un juge. Tests : assertion sur les **clés exactes** du DTO de parrainage (montant + mois, rien d'autre), **vue rougir** sur toute clé d'activité ; deux filleuls, l'un actif l'autre non → **DTO byte-identique** hors montant.
  <br>_module 14 · étape 10 · phase 0 · tâches : `DM-16`, `SEC-05`, `UX-P2-04`, `UX-P2-06`_ · _source : R3 ; tableaux-de-bord §6.1 ; audit-outil §7 ; contrat art. 4.6 ; nouvelle (UX-08) ; synthèse du 2026-09-03, M-12_
- **REQ-UX-007** — « Vérifier une entreprise » retourne un des 5 états (libre, suivie_place_disponible, suivie_file_complete, deja_connue, non_disponible) ; la date « jusqu'au » n'est affichée que si l'attribution est `active` ; l'état ne distingue jamais « cliente » de « en discussion » ; 30 vérifications/jour, la 31e affiche un message et laisse « Déposer » actif.
  <br>_module 10 · étape 7 · phase 1 · tâches : `SEC-16`, `UX-P1-01`_ · _source : audit-outil §1.3 et D5 ; anti-abus M1, M2 ; nouvelle (UX-04, UX-09)_
- **REQ-UX-008** — L'accueil de l'espace tient au-dessus du pli sur 375×667 px avec : 3 chiffres (versé / à verser / en cours), au plus 1 alerte, 1 champ Entreprise ; la navigation est une barre fixe à 4 onglets ≥ 48 px ; les autres écrans sont sous « Plus ».
  <br>_module 13 · étape 10 · phase 0 · tâches : `UX-P0-02`, `UX-P1-08`_ · _source : tableaux-de-bord §3.1 ; audit-outil §12.3 ; nouvelle (UX-06)_
- **REQ-UX-009** — La tuile « à verser » a 4 états (a_verser avec date, bloque avec le premier motif et un lien direct vers la pièce manquante, reporte sous le seuil de versement, rien) ; un motif de blocage est toujours accompagné de l'action qui le lève.
  <br>_module 15 · étape 11 · phase 2 · tâches : `UX-P2-02`_ · _source : audit-outil §6.3 et §7 Ma conformité ; tableaux-de-bord §2.7 B4_
- **REQ-UX-010** — « Mes commissions » ventile chaque prestation par payeur avec l'échéance attendue quand le webhook la fournit, et un texte de délai habituel (« 30 à 60 j après la formation ») quand elle est absente ; jamais une date calculée localement.
  <br>_module 8 · étape 10 · phase 2 · tâches : `UX-P2-01`_ · _source : tableaux-de-bord §2.9 et B13 ; audit-outil §2.3 (dépendance webhook)_
- **REQ-UX-011** — Une ligne de devis dont le palier n'a pas d'entrée dans la grille du contrat s'affiche « **Prestation hors grille de commissions** » — libellé apporteur du `motifBlocage = a_qualifier` (REQ-ARG-017) —, **jamais « 0 € », jamais un taux deviné, jamais un silence**. 🔴 **L'écran affiche aussi l'échéance de règlement** (contrat annexe A1.7 réécrit, A-11) : « la situation est réglée avant le [date] », soit `encaissement + PALIER_HORS_GRILLE_JOURS` (60 j, SSOT), et **à défaut de décision de la Société dans ce délai, la commission est due au taux ou au forfait que la grille PUBLIÉE portait à la date de la vente** — valeur écrite, opposable, lisible des deux parties, jamais une estimation par proximité de prix (arbitrage C-6). Tests : ligne hors grille → libellé + date d'échéance ; J+61 sans décision → ligne calculée sur la grille publiée à la date de vente et passée `acquise` (vu rougir sur l'ancien blocage sans terme) ; jamais « 0 € » dans le rendu.
  <br>_module 13 · étape 10 · phase 2 · tâches : `UX-P2-01`, `UX-P2-05`_ · _source : audit-attribution §3.3 A12-A16 ; pricing.ts (com-un-a-un `scale`) ; contrat annexe A1.5 et A1.7 ; nouvelle (UX-13) ; synthèse du 2026-09-03, A-11 et arbitrage C-6_
- **REQ-UX-012** — Depuis toute ligne de commission, l'apporteur atteint par un lien direct (une activation) la prestation, la facture (numéro, montant HT) et l'encaissement (date, montant) qui la justifient ; depuis la console, la même chaîne remonte jusqu'à l'apporteur et redescend jusqu'à l'encaissement ; un test E2E suit chaque lien depuis une fixture et vérifie la cible.
  <br>_module 17 · étape 10 · phase 2 · tâches : `UX-P2-01`_ · _source : tableaux-de-bord §2.2 et §4.4 ; reformulation d'annexe appliquée le 2026-09-03_
- **REQ-UX-013** — Le dépôt hors-ligne est stocké localement en `brouillon` (texte libre entreprise + ville, personne rencontrée, contexte, case RGPD), affiche « Enregistré sur votre téléphone — l'heure retenue est celle de la réception par Axion-IA », est envoyé automatiquement au retour du réseau, et affiche l'issue réelle (REQ-UX-002) une fois reçu ; un refus d'authentification au retour laisse le brouillon visible avec un message.
  <br>_module 9 · étape 7 · phase 1 · tâches : `UX-P1-03`_ · _source : audit-outil §12.3 point 3 ; contrat art. 1.5 ; nouvelle (UX-03)_
- **REQ-UX-014** — L'espace est une PWA installable (manifest, service worker, icône, écran hors-ligne) ; les notifications push sont opt-in explicite, jamais demandées à la première visite, et chaque push ouvre la route profonde de l'événement.
  <br>_module 8 · étape 7 · phase 3 · tâches : `UX-P3-01`_ · _source : audit-outil §12.3 point 3 ; nouvelle_
- **REQ-UX-015** — Après un lien magique réussi, la session sur l'appareil dure **30 jours** (REQ-SEC-003 prime : la sécurité l'emporte sur le confort), révocable, révocable depuis Mon profil et depuis la console ; l'e-mail porte aussi un code à 6 chiffres saisissable dans l'app ; un lien déjà consommé affiche une page dédiée avec « M'envoyer un nouveau lien », jamais une erreur brute.
  <br>_module 17 · phase 1 · tâches : `UX-P1-04`_ · _source : tableaux-de-bord §1 B8 ; src/server/formateur/magic-link.ts (TTL 15 min, usage unique) ; nouvelle (UX-17)_
- **REQ-UX-016** — Les événements de notification sont déclarés dans une table unique (événement → destinataire, canaux, défaut, opt-out possible, lien profond, gabarit, **`faitCourirUnDelai: boolean`**) ; l'écran de préférences est dérivé de cette table ; relevé disponible et paiement bloqué sont toujours envoyés par e-mail. 🔴 **Toute notification qui fait courir un délai part par COURRIER ÉLECTRONIQUE, et le délai court à compter de cet envoi** (contrat art. 20 complété, M-23) — mise à disposition d'une facture (art. 5.2), mise en demeure et résiliation (art. 11), réponse à contestation, décisions relatives aux attributions : trois délais couraient depuis un dépôt en ligne que l'apporteur pouvait n'avoir jamais vu, ce qui est une **obligation implicite de consultation régulière de l'outil de la Société assortie de la perte de droits** — et, après résiliation, `sessionVersion` coupait l'accès (REQ-SEC-032), de sorte que la Société aurait pu notifier valablement dans un espace fermé. 🔴 **Le dépôt d'un message dans l'espace ne fait courir AUCUN délai** : toute entrée de la table portant `faitCourirUnDelai: true` et n'ayant pas le canal e-mail obligatoire → **gate rouge**. La **date d'envoi est journalisée** et c'est elle que lisent les calculs de délai (jamais `lu_le`, jamais `dernierUsageAt` — REQ-JUR-039). **Après la fin du contrat**, les notifications sont faites par e-mail à la **dernière adresse déclarée** (REQ-ARG-035, REQ-SEC-032). Tests : chaque entrée `faitCourirUnDelai` a le canal e-mail ; le calcul du délai de contestation lit `envoyeAt`, jamais une date de consultation (vu rougir) ; apporteur résilié → e-mail envoyé, délai courant.
  <br>_module 21 · étape 12 · phase 1 · tâches : `INT-T24`, `UX-P1-09`, `UX-P1-10`, `UX-P1-11`, `UX-P3-01`, `UX-P3-06`_ · _source : audit-outil §7 Notifications, §9.3, §12.3 ; D12 ; contrat art. 5.2, 11, 12.3, 20 et annexe 2.4 ; synthèse du 2026-09-03, M-22 et M-23_
- **REQ-UX-017** — Toute page de l'espace apporteur passe axe-core avec 0 violation serious/critical (profil iPhone et desktop), toutes les cibles interactives mesurent ≥ 48×48 CSS px, le corps de texte est ≥ 18 px avec contraste ≥ 4.5:1, et à 320 px / zoom 200 % aucune page ne défile horizontalement.
  <br>_module 8 · étape 7 · phase 0 · tâches : `UX-P0-02`, `UX-P0-03`_ · _source : audit-outil §12.3 point 2 ; tests/e2e/a11y.spec.ts (harnais existant) ; nouvelle_
- **REQ-UX-018** — Toute page de la console passe axe-core avec 0 violation serious/critical et respecte les cibles 44 px mobile / 24 px desktop du design system admin (ADR 0028).
  <br>_module 17 · phase 0 · tâches : `UX-P0-03`, `UX-P3-04`_ · _source : docs/admin-design-system.md §A11y ; tableaux-de-bord §4_
- **REQ-UX-019** — Chaque écran de l'espace et de la console a un état vide déclaré (titre, phrase, action principale) ; l'état vide de « Mes entreprises » guide vers le premier dépôt.
  <br>_module 8 · étape 7 · phase 0 · tâches : `UX-P0-01`, `UX-P1-08`_ · _source : audit-outil §12.3 point 4 ; plan §4.2 (J7)_
- **REQ-UX-020** — L'autocomplétion entreprise répond en < 300 ms au p75 depuis le cache serveur, mesuré par un test de charge (k6, 200 requêtes, cache chaud) exécuté sur la preview de chaque PR touchant le proxy ; tolérance aux fautes : un test unitaire prouve qu'une raison sociale à distance de Levenshtein ≤ 2 est proposée ; la ville est un second critère ; le repli « Je ne trouve pas mon entreprise » (raison sociale + ville + code postal, SIREN facultatif) produit l'état `a_rapprocher` et un test prouve que le dépôt aboutit avec l'API coupée.
  <br>_module 9 · étape 7 · phase 0 · tâches : `INT-T09`_ · _source : audit-outil §12.3 point 5 ; audit-attribution P4, A9 ; reformulation d'annexe appliquée le 2026-09-03_
- **REQ-UX-021** — La fiche de qualification console est un formulaire à validation conditionnelle testée cas par cas (date obligatoire si rdv/rappeler ; motif obligatoire si perdue ; `non_confirme` force perdue + motif `non_confirme`), avec bouton d'appel `tel:`, compteur de tentatives et prochaine tentative ; le cas nominal (confirmé + rdv + date) se remplit en ≤ 6 interactions en E2E mobile (projet mobile-chrome), toutes les cibles ≥ 44 px.
  <br>_module 12 · étape 8 · phase 1 · tâches : `UX-P1-06`_ · _source : audit-outil §8 ; anti-abus M1 ; reformulation d'annexe appliquée le 2026-09-03_
- **REQ-UX-022** — La file de qualification est triée vérification prioritaire > `aQualifierDepuis` croissant ; le chrono SLA (48 h ouvrées, calendrier fériés France) porte sur `aQualifierDepuis` (posée à l'entrée en provisoire, y compris par promotion), jamais sur `deposeLe`.
  <br>_module 12 · étape 8 · phase 0 · tâches : `CPL-T13`, `UX-P1-07`_ · _source : audit-outil §8, §9.2 ; anti-abus M2 ; nouvelle (UX-25)_
- **REQ-UX-023** — Côté apporteur, une attribution en qualification affiche l'une des phrases dérivées serveur : « Axion-IA appelle avant le [date] », « Axion-IA a tenté N fois, prochaine tentative le … », « Nous vous appelons d'abord (vérification prioritaire) », « Rendez-vous le … », « Proposition envoyée » ; l'état invalidée affiche un texte non accusatoire avec la date de re-déclaration possible.
  <br>_module 12 · étape 8 · phase 1 · tâches : `UX-P1-05`_ · _source : audit-outil §7 Mes entreprises, §8 SLA ; anti-abus M1_
- **REQ-UX-024** → **absorbée par REQ-SEC-023** — La console applique une matrice écran × rôle × action (**admin, qualifieur, comptable, lecteur** — cf. REQ-SEC-023) déclarée dans un module unique, consommée par le layout et par chaque Server Action ; un écran ou une action absent de la matrice est refusé. _(source : nouvelle (UX-15) ; règle maison « droit porté par un rôle, défaut = refus »)_ **→ ABSORBÉE par REQ-SEC-023** (annexe de dédoublonnage, fusion appliquée par GOV-001, 2026-09-03) : **le texte en vigueur est celui de REQ-SEC-023** ; l'identifiant est conservé — et non supprimé — pour que les tâches qui le citent continuent de résoudre. **→ voir REQ-SEC-023.**
  <br>_module 12 · étape 8 · phase 0 · tâches : `SEC-17`, `UX-P2-03`, `UX-P3-05`_ · _source : critique de complétude (2026-09-03)_
- **REQ-UX-025** — L'écran Lot de paiement expose une machine d'états brouillon → approuvé → exporté → rapproché ; un seul fichier pain.001 par état exporté ; la régénération exige un motif et invalide le msgId précédent ; chaque ligne porte un EndToEndId unique posé à l'approbation.
  <br>_module 15 · étape 11 · phase 2 · tâches : `T-ARG-017`, `UX-P2-03`_ · _source : audit-outil §6.3 ; nouvelle (UX-16)_
- **REQ-UX-026** — La grille de commissions dans Partners est importée depuis pricing.ts (JSON généré + hash = version), affichée en lecture seule dans Paramètres avec version, date et diff ; la même version apparaît dans le contrat PDF et dans le calcul.
  <br>_étape 10 · phase 1 · tâches : `UX-P1-14`, `UX-P2-05`, `UX-P2-07`_ · _source : plan D3 ; audit-outil §6.2 et §9.2 ; règle maison « dériver, jamais recopier »_
- **REQ-UX-027** — Un changement de RIB crée une pièce `a_verifier` (l'ancien reste actif), déclenche un e-mail de confirmation et une alerte console ; un RIB modifié < 7 j avant le 1er bloque la ligne du lot avec le motif « RIB en cours de vérification ».
  <br>_module 17 · étape 4 · phase 1 · tâches : `SEC-22`, `UX-P1-09`_ · _source : audit-outil §6.1 et §7 Mon profil ; nouvelle (UX-20)_
- **REQ-UX-028** — Le fil Aide/Messages a des états (ouvert, répondu, clos), une recherche FAQ préalable avec 3 suggestions, un engagement affiché « réponse sous 2 jours ouvrés » mesuré par le même module de SLA que la qualification, et une notification à la réponse.
  <br>_module 12 · étape 8 · phase 0 · tâches : `CPL-T13`, `UX-P3-03`_ · _source : audit-outil §4 module 20, §7, §9.2 ; nouvelle (UX-21)_
- **REQ-UX-029** — Mon activité n'affiche aucun objectif, classement ni comparaison à d'autres apporteurs ; le palier et ce qui le fait monter sont formulés comme un capital de confiance (contacts confirmés), jamais comme un quota de vente.
  <br>_module 13 · étape 10 · phase 3 · tâches : `UX-P3-02`_ · _source : audit-outil §3.2, §7 Mon activité ; plan §3.2_
- **REQ-UX-030** — Mes documents contient chaque version du contrat, les avenants, les relevés, les autofactures, l'attestation annuelle, l'attestation de vigilance déposée avec son échéance, et un export RGPD de ses propres données ; Mon profil affiche les dernières connexions.
  <br>_module 4 · étape 11 · phase 2 · tâches : `UX-P2-04`_ · _source : audit-outil §7 Mes documents ; nouvelle (UX-26)_
- **REQ-UX-031** — Ma conformité affiche chaque pièce du §6.1 avec état (manquant / à vérifier / valide / périmé), date, ce qu'elle bloque (contrat / paiement / rien) et un bouton d'upload ; l'upload de la pièce d'identité affiche l'engagement de purge après validation.
  <br>_module 4 · étape 4 · phase 1 · tâches : `UX-P1-09`, `UX-P3-05`_ · _source : audit-outil §6.1, §7 Ma conformité_
- **REQ-UX-032** — Le lien de parrainage partageable (WhatsApp, LinkedIn, SMS, copie) est fourni avec un message prêt à copier disant « apporteur d'affaires », jamais « commercial ».
  <br>_module 14 · étape 10 · phase 2 · tâches : `UX-P2-04`_ · _source : audit-outil §12.1, D15 ; tableaux-de-bord §3.4_
- **REQ-UX-033** — Le bundle First Load JS de chaque route de l'espace apporteur est ≤ 75 KB gz et mesuré à la main avant/après dans la PR qui l'introduit ; LCP ≤ 1 800 ms et CLS = 0 sur profil 4G lente.
  <br>_module 8 · étape 7 · phase 0 · tâches : `QA-T20`, `UX-P1-08`, `UX-P3-01`_ · _source : AGENTS.md ; tableaux-de-bord §6.2 ; audit-outil §12.3 point 6_
- **REQ-UX-034** — Le mode sombre et le mode clair de l'espace sont pilotés par des tokens propres (jamais ceux de la console admin), tous deux passant les mêmes tests de contraste.
  <br>_module 17 · phase 0 · tâches : `UX-P0-02`_ · _source : audit-outil §12.3 point 6_
- **REQ-UX-035** — Le funnel réseau de la console se lit par canal et par cohorte mensuelle jusqu'à « 1er dépôt confirmé » et « actif à 90 j » ; l'écran Territoire est un tableau par département (carte SVG statique optionnelle à la demande).
  <br>_module 17 · étape 1 · phase 3 · tâches : `CPL-T15`, `UX-P3-04`_ · _source : audit-outil §9.2 ; plan §1, §9 ; anti-abus §3_

### qualite-ci-devops

- **REQ-QA-001** — Tout le code métier (commissions, prorata, reprises, parrainage, attributions, quotas, machines à états, scoring) vit sous src/domain/** sans aucune I/O (ni Prisma, ni Redis, ni fetch, ni new Date()) ; un lint bloquant le vérifie.
  <br>_module 13 · étape 10 · phase 0 · tâches : `QA-T01`_ · _source : nouvelle (dérivée de tableaux-de-bord §2 « irréprochable » et audit-anti-abus §2)_
- **REQ-QA-002** — La couverture de src/domain/** est de 100 % lignes et 100 % branches ; la gate a été vue rougir en retirant un test avant activation.
  <br>_phase 0 · tâches : `QA-T01`, `QA-T30`_ · _source : nouvelle (prompt : couverture du domaine 100 %)_
- **REQ-QA-003** — Pour toute facture découpée en 1..12 encaissements et tout taux de TVA ∈ {0, 5.5, 10, 20}, la somme des commissions acquises par encaissement est exactement égale à la commission totale (property-based, ≥ 500 cas générés).
  <br>_étape 10 · phase 2 · tâches : `QA-T21`_ · _source : tableaux-de-bord §2.4 + QA-F03_
- **REQ-QA-004** — Chaque machine à états (attribution : en_attente/provisoire/active/rdv_pris/proposition/signée/convertie/invalidée/perdue/expirée/périmée ; apporteur : candidat/retenu/vivier/signé/actif/dormant/gelé/suspendu/résilié ; ligne de commission : en_attente_encaissement/acquise/a_payer/payee/reprise/bloquée) est un enum Prisma + une table de transitions ; toute transition hors table lève une erreur typée ; un test énumère exhaustivement les couples (état, événement).
  <br>_module 11 · étape 10 · phase 1 · tâches : `DM-08`_ · _source : audit-outil-bout-en-bout §8, §5 étape 12 · tableaux-de-bord §2.6 · règle maison « colonne de vocabulaire = enum »_
- **REQ-QA-005** — Les attributions actives portent un index unique PARTIEL sur siren WHERE statut IN (ETATS_OCCUPANTS) — 7 états, cf. REQ-DM-003 ; aucune liste littérale hors REQ-DM-003 ; un test d'intégration prouve qu'un SIREN périmé peut être redéposé et qu'un second dépôt actif est refusé.
  <br>_module 11 · étape 7 · phase 2 · tâches : `QA-T28`, `QA-T29`_ · _source : audit-anti-abus F6, M2 · règle maison_
- **REQ-QA-006** — Les tests d'intégration tournent sur un Postgres 16 (pgvector) et un Redis éphémères via testcontainers, un schéma migré par `prisma migrate deploy`, isolation par transaction ou base par fichier ; aucun test ne dépend d'un .env partagé.
  <br>_phase 0 · tâches : `QA-T02`_ · _source : nouvelle (prompt) ; contraste avec vitest.integration.config.ts axionia_
- **REQ-QA-007** — Les 5 webhooks entrants et les 2 API sortantes ont chacun un schéma Zod versionné (`schemaVersion`) transcrit à l'identique dans les deux dépôts, avec un test de transcription datée de chaque côté ; les fixtures de contrat sont générées par le producteur réel d'axionia (FactureFormation, Payment, Devis, Client) et non écrites à la main.
  <br>_module 9 · étape 9 · phase -1 · tâches : `INT-T01a`, `INT-T01b`_ · _source : audit-outil-bout-en-bout §2.3 + QA-F01, QA-F14 · patron tests/e2e-crm-sync/contract.spec.ts_
- **REQ-QA-008** → **absorbée par REQ-SEC-010** — Tout webhook entrant est signé HMAC-SHA256 sur « timestamp.corps exact », vérifié en timing-safe, refusé hors fenêtre de 300 s, et persisté dans InboundEvent avec eventId unique avant tout traitement ; un doublon renvoie 200 sans effet ; une signature invalide renvoie 401 et une alerte SECURITY_ALEMENT plafonnée. _(source : audit-outil-bout-en-bout §2.3 · patron src/server/crm-sync/emit.ts et src/app/api/docuseal/webhook/route.ts)_ **→ ABSORBÉE par REQ-SEC-010** (annexe de dédoublonnage, fusion appliquée par GOV-001, 2026-09-03) : **le texte en vigueur est celui de REQ-SEC-010** ; l'identifiant est conservé — et non supprimé — pour que les tâches qui le citent continuent de résoudre. **→ voir REQ-SEC-010.**
  <br>_module 5 · étape 5 · phase -1 · tâches : `INT-T01b`, `SEC-06`_ · _source : critique de complétude (2026-09-03)_
- **REQ-QA-009** → **absorbée par REQ-ARG-003** — L'état des commissions est recalculable à partir de l'historique des InboundEvent ; le rejeu complet produit des soldes identiques (golden) ; toute permutation des événements d'un même dossier produit le même état final (property-based) ; un événement de schemaVersion inconnue est conservé en `held` et alerté, jamais perdu. _(source : audit-outil-bout-en-bout §2.3 « rejouables » + QA-F13)_ **→ ABSORBÉE par REQ-ARG-003** (annexe de dédoublonnage, fusion appliquée par GOV-001, 2026-09-03) : **le texte en vigueur est celui de REQ-ARG-003** ; l'identifiant est conservé — et non supprimé — pour que les tâches qui le citent continuent de résoudre. **→ voir REQ-ARG-003.**
  <br>_étape 10 · phase 0 · tâches : `QA-T27`, `SEC-06`, `T-ARG-022`_ · _source : critique de complétude (2026-09-03)_
- **REQ-QA-010** → **absorbée par REQ-SEC-009** — Pour chaque route de l'espace apporteur et chaque server action, un test paramétré appelle avec l'identité B une ressource de A et exige 404 ; une garde statique fait rougir la CI si une route ou action nouvelle n'est pas listée dans IDOR_CASES. _(source : tableaux-de-bord §6.1 · audit-outil-bout-en-bout §7 + QA-F09)_ **→ ABSORBÉE par REQ-SEC-009** (annexe de dédoublonnage, fusion appliquée par GOV-001, 2026-09-03) : **le texte en vigueur est celui de REQ-SEC-009** ; l'identifiant est conservé — et non supprimé — pour que les tâches qui le citent continuent de résoudre. **→ voir REQ-SEC-009.**
  <br>_module 8 · étape 7 · phase 0 · tâches : `SEC-05`_ · _source : critique de complétude (2026-09-03)_
- **REQ-QA-011** → **absorbée par REQ-SEC-008** — L'espace apporteur n'accède à la base que par un client Prisma scopé (`scopedPrisma(apporteurId)`) qui injecte apporteurId dans tout where des modèles cloisonnés ; une règle semgrep interdit prisma brut hors src/server/scope/** pour ces modèles. _(source : nouvelle (QA-F08, QA-F09))_ **→ ABSORBÉE par REQ-SEC-008** (annexe de dédoublonnage, fusion appliquée par GOV-001, 2026-09-03) : **le texte en vigueur est celui de REQ-SEC-008** ; l'identifiant est conservé — et non supprimé — pour que les tâches qui le citent continuent de résoudre. **→ voir REQ-SEC-008.**
  <br>_module 8 · étape 7 · phase 0 · tâches : `QA-T07`, `SEC-05`_ · _source : critique de complétude (2026-09-03)_
- **REQ-QA-012** → **absorbée par REQ-SEC-022** — Le message de collision et la réponse « déjà suivie » du module Vérifier ne contiennent ni identifiant, ni nom, ni date de dépôt, ni stade de l'autre apporteur ; un test snapshot du corps de réponse le prouve (seule la date de fin est autorisée). _(source : fonctionnement R3 · audit-outil-bout-en-bout §1.3)_ **→ ABSORBÉE par REQ-SEC-022** (annexe de dédoublonnage, fusion appliquée par GOV-001, 2026-09-03) : **le texte en vigueur est celui de REQ-SEC-022** ; l'identifiant est conservé — et non supprimé — pour que les tâches qui le citent continuent de résoudre. **→ voir REQ-SEC-022.**
  <br>_module 9 · étape 7 · phase 0 · tâches : `SEC-05`, `SEC-16`_ · _source : critique de complétude (2026-09-03)_
- **REQ-QA-013** — Une gate CI bloquante par PR exécute typecheck strict, ESLint (no-console error), Prettier, Vitest + couverture (REQ-QA-002), tests d'intégration testcontainers, semgrep (règles publiques + maison avec tests), pnpm audit --prod --audit-level=high, gitleaks, req:check, idor:check, lint de migration, size-limit par route.
  <br>_module 17 · phase -1 · tâches : `QA-T00`, `QA-T01`, `QA-T07`, `QA-T28`_ · _source : nouvelle (prompt)_
- **REQ-QA-014** — Chaque exigence REQ-*-nnn non différée est référencée par ≥ 1 test vert via l'annotation @req ; chaque @req pointe une REQ existante ; `pnpm req:check` rougit dans les deux sens d'orphelinat.
  <br>_phase 0 · tâches : `QA-T03`_ · _source : nouvelle (prompt : traçabilité REQ→test)_
- **REQ-QA-015** — Chaque PR est déployée sur un environnement de preview isolé (base éphémère seedée par un seed déterministe, jamais un dump prod), URL commentée sur la PR, détruit à la fermeture ; les E2E Playwright et ZAP authentifié s'exécutent contre cette preview.
  <br>_phase 0 · tâches : `QA-T06`_ · _source : nouvelle (QA-F07) · règle mémoire « jamais recopier prod → Preview »_
- **REQ-QA-016** — Les E2E de l'espace apporteur s'exécutent sur les projets Playwright mobile-safari (iPhone 14 Pro) et mobile-chrome (Pixel 7) uniquement ; ceux de la console sur chromium desktop ; un spec d'espace sans projet mobile fait rougir la CI.
  <br>_module 8 · étape 7 · phase 0 · tâches : `QA-T16`, `UX-P0-03`_ · _source : tableaux-de-bord §1 · audit-outil-bout-en-bout §12.3 + QA-F19_
- **REQ-QA-017** — Le parcours « déposer un contact » en E2E mobile aboutit en ≤ 8 interactions et ≤ 90 s de temps simulé, API entreprises mockée par fixtures enregistrées ; le parcours réussit aussi avec l'API coupée (repli saisie SIREN → état a_verifier_siren).
  <br>_module 13 · étape 10 · phase 1 · tâches : `QA-T16`_ · _source : fonctionnement §4.1-4.2 · audit-attribution P4 + QA-F16_
- **REQ-QA-018** — L'image Docker est construite sur GitHub Actions, poussée sur GHCR taguée latest + sha-<7> ; Coolify ne fait que pull ; un job Gate C bloquant démarre l'image avec Postgres+Redis éphémères et exige readyz 200 avec migrations_pending = 0 en ≤ 3 min.
  <br>_phase 0 · tâches : `QA-T05`_ · _source : AGENTS.md ADR 0026 · audit-outil-bout-en-bout §10 phase 0 + QA-F21_
- **REQ-QA-019** — L'entrypoint exécute `prisma migrate deploy` en mode bloquant : échec ⇒ exit ≠ 0 en < 60 s, l'ancien container reste servi ; SKIP_MIGRATE=1 n'est utilisé que par le runbook de rollback.
  <br>_phase 0 · tâches : `QA-T04`_ · _source : QA-F04_
- **REQ-QA-020** — /api/livez répond 200 dès que le process répond ; /api/readyz répond 503 si DB, Redis, validation d'env ou migrations pending sont KO, 200 sinon ; HEALTHCHECK Docker et Coolify pointent sur readyz ; les trois cas KO ont un test.
  <br>_phase 0 · tâches : `QA-T04`_ · _source : QA-F05 · code src/app/api/healthz/route.ts_
- **REQ-QA-021** — Les migrations respectent expand/contract : aucune PR ne contient DROP/RENAME/NOT NULL sans défaut sur une colonne lue par le code déployé ; Gate D applique migrate deploy sur base vierge ET sur un dump N-1 seedé, exige `prisma migrate diff` vide, et démarre l'image N-1 sur le schéma N.
  <br>_phase 0 · tâches : `QA-T11`_ · _source : QA-F12 · patron ci.yml gate-d-migration_
- **REQ-QA-022** — Un workflow rollback (workflow_dispatch sha) redéploie sha-<n-1> avec SKIP_MIGRATE=1 et vérifie readyz et `x-partners-build-sha` ; le runbook rollback porte l'identifiant du run de preview qui l'a exercé (date, SHA cible, résultat) avant le premier déploiement prod ; gate : absence de cet identifiant dans le runbook → rouge.
  <br>_phase 0 · tâches : `QA-T13`_ · _source : QA-F12 · règle mémoire « la vérité est dans x-axion-build-sha » ; reformulation d'annexe appliquée le 2026-09-03_
- **REQ-QA-023** — La base Partners est sauvegardée toutes les heures vers R2 (préfixe partners/) ; un drill mensuel restaure le dernier dump sur un Postgres éphémère, vérifie count(attributions) > 0 et migrate status propre, et alerte Telegram sur échec ; RPO ≤ 1 h, RTO ≤ 1 h.
  <br>_phase 0 · tâches : `QA-T12`_ · _source : QA-F11 · patron restore-drill-monthly.yml_
- **REQ-QA-024** — Tous les logs sont JSON structurés (pino) avec requestId, apporteurIdHash, eventId, jobName ; les champs nom/prénom/téléphone/email du contact rencontré, IBAN et jeton de lien de dépôt sont redactés ; un test prouve la redaction.
  <br>_module 9 · étape 4 · phase 0 · tâches : `QA-T08`_ · _source : audit-attribution P8 · QA-F17_
- **REQ-QA-025** — Les métriques métier (dépôts/j, SLA 48 h p50/p95/dépassés, taux de confirmation, InboundEvent par statut, lot mensuel montant + lignes bloquées, heartbeats) sont calculées à la lecture (jamais stockées en double) et exposées sur une route interne protégée par secret.
  <br>_module 9 · étape 7 · phase 1 · tâches : `QA-T19`_ · _source : audit-outil-bout-en-bout §9 · patron src/server/crm-sync/health.ts_
- **REQ-QA-026** — Chaque cron et la réception de webhook écrivent un Heartbeat(job, lastOkAt) ; un check nightly alerte si le relevé du 1er n'a pas tourné le 2 à 08:00 ou si aucun événement n'a été reçu sur 7 j alors que l'API 1 a été appelée ; une réconciliation hebdomadaire axionia↔Partners (compte et somme des Payment paid attribués du mois) alerte sur écart.
  <br>_module 15 · étape 9 · phase 1 · tâches : `INT-T08-P`, `QA-T19`, `QA-T27`_ · _source : QA-F18 · règle mémoire « une veille muette »_
- **REQ-QA-027** — Le domaine reçoit une Clock injectée ; les crons traitent « tout ce qui est dû à l'instant t » ; Releve(apporteurId, mois) et LotPaiement(mois) sont uniques ; un test prouve que le cron du 1er exécuté deux fois ne produit qu'un relevé et qu'un worker arrêté trois jours rattrape tout.
  <br>_module 15 · étape 11 · phase 0 · tâches : `CPL-T13`, `DM-13`, `T-ARG-015`_ · _source : tableaux-de-bord §2.7 · audit-outil-bout-en-bout §6.3 + QA-F15_
- **REQ-QA-028** — Le client de recherche-entreprises.api.gouv.fr est isolé, validé par schéma Zod contre des fixtures enregistrées et datées (≥ 20 cas), protégé par circuit-breaker + cache Redis 24 h + limite de débit ≤ 5 req/s ; un test de contrat nightly contre l'API réelle est bloquant nightly avec alerte.
  <br>_module 10 · étape 10 · phase 0 · tâches : `INT-T09`_ · _source : audit-attribution P4 · audit-anti-abus M5 + QA-F16_
- **REQ-QA-029** — Le fichier SEPA pain.001.001.03 généré pour un lot est validé en test contre le XSD ISO 20022 versionné dans le dépôt ; fixtures : 3 virements, IBAN étranger, caractères hors jeu SEPA translittérés, montant 0 refusé, référence de bout en bout unique par ligne.
  <br>_module 15 · étape 11 · phase 2 · tâches : `T-ARG-018`_ · _source : audit-outil-bout-en-bout §6.3 + QA-F25 · règle maison « valeur qu'un tiers doit accepter »_
- **REQ-QA-030** — `src/env.ts` valide strictement toutes les variables au démarrage (Zod) ; toute variable manquante empêche le démarrage (test : process exit ≠ 0) ; `docs/env.md` est généré depuis le schéma et une garde rougit s'il est périmé ; `AXIONIA_WEBHOOK_SECRET`, `DOCUSEAL_WEBHOOK_SECRET` et `AXIONIA_API_TOKEN` acceptent une clé courante et une clé précédente pendant 24 h (`kid` dans l'en-tête), avec un test qui prouve l'acceptation de l'ancienne clé avant l'échéance et son refus après.
  <br>_module 5 · étape 5 · phase 0 · tâches : `QA-T04`, `QA-T13`_ · _source : QA-F24 ; reformulation d'annexe appliquée le 2026-09-03_
- **REQ-QA-031** → **absorbée par REQ-UX-033** — First Load JS ≤ 75 KB gz par route de l'espace apporteur (budget absolu, size-limit bloquant) ; LHCI lab mobile sur les 6 routes clés : LCP ≤ 1 800 ms, CLS = 0, INP ≤ 100 ms ; axe-core WCAG 2.2 AA : 0 violation serious/critical. _(source : AGENTS.md budgets · tableaux-de-bord §6.2 · audit-outil-bout-en-bout §12.3)_ **→ ABSORBÉE par REQ-UX-033** (annexe de dédoublonnage, fusion appliquée par GOV-001, 2026-09-03) : **le texte en vigueur est celui de REQ-UX-033** ; l'identifiant est conservé — et non supprimé — pour que les tâches qui le citent continuent de résoudre. **→ voir REQ-UX-033.**
  <br>_module 8 · étape 7 · phase 0 · tâches : `QA-T16`, `QA-T20`, `QA-T20b`_ · _source : critique de complétude (2026-09-03)_
- **REQ-QA-032** — Aucun code de Partners ne contient la chaîne stub.invalid ni de Proxy Prisma court-circuitant ; `pnpm build` réussit sans DATABASE_URL (aucune page ne touche la base au build) ; une garde CI le prouve.
  <br>_phase 0 · tâches : `QA-T05`_ · _source : AGENTS.md ADR 0026 + QA-F20_
- **REQ-QA-033** → **absorbée par REQ-GOV-014** — Un seul producteur de déploiement à la fois : concurrency GitHub par environnement, fusion sur main en --ff-only, l'atterrissage vérifié (x-partners-build-sha == sha fusionné) avant la fusion suivante ; un script `deploy:verify` le mesure. _(source : règle maison « une fusion à la fois » · mémoire deploy-famine)_ **→ ABSORBÉE par REQ-GOV-014** (annexe de dédoublonnage, fusion appliquée par GOV-001, 2026-09-03) : **le texte en vigueur est celui de REQ-GOV-014** ; l'identifiant est conservé — et non supprimé — pour que les tâches qui le citent continuent de résoudre. **→ voir REQ-GOV-014.**
  <br>_phase 0 · tâches : `QA-T05`_ · _source : critique de complétude (2026-09-03)_
- **REQ-QA-034** — Cinq runbooks existent dans `docs/runbooks/` (rollback, migration échouée, webhook gave_up / secret désynchronisé, commission manquante, lot mensuel bloqué) ; chacun porte un bloc « Exécuté le : <date> · environnement : preview · SHA · résultat » ; gate : runbook sans bloc d'exécution daté → rouge ; le bloc est renouvelé à chaque modification du runbook.
  <br>_étape 10 · phase 0 · tâches : `QA-T13`, `QA-T25`_ · _source : nouvelle (prompt : runbooks) · audit-outil-bout-en-bout §6.3 ; reformulation d'annexe appliquée le 2026-09-03_

### gouvernance-agents

- **REQ-GOV-001** — Il existe un registre d'exigences unique (`docs/REQUIREMENTS.md` + `requirements.json`) où chaque exigence porte un identifiant `REQ-<DOMAINE>-nnn` unique, une source (document + section, ou « nouvelle »), un statut parmi {ouverte, planifiée, codée, testée, fusionnée, vérifiée} et un propriétaire (rôle). `pnpm gov:check` rougit sur id dupliqué, source vide ou statut hors liste.
  <br>_phase -1 · tâches : `GOV-001`_ · _source : nouvelle (dérivée des 7 docs : 5 séries de décisions sans registre)_
- **REQ-GOV-002** — Toute règle métier présente dans au moins deux documents a une entrée dans `docs/PRESEANCE.md` désignant la version qui prévaut et la REQ qui la porte ; les sections périmées portent un bandeau « remplacé par REQ-… ». Test : les sept couples connus (quota, collision, cycle de vie, barème, naissance de l'attribution, péremption, zéro arbitrage) ont une entrée.
  <br>_module 13 · étape 10 · phase -1 · tâches : `GOV-002`_ · _source : audit-bout-en-bout en-tête (« remplace en partie ») ; anti-abus C15_
- **REQ-GOV-003** — Tout identifiant de décision issu des documents est qualifié par un préfixe de document (PLAN-, REG-, SIREN-, TDB-, ABUS-, BEB-) et figure dans `docs/DECISIONS-INDEX.md`. Gate : un identifiant nu `\b[ABCDR]\d{1,2}\b` dans un titre/corps de PR, un ADR ou un commentaire de code → rouge. Un renvoi POINTÉ — un identifiant suivi d'un point et d'un CHIFFRE, comme le numéro d'une sous-section — n'est pas un identifiant nu : il résout, et il est cité comme tel. La garde trace donc la ligne sur ce qui SUIT le point, un chiffre le prolongeant et toute autre chose terminant la phrase ; sans cette distinction, elle rougissait sur un renvoi que personne n'avait écrit pour désigner une décision (GOV-025, mesuré le 2026-09-05).
  <br>_phase -1 · tâches : `GOV-003`, `GOV-005`, `GOV-025`, `GOV-028`, `GOV-037`_ · _source : plan §2/§5, anti-abus §7, audit-bout-en-bout §11 (collisions C/D)_
- **REQ-GOV-004** — Toute REQ ou tâche dont la source cite du code d'axionia porte une colonne « vérifié le » non vide (chemin, ligne, date, SHA) ; `pnpm gov:check` rougit si une source de type code a la colonne vide ; les affirmations invalidées (`Invoice`, `Refund`, `payerSiret`, « montant HT encaissé », « C3 codé ») figurent dans `docs/DECISIONS-INDEX.md` avec la mention FAUSSE et la réalité constatée, et un test vérifie la présence de ces cinq entrées.
  <br>_étape 10 · phase -1 · tâches : `GOV-004`_ · _source : nouvelle (vérification : plan §5, tableaux §2.3/2.5, audit-siren P10, audit-bout-en-bout §2.3) ; reformulation d'annexe appliquée le 2026-09-03_
- **REQ-GOV-005** → **absorbée par REQ-QA-014** — La matrice de traçabilité REQ → tâche → test → PR est DÉRIVÉE (jamais rédigée à la main) : les titres `it()` contiennent l'identifiant REQ, les corps de PR listent `Couvre: REQ-…`, `pnpm gov:trace` la génère et rougit si une REQ en statut ≥ « testée » n'a aucun test, si un test cite une REQ inconnue, ou si une tâche n'a aucune REQ. _(source : nouvelle ; patron axion-ops gate G4 (docs/ETAT.md))_ **→ ABSORBÉE par REQ-QA-014** (annexe de dédoublonnage, fusion appliquée par GOV-001, 2026-09-03) : **le texte en vigueur est celui de REQ-QA-014** ; l'identifiant est conservé — et non supprimé — pour que les tâches qui le citent continuent de résoudre. **→ voir REQ-QA-014.**
  <br>_phase -1 · tâches : `GOV-011`, `GOV-022`_ · _source : critique de complétude (2026-09-03)_
- **REQ-GOV-006** — Un seul fichier d'état vivant `docs/PLAN-STATE.md` est relu au début de chaque session d'agent et réécrit à la fin ; il contient : SHA de `main`, PR en vol et file de fusion ordonnée, tâche revendiquée par agent, décisions du jour, prochain pas, et un bloc « REPRENDRE EN 30 SECONDES ». Gate : sa date de mise à jour ≥ date de la dernière PR fusionnée, sinon rouge.
  <br>_phase -1 · tâches : `GOV-008`, `GOV-032`_ · _source : nouvelle ; patron `_AUDIT/ETAT-VIVANT-NUIT-2026-08-24.md` et `axion-ops/docs/ETAT.md`_
- **REQ-GOV-007** — Une tâche a au plus un `owner` à la fois, revendiqué par écriture dans PLAN-STATE via l'orchestrateur (seul écrivain) ; un agent ne travaille jamais sur une tâche non revendiquée. Gate : deux PR ouvertes citant la même tâche → rouge.
  <br>_phase -1 · tâches : `GOV-008`_ · _source : nouvelle_
- **REQ-GOV-008** — Toute décision d'architecture ou règle métier non couverte par une REQ sourcée fait l'objet d'un ADR dans l'unique dossier `docs/adr/`, numéro continu attribué par l'architecte, gabarit unique, index généré depuis le filesystem ; toute référence croisée est qualifiée par dépôt (`axionia/ADR-0014`, `ops/ADR-0050`, `partners/ADR-0003`). Gate : doublon de numéro, index ≠ `ls`, ou référence non qualifiée → rouge.
  <br>_module 9 · étape 7 · phase -1 · tâches : `GOV-009`_ · _source : nouvelle (dérive constatée : docs/adr 0034 ×2, 0036 ×2 ; docs/adrs/INDEX.md figé à 28)_
- **REQ-GOV-009** — Tout ADR en statut Accepted cite au moins une assertion de test (fichier + titre `it()`) qui verrait la décision mourir, ou porte la mention `hors-code` motivée. Gate : ADR Accepted sans assertion ni `hors-code` → rouge.
  <br>_phase -1 · tâches : `GOV-010`, `GOV-022`_ · _source : axion-ops/docs/ETAT.md (gate G4, ADR 0048)_
- **REQ-GOV-010** — `docs/CHARTE-AGENTS.md` définit neuf rôles à droit exclusif : orchestrateur (attribue, écrit PLAN-STATE), architecte (seul à accepter un ADR), gardien du spec (seul à modifier REQUIREMENTS/PRESEANCE/DECISIONS/GLOSSAIRE), leads de domaine (découpent, valident l'acceptation), développeurs (une tâche revendiquée à la fois), relecteurs adversariaux, vérificateurs de tests, release manager (seul à fusionner), documentaliste (LECONS.md). Gate : PR modifiant un fichier réservé sans le label du rôle → rouge.
  <br>_phase -1 · tâches : `GOV-007`, `GOV-023`_ · _source : nouvelle_
- **REQ-GOV-011** — Un agent ne relit jamais son propre code : chaque PR porte `Auteur:` et `Relecteur:` (identifiants d'agent) distincts, et toute PR touchant l'argent, l'attribution, l'authentification ou le cloisonnement porte en plus une revue adversariale documentée (scénario joué, résultat). Gate : relecteur = auteur, ou PR sur `commissions/**|attributions/**|auth/**` sans section « Attaque » → rouge.
  <br>_étape 10 · phase -1 · tâches : `GOV-007`, `GOV-021`_ · _source : nouvelle ; anti-abus §0 (scénario à rejouer)_
- **REQ-GOV-012** — Toute nouvelle garde (test, gate CI, lint) est livrée avec la preuve qu'elle a ROUGI avant le correctif : un bloc « ROUGE : <message d'échec verbatim> / VERT : <état> » dans le corps de la PR, ou un commit témoin. Gate : nouveau fichier `*.spec.ts` ou nouvelle étape de workflow sans bloc « ROUGE : » dans la PR → rouge.
  <br>_phase -1 · tâches : `GOV-007`_ · _source : règle maison « une garde ne vaut que si on l'a vue rougir » ; axion-ops ADR 0048_
- **REQ-GOV-013** — La définition de « terminé » est écrite dans le gabarit de PR et comporte huit cases : REQ listées ; tests nommés par REQ et vus rougir ; relecteur ≠ auteur ; ADR si décision ; glossaire/enum à jour ; mesure bundle avant/après si route UI ; PLAN-STATE mis à jour ; fusionnée et atterrissage vérifié. Le release manager refuse toute PR à moins de huit cases.
  <br>_phase -1 · tâches : `GOV-007`_ · _source : nouvelle_
- **REQ-GOV-014** — `docs/PROTOCOLE-FUSION.md` : une fusion à la fois sur `main`, historique linéaire (squash ou `--ff-only`), créneau réservé dans PLAN-STATE AVANT `update-branch`, `mergeStateStatus` lu et fusion exécutée dans le même appel, atterrissage vérifié par l'en-tête `x-partners-build-sha` avant la fusion suivante. Gate : aucun workflow ne pousse sur `main` (transposition de `aucun-workflow-ne-pousse-sur-main.spec.ts`) ; `main` protégée exige les gates A/B/D.
  <br>_phase -1 · tâches : `GOV-000`, `GOV-012`_ · _source : mémoire deploy-famine-cancel-in-progress ; la-file-de-fusion-se-reserve-avant-l-update-branch_
- **REQ-GOV-015** — `docs/DECISIONS.md` liste chaque décision non prise (≈ 60 issues des docs + celles des domaines) avec : id qualifié, question, HYPOTHÈSE PAR DÉFAUT retenue, propriétaire (**Will** par défaut, ou **expert-comptable** pour les seules questions comptables et fiscales — **il n'y a ni avocat ni DPO sur le projet, acté le 2026-09-03** : aucune question ne peut avoir « avocat » ou « DPO » pour décideur, et aucune phase n'est gatée par une relecture d'avocat), impact si renversée, test `HYP-<id>` qui échoue si l'hypothèse change. Gate : un marqueur `// HYP-` dans le code sans entrée correspondante → rouge ; une tâche dépendant d'une décision sans hypothèse → non attribuable.
  <br>_phase -1 · tâches : `GOV-005`, `GOV-022`, `GOV-027`, `JUR-T01b`_ · _source : plan §3.4, §4.3 ; audit-siren §8 ; tableaux §8 ; anti-abus §7 ; audit-bout-en-bout §11-12_
- **REQ-GOV-016** — `docs/GLOSSAIRE.md` fixe un terme canonique par concept (apporteur, dépôt, attribution, actif = premier dépôt CONFIRMÉ, dormant, **déclaration non confirmée** — jamais « strike », qui est un synonyme interdit —, palier, en_attente vs en_attente_encaissement…), ses synonymes interdits et l'enum Prisma qui le porte. Gate schéma : toute colonne `statut|status|etat|type|palier|priorite` de type `String` → rouge ; toute valeur d'enum absente du glossaire → rouge.
  <br>_module 9 · étape 10 · phase -1 · tâches : `GOV-006`_ · _source : règle maison « colonne de vocabulaire = enum » ; contradictions plan §1 / tableaux §4.1 / audit-bout-en-bout §5 étape 6_
- **REQ-GOV-017** — Le lexique interdit (« commercial », « commerciaux », « commerciale », « objectif », « quota de vente », « classement ») est absent de `prisma/**`, `messages/**`, `src/**/*.tsx`, des templates email et des ADR de Partners, hors liste d'exceptions justifiées ligne à ligne. Gate vue rougir sur un témoin avant activation.
  <br>_phase -1 · tâches : `GOV-013`_ · _source : plan §3.2 ; audit-bout-en-bout §3.2, §12.2, D15_
- **REQ-GOV-018** — `docs/CONVENTIONS.md` fixe : français pour docs, commentaires, messages et ADR ; identifiants de code en français (cohérence avec le glossaire et le schéma), anglais réservé aux API tierces ; montants en `Int` centimes ; dates en UTC ; Server Actions par défaut, API HTTP uniquement pour la frontière axionia ; Zod à toute entrée ; `// use-client:` justifié ; Prettier/ESLint versionnés. Gate : lint et format bloquants en CI dès le socle.
  <br>_phase -1 · tâches : `GOV-014`_ · _source : nouvelle ; conventions constatées dans axionia (enums en français, `use-client:check`)_
- **REQ-GOV-019** → **absorbée par REQ-DM-014** — La grille de commissions a une seule source : `axionia/src/content/pricing.ts` → export `commissions.v<N>.json` + hash SHA-256 généré par script ; Partners consomme le fichier versionné, le contrat référence la version, l'écran Paramètres l'affiche sans le modifier. Gate des deux côtés : hash embarqué = hash recalculé, sinon rouge. _(source : plan §2 D3 (SSOT pricing.ts) ; audit-bout-en-bout §6.2, §9.2 (snapshot, Paramètres) — contradiction levée)_ **→ ABSORBÉE par REQ-DM-014** (annexe de dédoublonnage, fusion appliquée par GOV-001, 2026-09-03) : **le texte en vigueur est celui de REQ-DM-014** ; l'identifiant est conservé — et non supprimé — pour que les tâches qui le citent continuent de résoudre. **→ voir REQ-DM-014.**
  <br>_étape 10 · phase 0 · tâches : `DM-03-A`_ · _source : critique de complétude (2026-09-03)_
- **REQ-GOV-020** → **absorbée par REQ-QA-007** — Le contrat d'intégration axionia ↔ Partners (5 événements, 2 API) est un schéma unique (Zod + JSON Schema) versionné, copié à hash identique dans les deux dépôts, avec des fixtures GÉNÉRÉES depuis les producteurs réels (`FactureFormation`, `Payment`, `Client`) d'une base de développement, jamais écrites à la main ; les deux dépôts testent sur les mêmes fixtures. Gate : hash du schéma divergent, ou fixture sans provenance → rouge. _(source : audit-bout-en-bout §2.3 ; règle maison « fixtures depuis le producteur réel » ; vérification schema.prisma (pas d'`Invoice`/`Refund`))_ **→ ABSORBÉE par REQ-QA-007** (annexe de dédoublonnage, fusion appliquée par GOV-001, 2026-09-03) : **le texte en vigueur est celui de REQ-QA-007** ; l'identifiant est conservé — et non supprimé — pour que les tâches qui le citent continuent de résoudre. **→ voir REQ-QA-007.**
  <br>_module 9 · étape 7 · phase -1 · tâches : `INT-T01a`_ · _source : critique de complétude (2026-09-03)_
- **REQ-GOV-021** — Chaque tâche du backlog (`tasks.json`) a : id, phase, owner unique ou vide, acceptance vérifiable, tests nommés, dépendances (ids), REQ couvertes (≥ 1), estimation ≤ 1 jour, étiquette `repo:partners|repo:axionia`. Gate : `estimateDays > 1`, dépendances cycliques, REQ inconnue ou tâche sans REQ → rouge.
  <br>_phase -1 · tâches : `GOV-017a`, `GOV-017b`, `GOV-024`, `GOV-027`, `GOV-037`_ · _source : nouvelle ; audit-bout-en-bout §10 (chantiers de 8 à 14 j)_
- **REQ-GOV-022** — Toute valeur produite pour un tiers (API recherche-entreprises, DocuSeal, SEPA pain.001, DAS2, URSSAF vigilance, IBAN/BIC, TIIME) est confrontée à `docs/tiers/<nom>.md` (URL officielle, date de lecture, extrait cité, exemple officiel, quotas, comportement en panne) ; la tâche qui l'utilise dépend de la fiche. Gate : fixture d'un format tiers sans `Source:` vers la fiche → rouge.
  <br>_module 15 · étape 11 · phase -1 · tâches : `GOV-015`_ · _source : règle maison « valeur qu'un tiers doit accepter » ; audit-bout-en-bout §3.3, §6.3 ; audit-siren P4_
- **REQ-GOV-023** — Toute PR fusionnée est précédée d'une entrée de journal (fait / reste / appris) dans `docs/PLAN-STATE.md` qui cite son numéro ; gate : PR fusionnée sans entrée la citant → rouge. `docs/LECONS.md` porte une date de dernière consolidation ; gate nightly : date > 7 jours alors que des entrées « appris » non consolidées existent → rouge. (La lecture en début de session ne se garde pas ; elle est rappelée dans CHARTE-AGENTS.)
  <br>_phase -1 · tâches : `GOV-008`, `GOV-018`_ · _source : nouvelle ; patron mémoire de session d'axionia ; reformulation d'annexe appliquée le 2026-09-03_
- **REQ-GOV-024** — Les règles maison (dériver jamais recopier ; garde vue rougir ; fixtures du producteur réel ; enum pour le vocabulaire ; masquage qui échoue ouvert et droit par rôle défaut refus ; index unique partiel ; appelants avant extraction ; tiers confrontés ; une fusion à la fois) vivent dans `docs/REGLES-MAISON.md` du dépôt Partners, référencées par numéro dans les ADR et le gabarit de PR.
  <br>_module 9 · étape 7 · phase -1 · tâches : `GOV-018`, `GOV-026`_ · _source : prompt (règles maison) ; mémoire index-methode-et-gardes_
- **REQ-GOV-025** — Les tâches côté axionia (émetteur de webhooks avec outbox et rejeu, API `GET /attributions` au devis, export de grille, SIREN client obligatoire si attribution active) sont étiquetées `repo:axionia`, suivent le protocole de fusion d'axionia (un producteur, annonce sur le canal des sessions), et ne sont attribuées qu'à un agent nommé « lead intégration ». **Porteurs : `GOV-017` et `GOV-007`** (CODEOWNERS / poste A08) — c'est l'exigence qui fonde le champ `repo` des tâches, absent de 155 d'entre elles, et aucune tâche ne la citait (corrigé le 2026-09-03).
  <br>_étape 9 · phase -1 · tâches : `GOV-017a`, `GOV-017b`_ · _source : audit-bout-en-bout §2.3 ; vérification : patrons webhook axionia entrants seulement_
- **REQ-GOV-026** — L'état d'avancement d'un chantier utilise la légende unique {spécifié, codé, testé, revu, fusionné, déployé, vérifié en prod} ; tout état ≥ codé porte une preuve (chemin de fichier ou SHA) ; gate : entrée de `tasks.json` ou de PLAN-STATE en état ≥ codé sans preuve → rouge. L'inventaire initial (C1 codé dans axionia à transposer, C5 codé, C2/C3/C4/C6/C7/C8 non codés) est consigné dans PLAN-STATE, pas dans une exigence.
  <br>_phase -1 · tâches : `GOV-001`, `GOV-020`_ · _source : anti-abus en-tête ; audit-bout-en-bout §4 ; vérification code ; reformulation d'annexe appliquée le 2026-09-03_
- **REQ-GOV-027** — Le périmètre est gelé par phase : aucune PR étiquetée phase N+1 n'est fusionnée avant la clôture de phase N consignée dans PLAN-STATE (phase 1 avant tout recrutement, phase 2 avant le premier encaissement attribué). Gate : label de phase de la PR > phase courante de PLAN-STATE → rouge.
  <br>_module 1 · étape 10 · phase -1 · tâches : `CPL-T23`, `GOV-017a`, `GOV-017b`, `GOV-022`_ · _source : audit-bout-en-bout §10, D13_
- **REQ-GOV-028** — Les budgets de performance de Partners sont posés dans le dépôt (`size-limit` ≤ 75 KB gz par route de l'espace apporteur ; LHCI lab mobile LCP ≤ 1 800 ms, CLS = 0, INP ≤ 100 ms) et **bloquants dès la première route livrée** ; aucun cliquet n'est posé sous la mesure courante ; une garde vérifie que chaque route de l'espace a une entrée size-limit (route sans budget → rouge).
  <br>_module 8 · étape 7 · phase -1 · tâches : `GOV-019`_ · _source : tableaux §6.2 ; AGENTS.md ; reformulation d'annexe appliquée le 2026-09-03_
- **REQ-GOV-029** — Chaque garde d'axionia candidate à la transposition (anti-siren, use-client, use-server exports, zod, isolation, surface Server Actions, aucun-workflow-ne-pousse-sur-main) fait l'objet d'une décision transposer / adapter / écarter avec motif, et toute garde retenue est vue rougir sur un témoin Partners avant activation.
  <br>_phase -1 · tâches : `GOV-014`_ · _source : nouvelle ; vérification `scripts/check-anti-siren.sh` et `tests/unit/ci/*`_
- **REQ-GOV-030** — L'exception unique au « zéro arbitrage » (rattachement manuel de groupe, motivé, tracé, journalisé) est portée par **REQ-DM-034** ; une garde grep rougit si l'expression « zéro arbitrage » apparaît dans `docs/`, `prisma/` ou `src/` sans la référence `REQ-DM-034` sur la même ligne ou la ligne suivante.
  <br>_phase -1 · tâches : `GOV-002`_ · _source : anti-abus F15 ; contrat art. 3.6 ; fonctionnement R3, §7.1, §10 ; reformulation d'annexe appliquée le 2026-09-03_
- **REQ-GOV-032** — Toute vue générée du dépôt (`docs/TASKS.md`, `docs/REQUIREMENTS.md`, `docs/TRACABILITE.md`, `docs/paths-proposes.json`, `docs/PLAN-STATE.md`) est produite par un générateur nommé et possède un mode de VÉRIFICATION qui n'écrit rien et sort 1 quand le fichier commité diffère d'un seul octet de ce que sa source produirait ; ce mode est appelé par le job de Gate A. Le message d'échec nomme l'écart en unités du domaine — nombre de tâches livrées, nombre d'exigences — et non « les deux fichiers diffèrent ». Gate : une vue périmée d'une seule ligne → rouge ; le dépôt à jour → vert (contre-témoin exigé, RM-02).
  <br>_phase -1 · tâches : `GOV-024`, `GOV-035`_ · _source : nouvelle (relectures de la PR 30, 2026-09-04) ; docs/PRESEANCE.md §2 lignes 1-2 et §5 point 5_
- **REQ-GOV-033** — Tout identifiant que le dépôt s'attribue à lui-même et qui doit rester unique sur sa vie entière — identifiant de lot au premier chef — se dérive d'au moins une source SUIVIE par git. Une séquence tirée d'un répertoire ignoré (`.gitignore`) repart de zéro dans tout arbre neuf, worktree, clone ou machine de CI, et réattribue un identifiant déjà porté ; l'écriture qui suit fusionne alors deux ensembles distincts sous un même nom, sans qu'aucune garde ne le voie, le champ porteur étant une chaîne libre. Gate : dans un arbre où le répertoire de travail est absent, le prochain identifiant proposé pour chaque phase n'est porté par aucune entrée de `docs/tasks.json` → vert ; le même calcul privé de sa source suivie propose au contraire un identifiant DÉJÀ porté → c'est le témoin, et il se joue en confrontant le calcul en vigueur à la fixture du calcul d'avant, conservée dans le module (RM-02). Deux sources vides ne sont PAS ce témoin : elles rendent légitimement le premier identifiant, et c'est un contre-témoin.
  <br>_phase -1 · tâches : `GOV-029`_ · _source : GOV-029, mesuré le 2026-09-05 dans un worktree neuf : « Lot L-1-01 : 7 tache(s) » pour un identifiant déjà porté par sept tâches fusionnées._

### complétude (REQ-CPL)

- **REQ-CPL-001** — L'entité contractante et payeuse est nommée ; contrat, mandat d'autofacturation et fichier pain.001 portent le même SIREN et le même IBAN débiteur (test de cohérence des trois fixtures).
  <br>_module 16 · étape 11 · phase -1 · tâches : `CPL-T01`, `T-ARG-018`_ · _source : critique de complétude (2026-09-03)_
- **REQ-CPL-002** — La banque réceptrice du pain.001 est connue (version, BIC, jeu de caractères, espace de test, format CSV de relevé) OU la saisie manuelle avec EndToEndId est actée.
  <br>_module 15 · étape 11 · phase -1 · tâches : `CPL-T01`, `GOV-015`, `T-ARG-018`_ · _source : critique de complétude (2026-09-03)_
- **REQ-CPL-003** — Le modèle des têtes de réseau est tranché : V1 mono-utilisateur, ou `Structure` + `Utilisateur` (dépôt d'un utilisateur secondaire attribué à la structure, quota et déclarations non confirmées comptés par structure).
  <br>_module 9 · étape 7 · phase -1 · tâches : `CPL-T01`_ · _source : critique de complétude (2026-09-03)_
- **REQ-CPL-004** — Résidence fiscale française obligatoire en V1 : le KYC refuse (fermé) un SIRET non français ; retenue à la source art. 182 B hors périmètre.
  <br>_module 4 · étape 4 · phase -1 · tâches : `CPL-T01`, `DM-11`_ · _source : critique de complétude (2026-09-03)_
- **REQ-CPL-005** — `Apporteur.IdentitesFacturation[]` datées (SIREN, régime TVA, IBAN, validFrom/To) ; un relevé chevauchant deux identités produit deux autofactures.
  <br>_module 4 · étape 11 · phase 0 · tâches : `DM-06`, `DM-11`_ · _source : critique de complétude (2026-09-03)_
- **REQ-CPL-006** — Étape 3 outillée : `DecisionCandidature` (retenu | vivier | refusé, motif, auteur, date), présence webinaire, seuil initial ; aucun contrat ne part sans `retenu`.
  <br>_module 1 · étape 2 · phase 1 · tâches : `CPL-T06`_ · _source : critique de complétude (2026-09-03)_
- **REQ-CPL-007** — Tout paramètre présent dans le contrat est snapshoté par version de contrat ; la console ne modifie que les contrats futurs ; changement de grille = nouvelle version = campagne de re-signature.
  <br>_module 5 · étape 5 · phase 2 · tâches : `INT-T23`, `UX-P2-05`_ · _source : critique de complétude (2026-09-03)_
- **REQ-CPL-008** — Le registre d'opposition d'axionia (`EmailOpposition`, opposition RNE) est consulté au dépôt ; entreprise/contact opposé → dépôt refusé fermé, motif non révélé.
  <br>_module 9 · étape 7 · phase 1 · tâches : `SEC-12`_ · _source : critique de complétude (2026-09-03)_
- **REQ-CPL-009** — AIPD réalisée et signée avant le premier dépôt réel (profilage d'anomalie, score, données de tiers, données financières).
  <br>_module 9 · étape 7 · phase 0 · tâches : `JUR-T04`_ · _source : critique de complétude (2026-09-03)_
- **REQ-CPL-010** — `LigneAjustement` : montant signé, motif obligatoire, auteur, **référence de la ligne corrigée (FK non nulle)**, double approbation au-delà d'un seuil, journalisée, comptée en DAS2 ; apparaît sur le relevé avec son propre libellé. 🔴 **Un montant signé est un débit unilatéral, et aucune clause ne l'autorisait** (M-13) : l'ajustement est désormais borné par l'article 5.7 du contrat — il **corrige une erreur matérielle de calcul**, dans un sens comme dans l'autre, **dans le délai de l'article 5.5** (`FORCLUSION_CONTESTATION_MOIS` = 12 mois à compter de la mise à disposition du relevé qui aurait dû porter la commission), avec l'indication de son motif et de la ligne corrigée. **Aucun autre ajustement ne peut réduire un relevé** : hors erreur matérielle et hors délai, la seule voie de réduction est la `reprise` de REQ-DM-019, adossée à une annulation d'encaissement réelle. Tests : ajustement négatif sans `ligneCorrigeeId` → refus ; ajustement au-delà de 12 mois → refus (vu rougir) ; ajustement positif hors erreur matérielle → refus ; le libellé apparaît distinctement sur le relevé.
  <br>_module 16 · étape 10 · phase 2 · tâches : `CPL-T12`, `T-ARG-010`_ · _source : critique de complétude (2026-09-03)_
- **REQ-CPL-011** — Un mois « à blanc » (gel du 1er en mode ombre, aucun SEPA, écart = 0 centime vs calcul manuel sur ≥ 5 apporteurs réels) précède l'armement de l'export ; `SEPA_EXPORT_ENABLED` fermé par défaut.
  <br>_module 15 · étape 11 · phase 2 · tâches : `CPL-T11`, `CPL-T23`, `T-ARG-015`_ · _source : critique de complétude (2026-09-03)_
- **REQ-CPL-012** — État `contestee` sur une ligne de commission et sur une attribution invalidée, délai de réponse (15 j), sortie `maintenue | ajustee` (→ LigneAjustement) ; clause compétence/loi au contrat. 🔴 **Ce mécanisme n'avait AUCUNE base contractuelle** (M-13) ; il en a une désormais, et le produit s'aligne sur **deux délais distincts** que le contrat confondait (trente jours à un endroit, douze mois à un autre, sur la même matière) : (a) **la FORME et les mentions de la facture** se contestent dans les **trente jours** de l'**envoi du courrier électronique signalant sa mise à disposition** (`CONTESTATION_FACTURE_JOURS`, art. 5.2 et annexe 2.4 — jamais depuis un dépôt dans l'espace, REQ-UX-016) ; à défaut elle est réputée acceptée, **sauf erreur matérielle ou preuve contraire** — sans cette réserve, la présomption serait irréfragable au profit du rédacteur, ce que l'art. 1356 al. 2 C. civ. prohibe ; (b) **l'existence, l'assiette ou le CALCUL de la commission** se contestent dans les **douze mois** de la mise à disposition du relevé qui aurait dû la porter (`FORCLUSION_CONTESTATION_MOIS`, art. 5.5) — ce délai **organise la contestation des relevés et n'abrège pas la prescription de l'action en paiement** ; toute référence à l'article 2254 C. civ. est retirée (arbitrage C-2). La Société **répond de façon motivée dans les quinze jours** (`REPONSE_CONTESTATION_JOURS`, art. 5.6) ; l'ajustement figure sur le relevé suivant sous son propre libellé (REQ-CPL-010). Tests : contestation de forme à J+31 → forclose sur la forme, **recevable sur le calcul** (vu rougir sur l'ancien délai unique) ; contestation de calcul à M+13 → forclose ; absence de réponse à J+16 → alerte console.
  <br>_module 17 · étape 5 · phase 0 · tâches : `CPL-T12`, `JUR-T01`_ · _source : critique de complétude (2026-09-03)_
- **REQ-CPL-013** — Module `temps` : horloge injectable, fuseau Europe/Paris unique pour le métier (stockage UTC), calendrier fériés FR versionné, tests aux changements d'heure et au 29 février.
  <br>_phase 0 · tâches : `CPL-T13`_ · _source : critique de complétude (2026-09-03)_
- **REQ-CPL-014** — Événement `client.fusionne {survivorId, absorbedId}` + re-résolution ; commission après fusion résolue vers la même attribution.
  <br>_étape 10 · phase 2 · tâches : `CPL-T14-A`, `CPL-T14-P`_ · _source : critique de complétude (2026-09-03)_
- **REQ-CPL-015** — `source`, `utm`, `campagneId` obligatoires dans le transfert candidat→apporteur ; `CampagneRecrutement {canal, coût, période}` ; € / actif calculable.
  <br>_module 1 · étape 1 · phase -1 · tâches : `CPL-T15`, `INT-T01b`, `INT-T22`_ · _source : critique de complétude (2026-09-03)_
- **REQ-CPL-016** — Migration idempotente des `Submission` reçues depuis le 2026-08-13 (ou re-candidature actée) avec parité de score.
  <br>_module 1 · étape 2 · phase 1 · tâches : `INT-T21-P`_ · _source : critique de complétude (2026-09-03)_
- **REQ-CPL-017** — Domaine servi, DNS Cloudflare, WAF, portée du cookie/param `?p=` décidés en phase 0 ; le lien de dépôt J0 pointe sur le domaine final sans redirection.
  <br>_module 6 · étape 6 · phase -1 · tâches : `CPL-T01`_ · _source : critique de complétude (2026-09-03)_
- **REQ-CPL-018** — ADR « mono-tenant en V1, aucune colonne tenant ».
  <br>_phase -1 · tâches : `CPL-T01`, `GOV-009`_ · _source : critique de complétude (2026-09-03)_
- **REQ-CPL-019** — Changement d'e-mail = confirmation sur l'ancienne adresse + délai + révocation des sessions ; sans confirmation → refusé.
  <br>_phase 1 · tâches : `UX-P1-09`_ · _source : critique de complétude (2026-09-03)_
- **REQ-CPL-020** — `Apporteur.isTest` exclu de tous les agrégats d'argent, du lot, de la DAS2 et du funnel ; une ligne `isTest` n'apparaît jamais dans un pain.001.
  <br>_module 15 · étape 11 · phase 0 · tâches : `DM-06`, `T-ARG-015`_ · _source : critique de complétude (2026-09-03)_
- **REQ-CPL-021** — Hors production, `NOTIFY_SINK=true` obligatoire (refus au boot sinon) ; allowlist de destinataires ; aucune migration prod par un agent ; matrice d'autonomie des agents écrite.
  <br>_phase -1 · tâches : `GOV-000`, `QA-T04`_ · _source : critique de complétude (2026-09-03)_
- **REQ-CPL-022** — Job CI « red-first » : les tests nouveaux d'une PR exécutés contre `main` doivent échouer (ou porter `@no-red-first` justifié).
  <br>_phase 0 · tâches : `CPL-T22`_ · _source : critique de complétude (2026-09-03)_
- **REQ-CPL-023** — Chaque affirmation des **documents de présentation** / FAQ / argumentaires porte une source ; gate lexicale OPCO/CPF/Qualiopi/financement sur les ressources Partners ; relecture Will avant publication. ⚠️ **Le terme canonique est « documents de présentation »** : « kit de vente » est banni (REQ-JUR-041, A-8) — le contrat ne peut pas nommer « vente » ce que l'art. 1.2 dit n'être pas une vente —, et ces documents sont **transmissibles en l'état, sans modification**, à seule fin d'identifier la Société et de communiquer ses coordonnées.
  <br>_module 7 · étape 6 · phase 3 · tâches : `UX-P3-02`_ · _source : critique de complétude (2026-09-03)_
- **REQ-CPL-024** — Verrou optimiste (`version`) sur la fiche de qualification ; second enregistrement concurrent rejeté avec l'état courant.
  <br>_module 12 · étape 8 · phase 1 · tâches : `DM-09`, `UX-P1-06`_ · _source : critique de complétude (2026-09-03)_
- **REQ-CPL-025** — État terminal `decede` + clause + procédure manuelle (commissions acquises dues à la succession, attributions, accès), versées sur justification de la qualité et de coordonnées bancaires au nom des ayants droit ou du mandataire, **l'exigence de SIREN étant écartée pour ce seul cas** (art. 12.5). 🔴 **Une procédure collective ouverte à l'égard de l'apporteur est SANS EFFET sur le contrat** (M-19) : la clause qui prévoyait sa fin de plein droit était **réputée non écrite** (art. L.641-11-1, III, 1° C. com. ; même règle à L.622-13, I) — en pratique, la Société aurait cru le contrat éteint, cessé les relevés, et se serait vu opposer par le liquidateur un contrat toujours en cours et des commissions à payer avec intérêts. Le produit **ne connaît donc aucun état ni aucune transition déclenchée par une procédure collective** : la seule fin de plein droit est le **décès**, la cessation d'activité ou la radiation de l'immatriculation ; le sort du contrat en procédure collective est réglé par les articles L.622-13 et L.641-11-1 C. com. Tests : aucune valeur d'enum ni aucun chemin de code ne nomme « liquidation », « redressement » ou « procédure collective » comme cause de fin → **gate rouge** ; radiation d'immatriculation → fin de plein droit, commissions acquises versées aux ayants droit sans SIREN.
  <br>_module 15 · étape 5 · phase 3 · tâches : `T-ARG-033`_ · _source : critique de complétude (2026-09-03)_
- **REQ-CPL-026** — Le SLA affiché à l'apporteur est paramétré par la capacité réelle (jours ouvrés × qualifieurs disponibles, calendrier d'absence) ; le seuil de vérification prioritaire (D3) se dérive de la capacité restante. ⚠️ **Le « nous prenons contact dans deux jours ouvrés » n'est plus un engagement ferme au contrat** (art. 3.2 réécrit : « la Société **s'efforce de** prendre contact ») — `MANQUES-ET-RISQUES` §F.2 n° 4 le classait parmi les cinq risques d'échec les plus probables. Le SLA reste un objectif **interne** de console (REQ-UX-022) ; sa seule conséquence externe est la confirmation tacite à trente jours (REQ-DM-042).
  <br>_module 12 · étape 8 · phase 0 · tâches : `CPL-T13`_ · _source : critique de complétude (2026-09-03)_
- **REQ-CPL-029** — 🔴 **Les personnes agissant pour le compte de l'apporteur sont DÉCLARÉES, sans compte, sans jeton et sans agrégat.** L'ancien article 2.6 interdisait de « se substituer un tiers » et de « sous-traiter » sans accord écrit, et exigeait que l'apporteur ait « **personnellement** rencontré » un représentant : une obligation d'exécution personnelle, marqueur constant du contrat de travail (Cass. soc. 28 nov. 2018 *Take Eat Easy* ; 4 mars 2020 *Uber*), et un pouvoir d'autorisation sur l'organisation interne de l'entreprise de l'apporteur — à la lettre, une SARL apporteur employant trois commerciaux ne pouvait déclarer aucune des entreprises qu'ils rencontraient (A-7). L'article 2.6 réécrit conserve l'effet utile sous la forme d'une **condition d'un droit** : seules ouvrent droit à attribution les déclarations portant sur des entreprises dont l'apporteur, **ou une personne agissant pour son compte et déclarée à la Société**, a effectivement rencontré ou joint un représentant. Côté produit, et **sans revenir sur la décision W4** (un apporteur = une personne, REQ-CPL-003) : `PersonneDeclaree {apporteurId, nom, qualite, declareeAt, retireeAt}` — une **simple liste déclarative** attachée à l'apporteur, éditable par lui dans Mon profil, **sans compte, sans session, sans jeton de dépôt, sans code de parrainage et sans DTO propre** ; le champ « personne ayant rencontré » du dépôt propose cette liste, l'apporteur restant seul titulaire de l'attribution et seul responsable envers la Société. La liste **n'entre dans aucun agrégat, aucun score, aucun classement et aucun compteur** (REQ-JUR-034). Tests : dépôt citant une personne déclarée → attribution au nom de l'apporteur, jamais d'elle ; création d'une session ou d'un jeton pour une personne déclarée → **impossible par construction** (vu rougir) ; nombre de personnes déclarées absent de tout DTO exposé.
  <br>_module 9 · étape 7 · phase 1 · tâches : `UX-P1-15`_ · _source : contrat art. 2.6 ; REQ-CPL-003, décision W4 ; synthèse du 2026-09-03, A-7_

### Annexe — passe de dédoublonnage (à appliquer par le Gardien du spec, GOV-001)

- **REQ-INT-032** — Payloads manquants du contrat : `candidature.recue {candidatureId, reponsesJson, scoreInitial, scorePartsJson, scoreBaremeVersion, sourceCanal, utm, campagneId, parrainCodeCapture}` ; `facture.annulee {factureId, motif}` ; `financement.mis_a_jour {factureId, payers[], echeanceFinanceurAt}`. _(source : juges — complétude ; sans `candidature.recue`, aucun apporteur n'existe jamais dans Partners)_ **PLUS** : `paiement.rembourse {paymentId, montantHtCents, motif}` où `motif` est un enum {remboursement, avoir, litige_tranche, rejet_prelevement, virement_rappele, autre} — l'événement couvre **toute annulation d'encaissement**, pas seulement un remboursement volontaire ; axionia doit donc l'émettre aussi sur un rejet bancaire ou un litige tranché.
  <br>_module 1 · étape 1 · phase -1 · tâches : `DM-15`, `INT-T01b`, `INT-T05`, `INT-T22`_ · _source : critique de complétude (2026-09-03)_
- **REQ-CPL-027** — `actif` (premier dépôt confirmé) et `dormant` (> 60 j sans dépôt, paramètre SSOT) sont **dérivés** par fonction pure, jamais stockés, et restent des indicateurs de **console** : **aucun message n'est envoyé à un apporteur en raison de son inactivité** (REQ-JUR-033, qui remplace toute relance de dormance par la lettre du réseau, identique pour tous, à intervalle fixe, dont le job d'envoi ne reçoit aucun filtre sur la date du dernier dépôt). La « relance de dormance » est retirée du produit le 2026-09-03.
  <br>_module 18 · étape 7 · phase 0 · tâches : `DM-06`, `JUR-T24`, `UX-P1-12`, `UX-P3-06`_ · _source : juges — complétude ; REQ-JUR-033_
- **REQ-CPL-028** — Avenant : publier une nouvelle version de gabarit ou de grille crée une soumission DocuSeal pour chaque apporteur `signe` ; l'ancien contrat passe `signe → remplace` **à la signature du nouveau, et à elle seule**. 🔴 **Le refus ou l'absence de signature ne bloque RIEN** : l'apporteur continue de déclarer et d'être commissionné sous la version qu'il a signée (contrat art. 13.2 et 13.3), sans limite de durée et sans relance automatique. Il n'existe aucun état `contrat_a_resigner`, aucune issue de dépôt de ce nom, aucun délai de grâce. Si la Société veut mettre fin aux conditions antérieures, elle résilie selon l'art. 11.1. Les lignes de commission gardent le `grilleVersionId` sous lequel elles sont nées. 🔴 **La gate `GATE-JUR-SIGNATURE-AVANT-DEPOT` doit être corrigée avec cette exigence** (P-1) : `GATES.md` l. 103 arme encore, **comme condition de fusion**, « contrat `a_resigner` → refus à J+30 » — c'est-à-dire exactement le mécanisme que l'article 13.3 interdit et que cette exigence a supprimé. Nouvelle colonne : « dépôt refusé tant que le contrat est `envoye` ou absent ; cases d'acceptation distinctes ; **un contrat non re-signé après publication d'un avenant n'entraîne aucun refus de dépôt** », avec la **preuve rouge inverse** : « dépôt refusé au motif d'un avenant non signé → rouge ». Une CI qui garde le contraire de la spécification est pire qu'aucune gate. _(corrigé le 2026-09-03 : la version antérieure refusait les dépôts à J+30, ce qui contraignait à accepter une grille possiblement moins favorable pour continuer d'exercer — modification unilatérale, déséquilibre significatif art. 1171 C. civ. et L.442-1 C. com., et apparence de sanction)_
  <br>_module 5 · étape 5 · phase 1 · tâches : `INT-T23`, `INT-T24`_ · _source : critique de complétude (2026-09-03)_
- **REQ-UX-036** — La fiche apporteur de la console porte 5 blocs : identité et KYC · activité et dormance · rendement (contacts → rdv → devis → signés, délais, CA, marge nette après commission) · conformité (pièces, échéances, cumul DAS2) · journal immuable. 🔴 **Elle n'expose AUCUN compteur de mises en demeure ni de suspensions** (M-17, REQ-JUR-031) : le journal immuable les rend lisibles une par une, avec leur date et leur motif — c'est le contrat qui l'exige (art. 11.2 : « aucune décision ne peut être fondée, en tout ou partie, sur le nombre de mises en demeure ou de suspensions antérieures ») —, mais **aucun total, aucun badge, aucun tri par nombre, aucun filtre « apporteurs les plus suspendus »** n'existe. `dormant` reste un indicateur de **console** et ne sort jamais vers l'apporteur (REQ-CPL-027, REQ-JUR-042). Tests : test d'absence sur le DTO de la fiche (aucune clé de comptage d'événements disciplinaires), **vu rougir** sur un témoin portant `nbSuspensions`.
  <br>_module 18 · phase 1 · tâches : `UX-P1-12`_ · _source : juges ; audit-outil §9.1 ; contrat art. 11.2 ; synthèse du 2026-09-03, M-17_
- **REQ-UX-039** — 🔴 **Déclaration de lien d'intérêt au dépôt — facultative, jamais un refus.** L'article 8.4 nouveau interdit à l'apporteur de percevoir de l'entreprise qu'il déclare une rémunération au titre de la mise en relation, et l'oblige à **informer la Société** de toute relation d'affaires ou d'intérêt le liant à une entreprise qu'il déclare (A-9) — le conflit d'intérêts le plus banal de l'apport d'affaires, qu'aucune clause ne visait. Le formulaire de dépôt porte une **case facultative** « j'ai une relation d'affaires ou d'intérêt avec cette entreprise » et un champ libre court, tous deux **sans effet sur l'issue du dépôt** : jamais un refus, jamais un signal d'anomalie, jamais un compteur, jamais une entrée dans le score (REQ-SEC-017). L'information est visible dans la fiche prospect de la console et journalisée. Tests : case cochée → dépôt **enregistré normalement**, aucune différence d'issue (DTO byte-identique hors le champ) ; case décochée ou absente → dépôt enregistré ; toute lecture de ce champ par le calcul de score → **gate rouge**.
  <br>_module 9 · étape 5 · phase 1 · tâches : `SEC-12`, `UX-P1-02`, `UX-P1-15`_ · _source : contrat art. 8.4 ; synthèse du 2026-09-03, A-9_
- **REQ-UX-037** — Écrans console **Attributions** (filtres état / département / apporteur, rattachement manuel motivé et tracé) et **Contrats** (versions signées, relances) ; les actions suspension / résiliation / levée de gel sont portées par un rôle.
  <br>_module 21 · étape 12 · phase 1 · tâches : `UX-P1-13`_ · _source : juges ; tableaux-de-bord §4.3_
- **REQ-UX-038** — Tout dépôt reçoit un accusé immédiat ; à J+5 ouvrés sans qualification, un e-mail automatique non accusatoire part **une seule fois** (« nous n'avons pas encore pu joindre … »).
  <br>_module 12 · étape 8 · phase 1 · tâches : `DM-13`, `UX-P1-10`_ · _source : juges ; tableaux-de-bord §4.2 « le silence tue le réseau »_
- **REQ-DM-041** — Le payload d'un événement du journal est un schéma Zod **fermé** par type : identifiants, hashes, enums, montants, horodatages — **jamais** nom, e-mail, téléphone, IBAN, adresse ; gate `journal:sans-pii` ; après un effacement de tiers, `verifierChaine()` reste vrai. **Porteurs : `DM-01` et `DM-20`** — cette exigence fondait la gate `journal:sans-pii` (phase 0) sans qu'aucune tâche ne la cite : la gate existait sans porteur (corrigé le 2026-09-03).
  <br>_module 4 · étape 4 · phase 0 · tâches : `DM-01`, `DM-20`_ · _source : juges — adversaire ; RGPD art. 17 vs journal immuable_
- **REQ-QA-035** — Le score de candidature est calculé **une seule fois par axionia** et transporté figé (total, parts, `scoreBaremeVersion`) ; Partners ne le recalcule jamais ; test de parité sur fixtures exportées.
  <br>_module 2 · étape 2 · phase 0 · tâches : `DM-06`, `INT-T22`_ · _source : juges ; F-DM-01_

### extensions du 2026-09-03 (REQ-EXT)

- **REQ-EXT-001** — La fiche prospect de l'espace présente **une seule frise** mêlant les événements produits par Axion-IA (qualification, rendez-vous, devis, facture, encaissement) et les échanges saisis par l'apporteur, triés par date décroissante ; chaque ligne porte son origine (`axion` | `apporteur`) et, pour un échange, sa visibilité.
  <br>_module 12 · étape 8 · phase 1 · tâches : `EXT-T01`_ · _source : EXTENSIONS §2.1_
- **REQ-EXT-002** — Un échange saisi par l'apporteur porte : date, canal enum `{appel, email, visite, linkedin, autre}`, texte libre, prochaine action et sa date (facultatives). 🔴 **Le drapeau `transmisAxion` est SUPPRIMÉ — il n'existe plus d'échange « privé »** (réécrite le 2026-09-03, M-14, « le choix sobre »). Le produit faisait héberger par la Société des échanges portant sur des **personnes physiques**, marqués `transmisAxion = false` par défaut, donc **invisibles d'elle** : c'est de la sous-traitance au sens de l'article 28 du RGPD, et cela dément frontalement l'article 7.5 du contrat (« aucune des parties n'agit pour le compte de l'autre »). En les rendant tous visibles, **l'espace entier repasse sous la responsabilité de traitement unique de la Société** — la voie sobre, sans coût charte relationnelle puisque REQ-EXT-003 garantit déjà qu'aucun échange n'est obligatoire ni compté. L'alternative (une annexe 3 de sous-traitance art. 28, sa gouvernance, ses audits) est plus lourde pour un gain nul. La politique de confidentialité (REQ-JUR-025) dit explicitement que les échanges saisis sont visibles de la Société, et l'écran de saisie le rappelle **avant** la première saisie. Tests : le champ `transmisAxion` absent du schéma, du DTO et de l'UI → **gate rouge** s'il réapparaît ; console et espace voient le même jeu d'échanges.
  <br>_module 20 · étape 7 · phase 1 · tâches : `EXT-T01`_ · _source : EXTENSIONS §2.2 ; contrat art. 7.5 ; RGPD art. 28 ; synthèse du 2026-09-03, M-14_
- **REQ-EXT-003** — 🔴 **Charte relationnelle** : aucun champ de suivi n'est obligatoire ; le nombre d'échanges n'entre ni dans le palier de confiance, ni dans le score, ni dans un classement, ni dans une alerte ; aucune relance automatique du type « vous n'avez rien saisi depuis N jours » n'existe. Test : une entreprise sans aucun échange n'affiche aucune alerte et ne change aucun compteur.
  <br>_module 13 · étape 10 · phase 1 · tâches : `EXT-T01`, `EXT-T02a`_ · _source : EXTENSIONS §2.3 ; plan §3.2_
- **REQ-EXT-004** — Les échanges suivent la durée de conservation de leur attribution : purgés **avec elle** par le cron existant (90 j après libération, 3 ans après le dernier contact si convertie) ; l'export RGPD de l'apporteur les inclut ; un effacement demandé par le tiers vide le **contenu** des échanges le concernant en conservant la ligne de journal.
  <br>_module 20 · étape 7 · phase 1 · tâches : `EXT-T01`_ · _source : EXTENSIONS §2.4_
- **REQ-EXT-005** — La fiche prospect côté console montre **les échanges** (tous — la notion d'échange privé est supprimée, REQ-EXT-002 / M-14), la fiche de qualification, les lignes de commission rattachées et le journal immuable ; la navigation est **bidirectionnelle** : d'une ligne de commission vers la fiche, et de la fiche vers l'encaissement. ⚠️ Voir un échange **ne crée aucun compteur** : le nombre d'échanges n'entre ni dans le palier, ni dans le score, ni dans un classement, ni dans une alerte (REQ-EXT-003, inchangée).
  <br>_module 20 · étape 7 · phase 1 · tâches : `EXT-T02a`, `EXT-T02b`_ · _source : EXTENSIONS §2.5 ; tableaux-de-bord §4.4 ; synthèse du 2026-09-03, M-14_
- **REQ-EXT-006** — L'écran « Vérifier une entreprise » distingue `libre` de `libre_deja_travaillee` (HYP-W7) : le second affiche « déjà travaillée par le réseau par le passé », **sans** identité, **sans** date précise, **sans** résultat. Les libellés des états `suivie_*` et `non_disponible` restent inchangés et mutuellement indistinguables.
  <br>_module 10 · étape 7 · phase 1 · tâches : `EXT-T06`_ · _source : EXTENSIONS §3.2_
- **REQ-EXT-007** — Un apporteur dont une part de dépôts au-delà du seuil de configuration d'une fenêtre glissante portent sur des entreprises `libre_deja_travaillee` produit un signal d'anomalie (ramassage), jamais un refus.
  <br>_module 10 · étape 7 · phase 1 · tâches : `EXT-T06`_ · _source : EXTENSIONS §3.2_
- **REQ-EXT-008** — Une candidature porte `origine` enum `{tunnel, saisie_console, import, parrainage}`, `canal` (contraint par la liste des campagnes) et `campagneId` ; sans ces trois champs, le coût par apporteur actif n'est pas calculable et la candidature est refusée à l'enregistrement.
  <br>_module 14 · étape 2 · phase 1 · tâches : `EXT-T03`, `EXT-T05`_ · _source : EXTENSIONS §4.2 ; REQ-CPL-015_
- **REQ-EXT-009** — À la création d'une candidature, quelle que soit son origine, une recherche par hash d'e-mail **et** de téléphone normalisés est faite : une correspondance **rattache** à la candidature existante au lieu d'en créer une seconde, et le journal enregistre le rattachement.
  <br>_module 1 · étape 2 · phase 1 · tâches : `EXT-T03`, `EXT-T05`_ · _source : EXTENSIONS §4.2_
- **REQ-EXT-010** — Le score de candidature est calculé par la **même fonction pure** quelle que soit l'origine ; si les champs nécessaires manquent, la candidature porte `score_indisponible` et l'écran affiche « à qualifier à la main » — jamais un score partiel ni un zéro.
  <br>_module 2 · étape 2 · phase 1 · tâches : `EXT-T03`_ · _source : EXTENSIONS §4.2 ; F-DM-01_
- **REQ-EXT-011** — Un CV est accepté en PDF, DOCX, JPG ou PNG, ≤ 10 Mo, **type réel vérifié par ses octets d'en-tête** (jamais par l'extension) ; stocké en privé, servi uniquement par URL signée de courte durée, accessible aux seuls rôles console ; purgé avec la candidature à l'échéance du vivier (2 ans), y compris si la personne est devenue apporteuse.
  <br>_module 1 · étape 2 · phase 1 · tâches : `EXT-T04`_ · _source : EXTENSIONS §4.3_
- **REQ-EXT-012** — Aucune donnée sensible n'est extraite, stockée ni affichée depuis un CV (âge, photographie, origine, santé, situation familiale) ; l'écran de candidature ne présente que les champs du modèle de candidature.
  <br>_module 1 · étape 2 · phase 1 · tâches : `EXT-T04`_ · _source : EXTENSIONS §5.3 ; RGPD art. 9_
- **REQ-EXT-013** — ⚠️ Si l'extraction assistée de CV est activée (`CV_EXTRACTION_ENABLED`, **fermé par défaut**) : elle **extrait des faits** vers les champs du formulaire, chacun modifiable, rien n'étant enregistré sans validation humaine ; elle ne produit **ni note, ni rang, ni avis de compatibilité** ; elle ne rejette, n'écarte, ne masque ni ne trie aucune candidature ; ce qui a été proposé et ce qui a été corrigé sont journalisés.
  <br>_module 1 · étape 2 · phase 3 · tâches : `EXT-T11`_ · _source : EXTENSIONS §5.2 ; règlement (UE) 2024/1689 annexe III point 4 ; RGPD art. 22_
- **REQ-EXT-014** — Le candidat reçoit une mention d'information (RGPD art. 13) précisant les données collectées, la durée de conservation, le fait qu'un score **oriente l'ordre des rappels sans jamais rejeter**, et son droit à un réexamen humain.
  <br>_phase 1 · tâches : `EXT-T04`_ · _source : EXTENSIONS §5.3_
- **REQ-EXT-015** — L'attribution capture, à la déclaration, la **latitude et la longitude** du siège renvoyées par l'API publique, avec les champs structurés déjà prévus ; la projection en coordonnées d'affichage est faite **côté serveur**, jamais dans le navigateur.
  <br>_module 9 · étape 7 · phase 1 · tâches : `EXT-T08`_ · _source : EXTENSIONS §1.3_
- **REQ-EXT-016** — La carte de la console porte trois couches : **couverture** (choroplèthe départementale : apporteurs actifs pour 10 000 entreprises employeuses — c'est la définition de « zone saturée » du scoring), **activité** (choroplèthe : attributions converties sur 12 mois glissants et CA), **détail** (un point par entreprise attribuée).
  <br>_module 17 · étape 10 · phase 3 · tâches : `EXT-T09`_ · _source : EXTENSIONS §1.1_
- **REQ-EXT-017** — Sur la couche de détail, la couleur porte l'**état** de l'attribution (6 valeurs), jamais l'identité de l'apporteur ; un apporteur se sélectionne par filtre et met ses points en surbrillance, les autres passant en gris. Chaque état porte en outre une forme ou un motif : l'information ne dépend jamais de la seule couleur.
  <br>_phase 3 · tâches : `EXT-T09`_ · _source : EXTENSIONS §1.2 ; WCAG 1.4.1_
- **REQ-EXT-018** — La carte est un segment de route **chargé à la demande** ; la vue par défaut de l'écran Territoire reste le tableau. Budget : route sans carte ≤ 75 KB gz, avec carte ≤ 140 KB gz, mesurés par le script maison. Aucune bibliothèque cartographique externe, aucune tuile, aucun appel réseau tiers : un SVG de départements simplifié au build. Au-delà de 2 000 points, agrégation par commune côté serveur.
  <br>_phase 3 · tâches : `EXT-T09`_ · _source : EXTENSIONS §1.3_
- **REQ-EXT-019** — L'apporteur dispose de la même carte, restreinte à **ses** entreprises par `forApporteur()` ; celles des autres n'y figurent sous aucune forme, pas même en gris — un point gris est une information.
  <br>_module 13 · étape 10 · phase 3 · tâches : `EXT-T10`_ · _source : EXTENSIONS §1.4 ; R3_
- **REQ-EXT-020** — Si la prolongation de fenêtre est retenue (HYP-W9) : une attribution dont un devis est `en cours` au terme des 12 mois est prolongée **une seule fois de 3 mois**, automatiquement, les deux parties étant notifiées ; la prolongation est journalisée et visible dans la fiche.
  <br>_étape 9 · phase 2 · tâches : `EXT-T07`_ · _source : EXTENSIONS §3.3_
- **REQ-EXT-021** — 🔴 **Séquence verrouillée.** L'ordre `KYC complet → grille du contrat fixée et COMPLÈTE (30/30) → génération du contrat → envoi DocuSeal → signature` est une contrainte dure, pas un ordre d'écran. Trois contrôles bloquants, **chacun vu rougir** : (a) la grille n'est éditable que si le KYC est complet ; (b) la fonction de génération du contrat refuse une grille absente ou incomplète — `Contrat.grilleId` est **non nul** en base (contrainte NOT NULL) ; (c) la fonction d'envoi refuse un contrat dont la grille n'est pas figée. L'envoi de l'enveloppe reste réservé au rôle `admin` et journalisé. Une fois le contrat signé, la grille du contrat est **immuable** : toute modification passe par un avenant et une re-signature (REQ-CPL-028, contrat art. 13).
  <br>_module 5 · étape 5 · phase 1 · tâches : `DM-23`_ · _source : GRILLE-PAR-APPORTEUR §6.3 ; décision de Will du 2026-09-03_
- **REQ-EXT-022** — 🔴 **La granularité est le palier, pas la famille.** Une `GrilleContrat` porte **une ligne par palier vendable** des quatre familles commissionnées, soit **30 lignes** : 9 formations collectives, 5 accompagnements 1-to-1, 5 implémentations, 11 audits. La durée (demi-journée, 1 jour, 2 jours) est un **axe distinct** : deux paliers de durées différentes sont deux lignes. La liste des 30 paliers est **dérivée de `pricing.ts`** par le script d'export (REQ-DM-014), jamais retapée ; un test compare le nombre et les identifiants de paliers de la grille publiée à ceux de `COMMERCIAL_COMMISSIONS`. Un forfait sur une formation d'une journée et un pourcentage sur les audits, pour le **même** apporteur, est le cas nominal, pas une exception. **Les cinq familles non commissionnées — développement web, maintenance, coaching récurrent, conférences, interventions sur demande — n'ont aucune ligne de grille** et sont énumérées **explicitement** sous l'annexe 1 du contrat : c'est le silence, et non l'omission, qui crée le litige.
  <br>_module 13 · étape 10 · phase 1 · tâches : `DM-23`, `T-ARG-037`_ · _source : GRILLE-PAR-APPORTEUR §1.1, §6.1 ; décision W6 du 2026-09-03_
- **REQ-EXT-023** — 🔴 **Modèles de grille et édition en masse.** Saisir 30 lignes à la main à chaque recrutement est intenable, et une ligne oubliée devient une commission bloquée le jour d'une vente. Deux mécanismes sont **obligatoires** : (a) `GrilleModele` — un jeu **complet** de 30 lignes, nommé et réutilisable (« Standard », « Tête de réseau », « Zone difficile ») ; on part d'un modèle et on ajuste ligne à ligne ; créer ou publier un modèle est une action `admin` journalisée ; (b) **édition en masse** — « toutes les formations à 300 € », « tous les audits à 25 % » en une action, puis dérogation ligne par ligne. Un modèle ne s'applique **jamais** rétroactivement à un contrat déjà signé.
  <br>_module 13 · étape 10 · phase 1 · tâches : `DM-23`, `UX-P1-14`_ · _source : GRILLE-PAR-APPORTEUR §6.2 ; décision de Will du 2026-09-03_
- **REQ-EXT-024** — 🔴 **Complétude 30/30 bloquante.** Aucune ligne ne peut rester indéfinie : tant que les 30 paliers ne portent pas chacun une valeur (`forfait`, `pourcentage` **ou** `aucune`), le contrat **ne peut pas être généré**. L'écran affiche le compteur `n/30` et la liste nominative des paliers manquants ; `genererContrat()` lève une erreur typée `grille_incomplete` et n'écrit rien. Test vu rougir : une grille à 29/30 → génération refusée, aucun `Contrat` créé, aucun envoi.
  <br>_module 13 · étape 10 · phase 1 · tâches : `DM-23`, `UX-P1-14`_ · _source : GRILLE-PAR-APPORTEUR §6.2, §6.3_
- **REQ-EXT-025** — 🔴 **Palier créé après la signature.** Le catalogue bouge (trois fois en 2026) ; un palier ajouté après la signature n'existe pas dans les grilles déjà figées, et une vente sur ce palier ne trouve aucune ligne. Comportement retenu : la ligne de commission est créée `bloquee` avec **`motifBlocage = a_qualifier`** — le cas « barème indéfini », libellé apporteur « **prestation hors grille de commissions** » (REQ-DM-015, REQ-ARG-017, REQ-UX-011 ; `bareme_indefini` n'est pas une valeur de l'enum, arbitrage l. 410) — et une alerte console : **jamais** un taux deviné, **jamais** un 0 €, **jamais** un silence. 🔴 **La situation est réglée dans les SOIXANTE JOURS de l'encaissement** (`PALIER_HORS_GRILLE_JOURS`, SSOT ; contrat annexe A1.7 réécrite, A-11) : sans ce terme, la commission restait suspendue **indéfiniment à la seule volonté du débiteur** (art. 1304-2 C. civ.), puisque les deux issues — proposer un avenant, ou constater unilatéralement le non-commissionnement — dépendaient entièrement de la Société, et que l'art. 13.3 garantit à l'apporteur qu'il n'a pas à signer l'avenant. L'écran de rattrapage « **N contrats ne couvrent pas la prestation X** » liste les contrats concernés, **affiche l'échéance des 60 jours par ligne**, et laisse choisir contrat par contrat : avenant (REQ-CPL-028), ou décision écrite « non commissionnée pour ces contrats », **motivée, journalisée et portée à la connaissance de l'apporteur avec son motif**. 🔴 **À défaut de décision dans les soixante jours, la commission est due au taux ou au forfait que la GRILLE PUBLIÉE portait à la date de la vente** — un cron l'applique, écrit l'événement et débloque la ligne. C'est une valeur écrite, opposable et lisible des deux parties, et non un taux fabriqué par proximité de prix (arbitrage C-6, qui rejette le rattachement au « palier dont le prix de référence est le plus proche »). Le catalogue bouge — trois fois en 2026 — donc le cas n'est pas théorique. Tests : palier ajouté après signature → ligne `bloquee` motif `a_qualifier` + alerte + échéance affichée ; décision « non commissionnée » sans motif → refus ; **J+61 sans décision → ligne calculée sur la grille publiée à la date de vente, passée `acquise`, événement écrit** (vu rougir sur le blocage sans terme) ; deux passages du cron → un seul déblocage.
  <br>_module 13 · étape 10 · phase 1 · tâches : `DM-23`, `T-ARG-037`, `UX-P2-07`_ · _source : GRILLE-PAR-APPORTEUR §6.4 ; contrat annexe A1.5 et A1.7 ; synthèse du 2026-09-03, A-11 et arbitrage C-6_
- **REQ-EXT-026** — Une ligne de `GrilleContrat` porte : `palierId` (identifiant du palier, dérivé de `pricing.ts`), `prestationFamille` enum `{formation_collective, accompagnement_individuel, audit, implementation}`, `type` enum **`{forfait, pourcentage, aucune}`**, `montantCents` (**Int, centimes, HT** — non nul ssi `type = forfait`, CHECK), `tauxBps` (**Int, points de base** — le point de base vaut un centième de pour cent — non nul ssi `type = pourcentage`, CHECK) , **`cpfEligible` (Boolean, NOT NULL, DÉRIVÉ de `pricing.ts` par le script d'export de REQ-DM-014 — jamais saisi, jamais retapé)** et `motifEcart`. 🔴 **`cpfEligible` manquait, et toute la garde CPF reposait dessus** : REQ-JUR-023 fonde l'interdiction sur ce champ, le contrat y renvoyait deux fois (art. 8.1 al. 2 et annexe A1.6), et **aucune ligne de grille ne le portait** — la garde n'avait pas de donnée à lire (M-15). Il est présent sur les lignes de `GrillePubliee` **et** de `GrilleContrat`, et rendu en **colonne « CPF »** dans l'annexe 1 générée (REQ-EXT-028 a) ; si aucun palier n'est éligible à la date d'une version, l'annexe porte la mention explicite « à la date de la présente version, aucun palier de la présente grille n'est éligible au compte personnel de formation ». **Aucun flottant nulle part** : ni pour un montant, ni pour un taux. Les paliers « sur devis » (`impl-mission-pme`, `impl-mission-eti`, `impl-grand-programme`, `impl-ia-custom`) n'admettent pas `forfait` — un forfait n'a pas de sens sans prix affiché : seuls `pourcentage` et `aucune` sont acceptés (CHECK + test). C'est là que se règle la dégressivité : un taux plus bas sur `impl-grand-programme` que sur `impl-poc`.
  <br>_module 13 · étape 9 · phase 1 · tâches : `DM-23`_ · _source : GRILLE-PAR-APPORTEUR §2.1, §6.1_
- **REQ-EXT-027** — 🔴 **Motif d'écart obligatoire (W12).** La grille d'un contrat **peut** être inférieure à la grille publiée — tranché par Will le 2026-09-03 — et le contrôle console est donc un **avertissement**, jamais un refus : « cette ligne est inférieure à la grille publiée ». En contrepartie, **toute ligne qui diffère de la grille publiée, dans un sens ou dans l'autre, porte un `motifEcart` non vide, journalisé avec son auteur et sa date ; l'enregistrement de la grille est refusé tant qu'un écart est sans motif** (échec fermé, test vu rougir). L'écart, son motif, qui l'a fixé et quand sont visibles dans l'écran Contrats de la console. 🔴 **L'écart et son motif sont en outre portés à la connaissance de l'APPORTEUR AVANT la signature** (contrat art. 4.1 al. nouveau, M-9) : le produit stockait déjà `motifEcart`, mais rien n'obligeait à le montrer à celui qui signe — un contrat d'adhésion dont une ligne est inférieure à la grille publiée **sans que le signataire en soit informé** est le cas d'école de l'art. 1112-1 C. civ. Le **document de présentation qui accompagne l'enveloppe DocuSeal** (généré, jamais retapé) liste chaque ligne en écart, sa valeur publiée, sa valeur contractuelle et le motif ; l'envoi de l'enveloppe est **refusé** si une ligne en écart n'y figure pas. Un écart documenté et communiqué est une décision commerciale ; un écart inexpliqué ou caché nourrit l'argument du déséquilibre significatif (art. L.442-1 C. com.). Test : grille portant deux écarts → le document d'accompagnement les porte tous les deux avec leur motif ; écart absent du document → **envoi refusé** (vu rougir). La contrepartie publique — la page de commissions passe en **formulation indicative** — est portée par la tâche `JUR-T29`.
  <br>_module 5 · étape 5 · phase 1 · tâches : `DM-23`, `UX-P1-14`_ · _source : GRILLE-PAR-APPORTEUR §2.2 ; décision W12 du 2026-09-03_
- **REQ-EXT-028** — 🔴 **L'annexe 1 est GÉNÉRÉE depuis la grille du contrat, et le moteur lit `contrat.grilleId`.** (a) L'annexe 1 du gabarit est **produite** depuis les 30 lignes de la `GrilleContrat` — montants en euros HT pour les forfaits, taux et assiette pour les pourcentages, mention explicite pour les `aucune`, **colonne « CPF » dérivée de `cpfEligible`** (REQ-EXT-026, M-15) — **jamais retapée** ; un test compare, cellule à cellule, l'annexe rendue au contenu de la grille, et la gate `jur:grille-chiffree` refuse de publier un gabarit dont l'annexe 1 porte les mots « forfait », « barème » ou « pourcentage » sans valeur numérique dans la même cellule. Le **chapeau** de l'annexe rendue définit le `forfait` comme « dû **une fois par commande** portant ce palier, quel que soit le nombre de journées vendues, la durée étant déjà portée par le palier lui-même » (A-2) — un test compare cette phrase au comportement de REQ-DM-015, faute de quoi le contrat et le moteur divergent d'un facteur 2. (b) Le moteur de commissions lit **`contrat.grilleId`** de l'apporteur concerné, **jamais** la grille globale ni la dernière version publiée ; un test le prouve sur un jeu à deux versions, **posé sur le pivot de REQ-ARG-008 — la date de CONFIRMATION de l'attribution, jamais `dateRef`** (M-9) : attribution confirmée sous v1, devis signé après publication de v2 → la ligne est calculée avec **v1**. Les lignes déjà nées gardent leur `grilleVersionId` (REQ-DM-014, REQ-ARG-008).
  <br>_module 13 · étape 9 · phase 0 · tâches : `JUR-T02`_ · _source : GRILLE-PAR-APPORTEUR §2.3, §6.3 ; règle maison « dériver, jamais recopier »_
- **REQ-EXT-029** — Dans « Mes documents », l'apporteur voit **sa** grille — celle de la version de contrat en vigueur pour lui — rendue depuis `GrilleCommission.contenuJson` de son `Contrat.grilleVersionId`, **jamais** depuis la version courante ; l'historique des versions qu'il a signées est consultable. Il ne voit **jamais** la grille d'un autre apporteur, ni une moyenne, ni un rang, ni un écart au réseau (REQ-JUR-034, gate `jur:aucun-agregat-reseau`) ; toute grille demandée pour un autre apporteur répond **404, strictement identique à un identifiant inexistant** (REQ-SEC-009).
  <br>_module 8 · étape 10 · phase 2 · tâches : `UX-P2-04`_ · _source : GRILLE-PAR-APPORTEUR §2.3 ; R3 ; note interne (hors dépôt) §3.3_

### charte relationnelle (REQ-JUR-031 → 038)

- **REQ-JUR-031** — 🔴 La suspension des déclarations n'est déclenchée que par `non_confirme` (une entreprise déclare ne pas connaître l'apporteur) ou `fraude` : **aucun compteur de volume, de rythme, de délai de réponse ou de méthode ne peut l'atteindre** (contrainte de type sur l'entrée de la fonction + test d'appel). Le vocabulaire exclut « strike », « sanction », « avertissement », « faute » au profit de « déclaration non confirmée » et « contrôle de sincérité » ; aucun compteur de gradation n'est affiché en permanence dans l'espace ; **aucun barème de gradation n'existe, ni dans l'espace, ni dans le contrat, ni dans les e-mails : le contrat énonce une obligation de sincérité (art. 3.7) et une faculté de résiliation motivée (art. 11.2), sans échelle**. 🔴 **Aucune vue, aucun DTO et aucune fonction ne COMPTE les mises en demeure ni les suspensions d'un apporteur** (ajouté le 2026-09-03, M-17) : le journal étant append-only et conservé dix ans (REQ-DM-024), un compteur reconstitué a posteriori en console pèserait **plus lourd qu'un barème écrit**, parce qu'il aurait l'air d'avoir été caché — et le contrat dispose désormais qu'« aucune décision de la Société ne peut être fondée, en tout ou partie, sur le nombre de mises en demeure ou de suspensions antérieures » (art. 11.2). La fiche apporteur (REQ-UX-036) n'expose aucun compteur de ce type ; l'écran Anomalies non plus. **Test d'absence**, dans le prolongement de cette exigence : toute agrégation (`count`, `length`, `_count`) portant sur les événements de suspension ou de mise en demeure d'un apporteur → **gate rouge**, vue rougir sur un témoin.
  <br>_module 21 · étape 12 · phase 0 · tâches : `JUR-T24`, `JUR-T27`, `JUR-T28`, `SEC-14`, `SEC-15`, `UX-P1-12`_ · _source : note interne (hors dépôt) §3.2 A ; motif 6 ; décision du 2026-09-03 ; contrat art. 11.2 ; synthèse du 2026-09-03, M-17_
- **REQ-JUR-032** — 🔴 Le palier est un **seuil de vérification prioritaire**, jamais un objectif **et jamais un plafond** : l'espace n'affiche **jamais** ce qui manque pour l'étape suivante ; aucun compte à rebours, aucune barre de progression, aucune notification de franchissement ; jamais nommé « niveau », « statut », « rang » ni « grade » ; le seuil **ne redescend jamais** pour cause d'inactivité. 🔴 **Le libellé « vous pouvez déclarer jusqu'à 15 entreprises par semaine » est RETIRÉ** (corrigé le 2026-09-03, P-2) : il annonçait un plafond que REQ-DM-010 ne pose pas — « **aucun dépôt n'est refusé pour dépassement de seuil** », le seuil 5/15/25 déclenche une **vérification prioritaire**, pas un refus. Soit l'espace mentait sur l'étendue des droits (art. 1112-1 C. civ., dont l'al. 5 est d'ordre public et que l'art. 17 ne peut pas écarter), soit le plafond existait et était inopposable. Libellé unique, qui ne promet ni ne restreint : « **Vos déclarations au-delà de quinze par semaine font l'objet d'une vérification prioritaire ; aucune n'est refusée.** » Tests : snapshot du libellé ; toute formulation contenant « jusqu'à N » ou « plafond » sur ce composant → **gate rouge** ; un dépôt au-delà du seuil aboutit avec `verificationPrioritaire = true`.
  <br>_module 9 · étape 7 · phase 1 · tâches : `JUR-T24`_ · _source : note interne (hors dépôt) §3.2 B ; motifs 1 et 6 ; REQ-DM-010 ; art. 1112-1 C. civ. ; synthèse du 2026-09-03, P-2_
- **REQ-JUR-033** — 🔴 Aucun message n'est envoyé à un apporteur **parce qu'il n'a pas déposé** : la relance de dormance est remplacée par une **lettre du réseau identique pour tous**, à intervalle fixe, dont le job d'envoi **ne reçoit aucun filtre sur la date du dernier dépôt** (contrainte de signature + test). La notion de « dormant » reste un indicateur de console et ne sort jamais vers l'apporteur.
  <br>_module 19 · phase 1 · tâches : `JUR-T25`, `UX-P1-10`, `UX-P3-06`_ · _source : note interne (hors dépôt) §3.2 C ; motif 4 ; remplace REQ-CPL-027 côté envoi_
- **REQ-JUR-034** — Aucun DTO exposé à l'espace apporteur ne contient de valeur agrégée du **réseau** (moyenne, médiane, rang, percentile, total réseau, comparaison) : un apporteur ne voit que **ses** données. Gate `jur:aucun-agregat-reseau`.
  <br>_module 8 · étape 7 · phase 0 · tâches : `JUR-T26`_ · _source : note interne (hors dépôt) §3.3, §5.2 ; motifs 2 et 3_
- **REQ-JUR-035** — Aucun composant de l'espace n'est nommé ni typé comme une progression vers un seuil (`progress`, `remaining`, `toGoal`, `nextTier`, `objectif`, `restant`). Gate `jur:aucune-progression`.
  <br>_phase 0 · tâches : `JUR-T26`_ · _source : note interne (hors dépôt) §5.2 ; motif 1_
- **REQ-JUR-036** — Toute PR touchant `src/app/(espace)/**`, `emails/apporteur/**`, `micro-copy/**` ou les ressources diffusées porte le label `apporteur-facing`, **exige la revue du rôle `juriste`** (bloquante) et recopie dans son corps la checklist des 12 motifs interdits, chaque ligne cochée ; un motif coché « présent » bloque la fusion.
  <br>_phase 0 · tâches : `JUR-T26`_ · _source : note interne (hors dépôt) §5.3_
- **REQ-JUR-037** — La gate lexicale couvre **tout ce qui est vu ou reçu par un apporteur** et refuse : objectif, quota, cible, performance, résultats, productivité, taux de transformation, classement, top, meilleur, rang, niveau, strike, sanction, avertissement, relance, « vous devez », « il faut », « obligatoire », « pensez à », « cela fait longtemps », « votre manager », « votre responsable », « nos commerciaux », « notre force de vente », « formation obligatoire ».
  <br>_phase 0 · tâches : `JUR-T26`_ · _source : note interne (hors dépôt) §4 ; durcit GATE-JUR-TEXTES-APPORTEURS)_ 🔴 **Étendu le 2026-09-03 au vocabulaire du droit social** : sont également interdits partout où un apporteur peut les lire — **bulletin**, bulletin de commission, fiche de paie, net à payer, brut, salaire, rémunération mensuelle, charges salariales, cotisations retenues, attestation d'emploi, certificat de travail, solde de tout compte, congés, ancienneté, prime. Le document mensuel s'appelle **relevé de commissions** ; le document légal est l'**autofacture** ; le document de janvier est le **récapitulatif annuel des commissions versées**. _(le document mensuel n'emprunte ni le titre ni la mise en page d'un document social_
- **REQ-JUR-038** — À chaque fin de phase, le juriste repasse **tous** les écrans et **tous** les e-mails déjà livrés, pas seulement les nouveaux : un indice naît souvent de la **combinaison** de deux écrans anodins pris isolément. **Porteur : `GOV-022`**, dont l'acceptation porte la repasse intégrale (tous les écrans et tous les e-mails déjà livrés, pas seulement les nouveaux) ; aucune tâche ne citait cette exigence (corrigé le 2026-09-03).
  <br>_phase 3 · tâches : `GOV-022`_ · _source : note interne (hors dépôt) §5.4 ; appréciation par faisceau_
- **REQ-JUR-039** — 🔴 **Rien de ce que l'outil envoie n'est une instruction — la contrepartie produit de l'article 2.7.** Rien n'interdisait à la Société d'adresser des directives par l'espace, les notifications ou les e-mails : l'art. 17 les privait de valeur **contractuelle**, pas de leur qualité de **faits**, et le juge apprécie des faits (M-6). Le contrat s'engage désormais sur sa propre pratique (art. 2.7), et le produit porte cet engagement : **aucun message, notification, contenu ou fonctionnalité** de l'espace, des documents de présentation, de la documentation ou des courriers électroniques ne constitue une instruction, une directive, une consigne de méthode ou une **demande de compte rendu** — la gate lexicale de REQ-JUR-037 refuse l'impératif de méthode dans tout texte reçu par un apporteur. **L'apporteur n'est tenu à aucune fréquence de connexion** : aucune relance, aucun signal, aucune alerte et aucune conséquence ne naissent de l'absence de connexion, de l'absence de réponse à un message ou de leur délai — **`dernierUsageAt`, `lastSeenAt` et les délais de réponse n'entrent dans aucun score, aucun indicateur restitué, aucun DTO exposé et aucun déclencheur automatique** (garde AST, sœur de `jur:aucun-agregat-reseau`). Cette exigence suppose REQ-UX-016 corrigée (M-23), faute de quoi elle se contredit : un délai qui court depuis un dépôt dans l'espace **est** une obligation implicite de consultation. Tests : apporteur sans connexion depuis 200 jours → aucun message, aucun signal, aucun changement d'état ; toute lecture de `lastSeenAt` hors du module de session → **gate rouge**.
  <br>_module 9 · étape 7 · phase 1 · tâches : `JUR-T30`, `UX-P1-10`_ · _source : contrat art. 2.7 et 17 ; note interne (hors dépôt) §3.2 ; synthèse du 2026-09-03, M-6_
- **REQ-JUR-040** — 🔴 **`dateContact` est recueillie pour l'article 3.7 et pour rien d'autre.** C'est la **seule** donnée du contrat à partir de laquelle un rythme d'activité peut être reconstitué, elle est collectée à chaque déclaration, et le signal `nocturne` prouve que le produit savait déjà exploiter ce type d'information (M-28). Elle n'est pas nécessaire à l'attribution, qui se règle sur l'horodatage serveur (REQ-DM-005). Le contrat le dit désormais (art. 3.2 al. 2 : « laquelle n'est recueillie qu'aux fins de l'article 3.7 et ne fait l'objet d'aucune exploitation statistique »), et le produit le garde : **`dateContact` n'entre dans aucun agrégat, aucun indicateur restitué à l'apporteur, aucun signal d'anomalie et aucun déclenchement automatique** ; sa seule lecture autorisée est l'affichage de la fiche prospect et le contrôle de sincérité mené par un humain. Gate explicite, au même titre que `jur:aucun-agregat-reseau` : toute lecture de `dateContact` hors de ces deux modules → **rouge**, vue rougir sur un témoin (un `groupBy` par tranche horaire).
  <br>_module 9 · étape 7 · phase 1 · tâches : `JUR-T30`, `SEC-14`_ · _source : contrat art. 3.2 al. 2 ; REQ-SEC-017 (signal `nocturne` retiré) ; synthèse du 2026-09-03, M-28_
- **REQ-JUR-041** — 🔴 **Aucun droit d'usage du nom, du logo ou de la charte n'est concédé — et l'outil n'en distribue aucun.** L'ancien article 22 concédait un droit d'usage « de sa dénomination, de son logo et des supports » « à seule fin de **présenter ses prestations** » : depuis CJUE 4 juin 2020 (C-828/18) et Cass. com. 2 déc. 2020, autoriser à **présenter l'offre** est précisément ce qui caractérise la négociation au sens de L.134-1, et le port du nom crée en outre un mandat apparent (art. 1156 C. civ.) que l'art. 8.2 prétend interdire (A-8). L'article 22 réécrit ne concède plus aucun droit d'usage. Côté produit : (a) l'espace, les e-mails et le kit **ne mettent à disposition aucun fichier de logo, de charte, de gabarit de signature électronique, de visuel de profil ni de carte** — un test d'inventaire des ressources téléchargeables rougit si l'un d'eux apparaît ; (b) les **documents de présentation** sont servis en PDF **non modifiable**, transmissibles **en l'état**, et portent la mention de leur objet (identifier la Société et communiquer ses coordonnées) ; (c) **l'expression « kit de vente » est bannie** de tout support, de toute REQ, de toute tâche et de tout écran — le contrat ne peut pas nommer « vente » ce que l'art. 1.2 dit n'être pas une vente : le terme canonique est « **documents de présentation** » (glossaire, REQ-GOV-016), et la gate lexicale de REQ-JUR-037 le refuse. Tests : inventaire des ressources → aucun `.svg`/`.png`/`.ai` de marque, aucun gabarit de signature ; « kit de vente » présent dans `docs/`, `micro-copy/**` ou `emails/apporteur/**` → **rouge**, vu rougir sur les occurrences actuelles.
  <br>_module 5 · étape 5 · phase 0 · tâches : `JUR-T29`, `JUR-T30`, `UX-P1-11`, `UX-P3-02`_ · _source : contrat art. 22, 2.3, 8.2 et 17 ; CJUE C-828/18 ; Cass. com. 2 déc. 2020 ; art. 1156 C. civ. ; synthèse du 2026-09-03, A-8_
- **REQ-JUR-042** — 🔴 **Aucune résiliation pour inactivité — verrou produit, arrêté par Will le 2026-09-03 (§B.2).** L'article 11.1 permet déjà de résilier à tout moment, sans motiver, sous préavis de 30 jours : une clause de résiliation pour inactivité **n'apporterait rien** et réunirait les trois pouvoirs que la charte relationnelle interdit de réunir en une phrase — obligation de produire, mesure de cette production (`dormant` = 60 jours sans dépôt, le produit sait déjà la calculer), conséquence la plus lourde attachée à l'absence de travail. Quatre verrous, chacun testé : (1) `resiliationMotif` reste un enum **fermé** à `{ordinaire_apporteur, ordinaire_axion, manquement_grave}` (REQ-DM-011), gardé par un **test-cliquet sur l'enum** qui rougit à l'ajout de toute valeur ; (2) l'écran de résiliation de la console **n'offre aucun motif d'inactivité**, ni dans une liste, ni en texte libre (snapshot des options) ; (3) **aucun gabarit d'e-mail de résiliation ne mentionne l'absence de dépôt**, l'activité ou la dormance (liste noire lexicale sur `emails/apporteur/**`) ; (4) `dormant` reste un **indicateur de console** dérivé par fonction pure (REQ-CPL-027) et **n'entre dans aucun DTO exposé à l'apporteur** (gate `jur:aucun-agregat-reseau`). Si une purge du réseau devait un jour être décidée pour une raison de gestion, elle se décide **sur ce motif-là**, se journalise en `ordinaire_axion`, et **ne se sélectionne pas sur un critère d'activité** : le journal est inaltérable et conservé dix ans (REQ-DM-024) — ce qui aura servi à choisir sera lisible.
  <br>_module 18 · étape 12 · phase 1 · tâches : `JUR-T24`, `SEC-19`, `UX-P1-10`, `UX-P1-12`_ · _source : note interne (hors dépôt) §3.2 ; contrat art. 2.2 et 11.1 ; REQ-DM-011, REQ-CPL-027, REQ-JUR-033 ; synthèse du 2026-09-03, réponse B.2 de Will_
- **REQ-GOV-031** — 🔴 **Règle de publication (dépôt PUBLIC, décision W13).** Le dépôt `axion-apporteurs` est public : tout ce qui y est poussé est définitivement lisible, y compris après un passage en privé (forks, caches, miroirs). Trois catégories n'y entrent **jamais** :
  <br>_module 9 · étape 7 · phase -1 · tâches : `GOV-000`, `GOV-036`_ · _source : critique de complétude (2026-09-03)_
